STATUS: PROPOSED — NOT APPROVED

# AD-01T — The `P-G` Sequence RATIFIED IN FULL, and the Attribution/Clash Decision Authority NAMED — reconciling the Project Owner's ten-step confirmation and authority clarification into AD-01J … AD-01S

**What this document is.** The Project Owner has answered the **one question AD-01S posed back to them** at
its [§6.2](./03s-c-xvi-no-special-weight-ratification-decision.md) — a plain-language restatement of the
whole `P-G` sequence, asking the owner to confirm **the order and the actor at each step**, or to correct
either. **The answer is YES, with ONE authority clarification.** The answer is **authoritative business
input.** It is **not re-argued, weighed against an alternative, ranked, hedged or softened anywhere below.**

**What this document does.** It records the decision verbatim; states the ratification in its own line;
**investigates the authority clarification rather than accepting the owner's characterisation of it**;
keeps the **three named decision points** sharply apart; verifies the confirmed sequence against **thirteen
named checkpoints, one at a time**; ties each of the owner's **three boundary statements** to what already
enforces it; reconfirms the owner's **twelve-item locked list** item by item against its exact source; names
the amendments it requires; and accounts individually for every open item it does **not** reach. **Every
amendment it finds necessary is NAMED and NOT MADE.**

**What this document is NOT.** Not an implementation, not a schema, not a migration, not a screen, not a
workflow, not an approval to build. **[§6](#6-recommendation)'s recommendation is advisory.** Nothing below
is self-authorizing. **No prior document is edited. AD-01N remains at `VALIDATE` and is not approved; AD-01O
remains advisory and is not approved; AD-01P, AD-01Q, AD-01R and AD-01S all remain at `VALIDATE` and none is
approved.**

**This is the LARGEST single discharge in this sub-series, and it is also a NARROW document.** **It ratifies
an entire multi-step RULE rather than a point within one — the first time the series has done that.** **It
adds, on its own account, exactly one constraint and one question.** **Both facts are stated together
because reporting either alone would misdescribe it.**

**Predecessors, all treated as input and none edited:** AD-01 · AD-01A · AD-01B · AD-01C · AD-01E ·
AD-01F · AD-01G · AD-01H · AD-01I · AD-01J · AD-01K · AD-01L · AD-01M · AD-01N · AD-01O · AD-01P ·
AD-01Q · AD-01R · **AD-01S**.

---

## How to read this document

| Label | Meaning |
|---|---|
| ⟦SOURCE⟧ | Quoted or closely paraphrased from the Master Spec, the consolidated Product-Owner requirements, or `ENGINEERING_RULES.md`. |
| ⟦PRODUCT-OWNER DECISION⟧ | Already decided by the owner and treated as fixed input. **Not re-argued.** Includes the decision ratified here in its entirety, AD-01S's `PO-S1`/`PO-S2`, AD-01R's `PO-R1`/`PO-R2`, AD-01Q's `PO-Q1`/`PO-Q2`, AD-01P's `PO-P1 … PO-P12`, AD-01O's `PO-O1 … PO-O14` and AD-01N's `PO-N1 … PO-N9`. |
| ⟦SETTLED⟧ | Forced by the architecture, or decided earlier and not reopened here. |
| ⟦ARCHITECT ANALYSIS⟧ | Reasoning. Not a decision, not a requirement, not approved. |
| ⟦ARCHITECT RECOMMENDATION⟧ | A proposal requiring the Project Owner's explicit written approval. **NOT APPROVED.** |
| ⟦BUSINESS DECISION REQUIRED⟧ | The sources — including this decision — are insufficient. Nothing is guessed (Spec **Rule 1**, **§88**, **§97**; consolidated **§1**, **§30**). |

### Numbering discipline, continued without collision

**High-water marks were VERIFIED against AD-01S's own text before anything was minted, not taken on trust,
and not taken from AD-01S's close reason either.** **AD-01S closed at `PF-79`, `A-50`, `AC-32` (with `AC-33`
expressly NOT allocated), `X-25` (with `X-26` expressly NOT allocated) and `C-XIX` (with `C-XX` expressly
NOT minted).** **All five were re-derived from the file itself and all five check out.**

**Findings continue AD-01P's, AD-01Q's, AD-01R's and AD-01S's shared series as `PF-80 … PF-94`.**
⟦ARCHITECT ANALYSIS⟧ **No `TF-` series is minted, for the reason AD-01Q gave for declining `QF-`, AD-01R for
declining `RF-` and AD-01S for declining `SF-`: continuing `PF-` costs nothing and collides with nothing.**

**ONE new constraint IS minted, as `C-XX`, and the `C` series moves to `C-XX` for the first time since
AD-01Q.** ⟦ARCHITECT ANALYSIS⟧ **AD-01S declined `C-XX` because a pure ratification needs no new mechanism.
This document is NOT a pure ratification — [§2.2](#22-the-authority-clarification-investigated) finds that
the owner's authority clarification is NEW INFORMATION and not a restatement, and a narrowing of an
⟦SOURCE⟧-level term that must survive contact with `R2` needs its own numbered clause or it will be
implemented as a role-name branch. `C-XX` is that clause and it is the whole of what this document adds to
the `C` series** — [§3.2](#32-c-xx-the-authority-clause-minted-and-stated-in-full).

**ONE new open item IS minted, as `AC-33`, and the `AC` series moves to `AC-33`.** ⟦ARCHITECT ANALYSIS⟧
**This is a CHECKED RESULT and not an artefact of momentum — [§5.1](#51-the-one-new-question-ac-33). The
question it asks did not exist in answerable form before the owner named the step-7 authority, and it is
NOT a duplicate of `AC-27` or of `AC-13`; the difference is shown rather than asserted.**

**NO new contradiction is minted. The `X` series stands at `X-25` and `X-26` remains NOT allocated.**
⟦ARCHITECT ANALYSIS⟧ **This was CHECKED rather than assumed —
[§3.7](#37-no-existing-rule-is-contradicted) runs the search, and the candidate that had to be ruled out was
the obvious one: whether the owner's named authority CONTRADICTS ⟦SOURCE⟧ §11's generic phrase. It does not,
and the reason is given.**

**Amendments that are logically required but deliberately NOT made continue as `A-51`, `A-52` and `A-53`**
(`A-1 … A-50` remain named and not made). ⟦ARCHITECT ANALYSIS⟧ **THREE — the same count as AD-01R and
AD-01S, reported rather than fitted.** **`A-38`(b) is CONTENT-RATIFIED by this decision, NOT RENUMBERED and
still NOT MADE**, on the precedent AD-01R set for `A-32` and AD-01S followed for `A-39` —
[§4.1](#41-the-three-amendments-named-and-not-made).

> ⟦ARCHITECT ANALYSIS⟧ **One collision warning, stated once and then relied upon.**
>
> **`PO-T1`, `PO-T2` and `PO-T3` are this document's labels for the owner's three clauses, and the `PO-`
> prefix is load-bearing and is never dropped.** **`PO-T1·1 … PO-T1·10` are the owner's ten confirmed steps;
> `PO-T2` is the authority clarification; `PO-T3` is the four-bullet authority model attached to it.**
> **The numbering is the architect's; the content is the owner's.** **No bare `T-n` identifier is used
> anywhere for these** — ⟦SETTLED⟧ **`T-1`, `T-3`, `T-5`, `T-6 … T-11` are AD-01C's and AD-01N's pre-existing
> `T` series and are untouched, unreferenced as owner clauses, and not reopened.**

### Scope discipline, stated once and honoured throughout

No SQL, schema, table, column, migration, master value or type appears anywhere. No screen, workflow,
approval step, queue, notification or gate is designed (AD-01A **§8.5**). **No commission formula, rate,
slab, percentage, amount, milestone value, clawback quantum, passback rule or TDS treatment appears
anywhere — `M-9` owns all of it. No lock-in number is proposed. No boundary convention is selected —
`AC-6` is NOT resolved here and [§3.6](#36-the-ac-6-investigation) explains at length why not.**

**NO PRECEDENCE RULE BETWEEN TWO CPs IS INVENTED, PROPOSED, RANKED, HINTED AT OR IMPLIED. `AC-15`/`AC-20`
are NOT resolved here.** ⟦ARCHITECT ANALYSIS⟧ **This document names WHO decides a clash. It does not supply
WHAT they apply, and the distinction is the single most over-readable thing in it** —
[§3.3](#33-the-thirteen-checkpoints-verified-one-at-a-time) checkpoint 5. **`C-IX`'s prohibition stands
exactly as it stood.**

**NO LITERAL ROLE-NAME BRANCH, ENUM, ROLE KEY, PERMISSION KEY OR ROLE HIERARCHY IS INVENTED, PROPOSED OR
IMPLIED ANYWHERE.** ⟦SOURCE⟧ **R2**: *"Application logic never branches on a role name."* **The owner naming
a role is a BUSINESS fact about who holds an authority. It is not a licence to test a name in logic, and
`C-XX` exists to make that impossible to misread** — [§3.2](#32-c-xx-the-authority-clause-minted-and-stated-in-full).

**NO ANTI-ABUSE MECHANISM, THRESHOLD, CADENCE, CAP, RATIO, SCORE, FLAG, ALERT OR DETECTION RULE IS INVENTED,
PROPOSED, DESIGNED OR IMPLIED ANYWHERE — `AC-32` is NOT answered and NOT narrowed in what it asks.** **No
re-registration cadence, minimum gap or cap is invented. No Dump-reason value is proposed — `N-4` owns every
value. No adjudication procedure, workflow, queue, notification or UI is designed around §11.**

**`Q1`, `Q4`, `Q6`, `Q7`, `V-1`, `V-2`, `V-3`, `V-7`, `V-11`, `V-12`, AD-01J's `J-2`, AD-01K's `K-18`,
`K-26`, `K-52` and `T-5`, and AD-01L's `L-6` and `K-22` are NOT reopened — and `K-18` is CONFIRMED for a
SEVENTH consecutive document.** **`Q-OV` and `Q-AR` are adopted as AD-01Q recommends them and are NOT
re-derived and NOT narrowed.** **`PO-R1` and `PO-S1` are LOCKED and neither is reopened, qualified or
conditioned by anything below.**

---

## 1. The decision

### 1.1 The source of this decision

> ⟦ARCHITECT ANALYSIS⟧ **`PF-80` — recorded first, because the provenance determines which register row the
> answer may be posted against, and this time it is again the row it was addressed to, and it is the row
> that was the head of the whole gating list.**
>
> **AD-01S closed at `VALIDATE` with an EIGHT-item gating list at its §6.1, restating AD-01R's eight with
> gate 2's head limb discharged. Its new GATE 2 HEAD was `P-G` itself, with `A-38`(b)'s seventh step, and
> AD-01S §6.2 posed it back to the owner in their own plain register as the single next question:**
>
> > *"When a partner's registration is used as the basis for a claim on a later enquiry, the steps we have
> > written down run in this order: … the claim is then argued out and decided by your authorised leadership
> > exactly like any other, and it can lose; an authorised holder may afterwards override that decision; and
> > only then, and only if the enquiry converts, does anyone look at commission eligibility at all. **Is
> > that the right order, with the right people at each step** — or does any step belong to someone else, or
> > happen at a different point?"*
>
> ⟦PRODUCT-OWNER DECISION⟧ **The owner has answered: YES — that is the correct order — with ONE authority
> clarification.**
>
> **This answer lands on the row it was addressed to, and it lands on BOTH halves of it.** ⟦ARCHITECT
> ANALYSIS⟧ **AD-01S's question asked two things in one sentence — *the right order* and *the right people
> at each step*. The owner answered the first with an unqualified YES and the second with a correction. That
> is the shape of answer a two-part question should produce, and it is the strongest available evidence that
> the question was understood as asked rather than skimmed.**
>
> **`P-G` is therefore the THIRD item in this series to be CLOSED BY A DIRECT PRODUCT-OWNER RULING rather
> than by an architect's derivation from other owner input** — the framing AD-01R introduced at its `PF-55`
> for `AC-14` and AD-01S followed for `C-XVI`. ⟦ARCHITECT ANALYSIS⟧ **The three are not the same kind of
> closure, and the difference is worth one sentence each. `AC-14` was an open QUESTION the architecture
> could characterise but not answer. `C-XVI` was a STATED CONSTRAINT the architecture had written in full
> and could not authorize. `P-G` is a STATED MULTI-STEP RULE — an order, a set of actors and a set of
> governing authorities — and it is the first RULE, as opposed to a question or a constraint, that the owner
> has ratified in this sub-series.**

### 1.2 The decision, recorded verbatim before any analysis

> ⟦PRODUCT-OWNER DECISION⟧ **Nothing in this section is the architect's, and nothing in it is weighed,
> ranked, softened, qualified or treated as an option.**

> ⟦PRODUCT-OWNER DECISION⟧ **`PO-T1` — the ten confirmed steps, verbatim:**

| # | Step, verbatim |
|---|---|
| **`PO-T1·1`** | *"A genuine Inquiry arrives and becomes its own Inquiry record."* |
| **`PO-T1·2`** | *"The system checks whether the relevant CP has a currently-live CP Project Registration for the same Client × Project."* |
| **`PO-T1·3`** | *"If such a registration exists, an attribution claim may be recorded against that Inquiry, explicitly citing that specific Registration record."* |
| **`PO-T1·4`** | *"The attribution claim is then evaluated through the normal attribution/clash-resolution process."* |
| **`PO-T1·5`** | *"The registration itself carries NO special weight, priority, head start, benefit of the doubt, automatic-win status, or extra post-win protection."* |
| **`PO-T1·6`** | *"The claim can lose."* |
| **`PO-T1·7`** | *"The authorised decision-maker resolves the attribution/clash."* |
| **`PO-T1·8`** | *"Site Head may exercise the separately defined attribution override mechanism where its conditions are satisfied."* |
| **`PO-T1·9`** | *"Only after attribution is resolved, and only if the Inquiry converts, is commission eligibility evaluated."* |
| **`PO-T1·10`** | *"Commission approval/payout remains downstream of commission eligibility."* |

> ⟦PRODUCT-OWNER DECISION⟧ **And the owner's own confirmation line, verbatim:**
>
> > ***"YES — that is the correct order."***

> ⟦PRODUCT-OWNER DECISION⟧ **`PO-T2` — the authority clarification, verbatim:**
>
> > *"However, make one authority clarification explicit: for a competing CP attribution/clash, the
> > decision-maker at step 7 is the previously established **Site Head / Project Head**, according to the
> > existing authority model. **Do NOT replace this with an undefined generic 'leadership' role.**"*

> ⟦PRODUCT-OWNER DECISION⟧ **`PO-T3` — the four-bullet authority model, verbatim:**
>
> > *"The authority model must remain:*
> > - *Normal CP-vs-CP attribution/clash decision → authorised Site Head / Project Head as already
> >   established.*
> > - *Site Head attribution override → the separately defined override authority/mechanism already
> >   established in AD-01Q.*
> > - *Commission approval → remains governed by the existing commission authority model; do not move this
> >   into the attribution decision.*
> > - *Registration itself never decides the winner."*

### 1.3 `P-G` IS RATIFIED IN FULL — stated in its own line

> ⟦PRODUCT-OWNER DECISION — THE RATIFICATION⟧
>
> **THE FULL `P-G` SEQUENCE — AS AMENDED BY AD-01R'S STEP-3 OPERAND CONFIRMATION AND AS RATIFIED AT STEP 5
> BY AD-01S — IS NOW RATIFIED IN FULL BY THE PROJECT OWNER, IN ITS ORDER AND IN ITS ACTORS.**
>
> **It is no longer an ⟦ARCHITECT RECOMMENDATION⟧. It is no longer *"still an architect derivation and still
> unratified."* It is no longer gate 2's head. It is no longer, in AD-01S's own words, *"the largest single
> thing standing between this sub-series and a buildable architecture."*** **An implementation that runs
> these steps out of order, at the wrong actor, or under the wrong authority is now breaching a
> PRODUCT-OWNER DECISION and not an unapproved architect recommendation.**
>
> **This is the THIRD item in the series closed by a direct Product-Owner ruling rather than by architect
> derivation — after `AC-14` at AD-01R and `C-XVI` at AD-01S — and it is the FIRST to ratify an entire
> multi-step RULE rather than a single point within one.**
>
> ⟦ARCHITECT ANALYSIS⟧ **What is ratified, stated precisely so it is not over-read.** **The ORDER of the
> steps, the ACTOR at each step, and the AUTHORITY governing each step are ratified.** **What is NOT thereby
> ratified is anything `P-G` merely cites, sits beside, or hands to: `C-XV`, `PF-6`, `PF-23`, `Q-OV`,
> `Q-AR`, `C-XVII`, `C-XVIII`, `C-XIX` and AD-01Q's minimum field set and permission are ALL STILL
> UNRATIFIED and NONE of them is ratified by anything below** — [§6](#6-recommendation). **And ratifying a
> rule's ORDER is not supplying the CONTENT of a decision the rule routes to: `AC-15`/`AC-20` are as
> unanswered after this document as before it.**

### 1.4 What this decision does NOT restate, and is therefore unchanged

> ⟦SETTLED⟧ **`PO-N1 … PO-N9`, `PO-O1 … PO-O14`, `PO-P1 … PO-P12`, `PO-Q1`, `PO-Q2`, `PO-R1`/`PO-R2` and
> `PO-S1`/`PO-S2` are untouched except where `PO-T1 … PO-T3` restate one of them, and every one of them
> remains in force.** In particular:

| Prior clause | Status after `PO-T1 … PO-T3` |
|---|---|
| **`PO-P1`** — a currently-valid registration MAY be the basis for CP attribution on a later inquiry, same client/project, subject to normal attribution/clash rules | **UNCHANGED and RESTATED IN SUBSTANCE by `PO-T1·2`/`PO-T1·3`** — the owner's *"currently-live … for the same Client × Project"* is `PO-P1`'s *"currently-valid"* and *"same client/project"*, and *"may be recorded"* is `PO-P1`'s *"MAY"* |
| **`PO-P2`/`PO-P4`** — registration is NOT automatic attribution and NOT an automatic clash winner | **RESTATED by `PO-T1·5` and by `PO-T3`'s fourth bullet — *"Registration itself never decides the winner"* — and therefore stated by the owner for a FIFTH time** counting `PO-O13`/`PO-O14`, `PO-P2`/`PO-P4`, `PO-R1·f` and `PO-S1·4`/`PO-S1·8` |
| **`PO-P3`** — registration is NOT automatic commission eligibility | **UNCHANGED, and RESTATED IN SEQUENCE FORM by `PO-T1·9`/`PO-T1·10`** — eligibility is reached only after attribution resolves and only if the Inquiry converts |
| **`PO-P5`** — expired registrations cannot establish new attribution claims | **UNCHANGED and UNTOUCHED, and CORROBORATED by `PO-T1·2`'s word *currently-live*** |
| **`PO-P10`** — the seven levels the architecture must never conflate | **UNCHANGED as a PROHIBITION, and NOT extended by `PO-T1`** — `PF-84`. **It remains INCOMPLETE in its ENUMERATION exactly as AD-01Q's `X-24`/`A-38`(a) left it and AD-01R and AD-01S each declined to fill, and this document does not fill it either** |
| **`PO-P11`/`PO-P12`** — the grounding registration record must be referenced and attribution traceable to the specific record | **RESTATED by `PO-T1·3`'s *"explicitly citing that specific Registration record"*, and therefore stated by the owner for a FOURTH time** counting `PO-P11`, `PO-P12` and `PO-Q2·c` |
| **`PO-Q1`** — the Site Head attribution/clash override | **UNCHANGED and UNTOUCHED, and POSITIONED.** `PO-T1·8` places it at step 8 and `PO-T3`'s second bullet routes it explicitly to *"the separately defined override authority/mechanism already established in AD-01Q"* — [§3.1](#31-the-three-decision-points-kept-distinct) |
| **`PO-Q2`** — the audit-reconstruction requirement | **UNCHANGED and UNTOUCHED. `Q-AR` gains no link and loses none** — [§3.3](#33-the-thirteen-checkpoints-verified-one-at-a-time) checkpoint 13 |
| **`PO-R1`**/**`PO-R2`** — `AC-14` = Option A, and the recorded anti-abuse tradeoff | **UNCHANGED, LOCKED, and NOT REOPENED BY ANYTHING BELOW** |
| **`PO-S1`**/**`PO-S2`** — `C-XVI` = No Special Weight, and the preserved chain | **UNCHANGED, LOCKED, and RESTATED by `PO-T1·5`/`PO-T1·6`, which are `PO-S1`'s ten points compressed into two** — [§2.1](#21-the-ten-steps-walked-one-at-a-time) |

---

## 2. What changes and what does not

### 2.1 The ten steps, walked ONE AT A TIME

> ⟦ARCHITECT ANALYSIS⟧ **`PF-81` — each of the owner's ten steps is checked AGAINST `P-G` AS AD-01P §3.1
> STATES IT, step for step, and against the owner's own prior `PO-` clauses. The classification is done by
> comparison and not by impression. `P-G` has SIX steps as AD-01P wrote it, plus `A-38`(b)'s proposed
> seventh; the owner has TEN. The mapping is not one-to-one and the discrepancy is worked rather than
> smoothed.**
>
> **`P-G` as AD-01P §3.1 states it, quoted once so every row below can be checked against it:**
>
> > **1** — an expression of interest arrives; `J-2` as bounded by `K-50` decides whether a new Inquiry
> > originates. **Who acts: the customer.**
> > **2** — the arrival reaches the server-side duplicate/clash gate. **Who acts: the system.** Which date
> > this is, is `AC-24`.
> > **3** — the grounding test runs at `G`: does a registration exist for `(CP c, this client, THIS
> > Inquiry's Project)` whose window contains `G`? **Who acts: the system.**
> > **4** — if it does, that registration is an ADMISSIBLE GROUND; a claim naming CP c **MAY** be recorded,
> > citing that specific registration RECORD. **Who acts: ⟦BUSINESS DECISION REQUIRED — `AC-25`⟧.**
> > **5** — the minted claim enters the claim set as an ORDINARY CLAIM, adjudicated under §11, **which it
> > may LOSE. Who acts: *builder-side authorized leadership*.**
> > **6** — if and only if RESOLVED IN CP c's FAVOUR on an Inquiry that CONVERTS, eligibility is tested as
> > the three-conjunct join. **Who acts: Accounts / the eligibility authorization.**
> > **7 (`A-38`(b), proposed)** — the `Q-OV` override step.

| # | The owner's step | Maps to | Verdict | What the comparison found |
|---|---|---|---|---|
| **1** | *"A genuine Inquiry arrives and becomes its own Inquiry record"* | **`P-G` step 1** | **EXACT RATIFICATION. NOTHING NEW** | **`J-2` as bounded by `K-50`.** ⟦ARCHITECT ANALYSIS⟧ **The owner's word *"genuine"* is `PO-R1·e`'s word, carried forward from AD-01R, and its work there was to distinguish the later real Inquiry from the earlier invalidated one — not to add a validity gate to `J-2`.** **`J-2` acquires NO new input** — the FIFTH consecutive document to report this |
| **2** | *"The system checks whether the relevant CP has a currently-live CP Project Registration for the same Client × Project"* | **`P-G` steps 2 AND 3, compressed** | **EXACT RATIFICATION of both, INCLUDING THE ACTOR. NOTHING NEW** | ⟦PRODUCT-OWNER DECISION⟧ **The actor is *"the system"* in the owner's own words, which is what AD-01P wrote for both steps.** **The operand set — CP × Client × Project, live at the check date — is `PO-P1` + `O-U` + AD-01R's `PF-57` owner-confirmed operand set, and NO operand is added or removed.** **WHICH DATE the check runs at is still `AC-24` and the owner does not name it** — [§3.3](#33-the-thirteen-checkpoints-verified-one-at-a-time) checkpoint 2 |
| **3** | *"If such a registration exists, an attribution claim may be recorded against that Inquiry, explicitly citing that specific Registration record"* | **`P-G` step 4** | **EXACT RATIFICATION, INCLUDING THE PERMISSIVE *MAY* AND THE RECORD-LEVEL CITATION. NOTHING NEW — and `AC-25`'s first half is NOT answered** | **`PO-P1`'s *MAY*; `PO-P11`/`PO-P12`'s specific-record requirement; `C-X` + `A-21`.** ⟦ARCHITECT ANALYSIS⟧ **The owner names NO actor for this step, exactly as `P-G` step 4 names none. That is not an omission the architect may fill: `AC-25` asks precisely whether the mint is automatic or a discretionary authorized act, and the owner's *"may be recorded"* is `PO-P1`'s *"MAY"* restated — permissive, and silent on by whom** — [§3.8](#38-which-open-items-this-decision-actually-reaches) |
| **4** | *"The attribution claim is then evaluated through the normal attribution/clash-resolution process"* | **`P-G` step 5, first limb** | **RATIFICATION, and this is `PO-S1·7` restated — the owner's THIRD statement of it** | ⟦PRODUCT-OWNER DECISION⟧ **`PO-P1`'s *"subject to normal attribution/clash rules"*, `PO-R1·e`'s *"subject to the normal attribution / clash-resolution process"*, `PO-S1·7`, and now `PO-T1·4`. FOUR statements** |
| **5** | *"The registration itself carries NO special weight, priority, head start, benefit of the doubt, automatic-win status, or extra post-win protection"* | **`P-G` step 5's `C-XVI` citation** | **`PO-S1` COMPRESSED. NOTHING NEW — and the compression is FAITHFUL, which was CHECKED** | ⟦ARCHITECT ANALYSIS⟧ **Checked term by term against `PO-S1·1 … PO-S1·10`: *weight* ← `·1`/`·2`/`·6`; *priority* ← `·6`'s *"not a precedence rule"*; *head start* ← `·1`; *benefit of the doubt* ← `·2`; *automatic-win status* ← `·3`/`·4`/`·8`/`·9`; *extra post-win protection* ← `·5`/`·10`. **All ten of `PO-S1`'s points are covered and none is dropped.** `C-XVI` is NOT re-ratified, NOT re-scoped and NOT weakened** |
| **6** | *"The claim can lose"* | **`P-G` step 5's *"which it may LOSE"*** | **EXACT RATIFICATION — and specifically of `C-XVI` COROLLARY 2. NOTHING NEW** | ⟦PRODUCT-OWNER DECISION⟧ **`C-XVI` corollary 2, now at owner status since AD-01S:** *"A minted claim may LOSE, and losing must be a reachable, recorded, permanent outcome."* **The owner has now stated it in four words of their own** |
| **7** | *"The authorised decision-maker resolves the attribution/clash"* | **`P-G` step 5's ACTOR** | ⟦PRODUCT-OWNER DECISION⟧ **THIS IS THE STEP `PO-T2` GOVERNS, AND IT IS THE ONLY ONE. The step is ratified; its ACTOR is NARROWED** | **See [§2.2](#22-the-authority-clarification-investigated), which is the analytical centre of this document. AD-01P wrote *"builder-side authorized leadership."* The owner has now named that authority. This is the ONE place the owner's words reach past the architect's** |
| **8** | *"Site Head may exercise the separately defined attribution override mechanism where its conditions are satisfied"* | **`A-38`(b)'s PROPOSED seventh step** | ⟦PRODUCT-OWNER DECISION⟧ **CONTENT-RATIFIES `A-38`(b). The seventh step is now the owner's own step 8, in the owner's own ordering** | **See `PF-83` below. `A-38`(b) is CONTENT-RATIFIED, NOT RENUMBERED and STILL NOT MADE** — [§4.1](#41-the-three-amendments-named-and-not-made) |
| **9** | *"Only after attribution is resolved, and only if the Inquiry converts, is commission eligibility evaluated"* | **`P-G` step 6** | **EXACT RATIFICATION of step 6's TWO CONJUNCTS AND ITS ORDERING. NOTHING NEW** | **`P-G` step 6: *"If and only if the claim is RESOLVED IN CP c's FAVOUR on an Inquiry that CONVERTS."*** ⟦ARCHITECT ANALYSIS⟧ **The owner's *"only after … and only if"* is AD-01P's *"if and only if"* split into its two conjuncts, and it is the third owner statement of the same ordering after `PO-P3` and `PO-S2`** |
| **10** | *"Commission approval/payout remains downstream of commission eligibility"* | **BEYOND `P-G`'s last step** | **NOT COVERED BY `P-G`'S ENUMERATION — and it is NOT NEW EITHER. It is `PO-S2`'s last link restated** | ⟦PRODUCT-OWNER DECISION⟧ **`PO-S2`'s chain already ended *"… commission eligibility → approval/payout"*, and `PO-Q2·c`'s ended *"… commission eligibility → commission approval/payout."* THIRD statement. ⟦ARCHITECT ANALYSIS⟧ `P-G` was never a rule about payout — `Q-AR` link 12 is where the series carries it — and the owner adding it to the sequence extends the SEQUENCE'S REACH, not `P-G`'s content** |

> ⟦ARCHITECT ANALYSIS⟧ **`PF-82` — the ten-versus-six discrepancy, worked rather than waved through, because
> a reader who is not told how the counts reconcile will suspect a step was added or lost.**
>
> | `P-G` step | Owner's step(s) | Note |
> |---|---|---|
> | **1** | **1** | One-to-one |
> | **2** and **3** | **2** | **The owner COMPRESSES two architect steps into one sentence.** ⟦ARCHITECT ANALYSIS⟧ **This is not a loss. AD-01P split them because step 2 fixes WHICH DATE the test runs at (`AC-24`, open) and step 3 is the test itself. The owner's sentence contains both and resolves neither, so the split survives and `AC-24` survives with it** |
> | **4** | **3** | One-to-one |
> | **5** | **4**, **5**, **6**, **7** | **The owner EXPANDS one architect step into four.** ⟦ARCHITECT ANALYSIS⟧ **This is the most informative fact in the mapping. AD-01P's step 5 bundled the process (*"adjudicated under §11"*), the constraint (`C-XVI`), the outcome (*"which it may LOSE"*) and the actor (*"builder-side authorized leadership"*) into one cell. The owner has unbundled exactly those four, and it is the ACTOR — their step 7 — that they then correct. A reader of AD-01P's single cell could have missed that the actor was generic; a reader of the owner's four steps cannot** |
> | **6** | **9** | One-to-one |
> | **7 (`A-38`(b), proposed)** | **8** | **The owner places the override BETWEEN step 7 and step 9 — i.e. after the ordinary decision and before eligibility.** ⟦ARCHITECT ANALYSIS⟧ **`A-38`(b) proposed it as a SEVENTH step appended after eligibility in AD-01Q's numbering; `PO-Q2·c` had already placed it *"attribution/clash decision → any Site Head override → Booking"*. The owner's step 8 agrees with `PO-Q2·c` and therefore with AD-01Q `PF-35`'s finding, and it CORRECTS the position implied by a seventh-step-appended reading.** **This is a POSITIONAL correction and it is recorded as one** → `A-51` |
> | **— (none)** | **10** | **The owner extends past `P-G`'s last step into approval/payout, restating `PO-S2`/`PO-Q2·c`** |
>
> **NET: every one of `P-G`'s six steps is present in the owner's ten; the proposed seventh is present and
> repositioned; one step is added beyond `P-G`'s reach and is not new content. NO STEP OF `P-G` IS DROPPED,
> NO STEP IS REORDERED RELATIVE TO ANOTHER, AND NO STEP ACQUIRES AN OPERAND.**

> ⟦ARCHITECT ANALYSIS⟧ **`PF-83` — step 8 and `A-38`(b), stated precisely because over-reporting it is the
> easiest error available here.**
>
> | | Finding |
> |---|---|
> | **What `PO-T1·8` DOES do** | ⟦PRODUCT-OWNER DECISION⟧ **It places the override IN THE SEQUENCE, at a determinate position, in the owner's own ordering, for the second time (after `PO-Q2·c`). It confirms the override is a SEPARATE ACT following the ordinary decision, not the same event.** **And `PO-T3`'s second bullet routes it by name to AD-01Q's mechanism** |
> | **What `PO-T1·8` does NOT do** | ⟦ARCHITECT ANALYSIS⟧ **It does not ratify `Q-OV`.** **`Q-OV` is AD-01Q's derived mechanism — its record shape, its minimum field set, its permission, its timing bound and its prohibitions. `PO-T1·8` says the owner's own `PO-Q1` grant sits at step 8 and is exercised *"where its conditions are satisfied."* WHAT those conditions are is `PO-Q1·b` (owner) plus `Q-OV` (architect, unratified) plus `AC-27 … AC-31` (open). Gate 3 is UNTOUCHED** |
> | **What `A-38`(b) gets** | **CONTENT-RATIFIED, NOT RENUMBERED, STILL NOT MADE.** ⟦ARCHITECT ANALYSIS⟧ **AD-01R set this precedent for `A-32` and AD-01S followed it for `A-39`: the owner has supplied the content; only the owner may make the amendment; the number does not move. And the amendment as `A-38`(b) states it now needs its POSITION corrected too** → `A-51` |

### 2.2 The authority clarification, INVESTIGATED

> ⟦ARCHITECT ANALYSIS⟧ **`PF-84` — this is the analytical centre of the document, and it is the one place
> where the architect's job is to check the owner's characterisation rather than to accept it. The owner
> asserts that *"Site Head / Project Head"* is *"the previously established"* authority *"according to the
> existing authority model"* — i.e. that this is ALREADY architecture. THE DECISION IS NOT RE-ARGUED. But
> whether it CONFIRMS prior architecture or SUPPLIES a narrowing for the first time is a question of fact
> about the sources, and this series has always worked that question explicitly. It was run against the
> text, not against memory.**

**The search, and exactly what was searched.**

| Source searched | For | Result |
|---|---|---|
| ⟦SOURCE⟧ **Master Spec §11 (Clash Detection)**, read directly and not through any prior document's paraphrase | *"Site Head"*, *"Project Head"*, any named role, any role hierarchy | **ABSENT.** §11's entire statement on the point is one sentence: *"**Builder-side authorized leadership resolves attribution.** Sales Reps should not automatically see sensitive clash information … The UI visibility rule must be enforced by authorization—not merely by hiding a badge."* **No role is named anywhere in §11** |
| ⟦SOURCE⟧ **Master Spec, whole document** | *"Site Head"*, *"Project Head"* | **PRESENT EXACTLY ONCE, at §03 (Primary Users):** *"Builder-side users: Super Admin, Builder Admin, CEO / Promoter, VP / Sales leadership, Sales Head, **Project Head / Site Head**, Sales Rep, Helpdesk, Sales Support, Accounts, Customer Support."* ⟦ARCHITECT ANALYSIS⟧ **That is a LIST OF DEFAULT ROLE LABELS. It assigns no decision to any of them, and §03's own closing line is *"Never solve permissions by simply hiding buttons in the frontend"* — a statement about enforcement, not about who adjudicates** |
| **AD-01J** and **AD-01K** — the documents that establish the §11 adjudication mechanism, `K-18` and `K-52`'s clash-scoping | *"Site Head"*, *"Project Head"* | **ABSENT. ZERO OCCURRENCES IN EITHER FILE.** ⟦ARCHITECT ANALYSIS⟧ **The two documents that built the attribution layer never name a role for it at all** |
| **AD-01L** and **AD-01M** | *"Site Head"*, *"Project Head"* | **ABSENT. ZERO OCCURRENCES IN EITHER FILE** |
| **AD-01N** | *"Site Head"* | **PRESENT — and every occurrence is at the COMMISSION ELIGIBILITY layer.** `PO-N8`: *"an authorized **Site Head** can explicitly override an **expired CP eligibility decision** and approve the commission as an exception."* `NF-24`'s subject is *"one (CP claim × booking) eligibility outcome."* `NF-26`(1) makes it a permission. **NOT ONE occurrence assigns Site Head the §11 attribution/clash decision** |
| **AD-01O** | *"Site Head"* | **PRESENT, and every occurrence cites `PO-N8`/`PO-N9`'s eligibility exception. NONE reaches the attribution/clash decision** |
| **AD-01P** — where `P-G` is defined | *"Site Head"* | **PRESENT SIX TIMES, and EVERY ONE is the `NF-24`/`PO-N8` ELIGIBILITY exception** (step 6's row, `PF-5` configuration 3, §3.14's `PO-N7` route, `PO-P10` level 7). **`P-G` STEP 5's own *"Who acts"* CELL READS: *"Builder-side authorized leadership."*** |
| **AD-01Q** — where `Q-OV` is established | *"Site Head"* | **PRESENT, and it names Site Head for the OVERRIDE — `PO-Q1·a`, the owner's own words. AND its §2.1 table, checked directly because it is the most likely place a generic-vs-named gap would show: the *"Who decides it"* cell for THE ATTRIBUTION / CLASH DECISION reads, in full, ⟦SOURCE⟧ **§11**: *"Builder-side authorized leadership resolves attribution."*** **GENERIC. The adjacent cell, for commission eligibility, names *"the eligibility authorization — `NF-25`, §40, §50."* The table names an authority for the eligibility column and cites a generic phrase for the attribution column** |
| **AD-01R** and **AD-01S** | *"Site Head"* | **PRESENT TWICE EACH, and all four occurrences are either `PO-N8`/`PO-N9`'s eligibility exception or `Q-OV`. NEITHER document names a role for the ordinary attribution/clash decision** |

> ⟦ARCHITECT ANALYSIS⟧ **THE FINDING, STATED PLAINLY AND WITHOUT SOFTENING.**
>
> > **`PO-T2` IS NEW INFORMATION. It is NOT a confirmation of something already explicit in the sources.**
> >
> > **No Master Spec section, and no document from AD-01J through AD-01S, names *Site Head* or *Project
> > Head* — or any other role — as the decision-maker for the ORDINARY ATTRIBUTION/CLASH DECISION.** **The
> > phrase every one of them uses is ⟦SOURCE⟧ §11's *"builder-side authorized leadership."*** **The owner
> > has NOW narrowed that generic phrase to a named authority, for this step, for the first time.**
>
> **What IS previously established, and must not be confused with it:** ⟦PRODUCT-OWNER DECISION⟧ **Site Head
> is previously established as the named authority for TWO OTHER decisions — the `PO-N8`/`NF-24` COMMISSION
> ELIGIBILITY EXCEPTION (AD-01N) and the `PO-Q1` ATTRIBUTION OVERRIDE (AD-01Q). Both are the owner's own
> prior clauses. Neither is the step-7 decision.** **The owner's characterisation is therefore accurate
> about the ROLE being previously established in this architecture, and inaccurate about it being
> previously established FOR THIS STEP. Both halves are reported.**
>
> ⟦ARCHITECT ANALYSIS⟧ **Why this matters enough to have been worked rather than assumed, in one line:**
> **a document that had recorded `PO-T2` as *"confirmed, already in the sources"* would have left no trace
> that an ⟦SOURCE⟧-level term had been narrowed by owner decision — and the next reader, finding §11's
> generic sentence and no clause narrowing it, would have had to re-derive the narrowing or ignore it.**
> **That is exactly the failure `A-51` and `A-52` exist to prevent.**

> ⟦ARCHITECT ANALYSIS⟧ **`PF-85` — the owner's prohibition, taken literally, and what it does and does not
> license.**
>
> **`PO-T2` ends: *"Do NOT replace this with an undefined generic 'leadership' role."*** **That is an
> instruction with a determinate target and it is obeyed: [§3.2](#32-c-xx-the-authority-clause-minted-and-stated-in-full)
> mints `C-XX` precisely so that the step-7 authority is DEFINED rather than left as a generic phrase, and
> `A-51`/`A-52` name the two places where the generic phrase currently stands unqualified.**
>
> **What it does NOT license, stated with equal force because the two instructions pull in opposite
> directions and BOTH must hold:** ⟦SOURCE⟧ **R2**, verbatim: *"Roles are tenant-scoped rows, not an enum …
> **Application logic never branches on a role name.** … Any logic of the form `if (role.key ===
> 'executive')` is disabled by a cosmetic edit."* **`PO-T2` names a ROLE. `R2` forbids branching on a role
> NAME. These do not conflict, and the resolution is the one AD-01N `NF-26`(1) and AD-01Q §3.4(1) already
> applied — twice, to two other permissions, both times to words the OWNER had written:**
>
> | | AD-01N's treatment of `PO-N8`'s *"Site Head"* | AD-01Q's treatment of `PO-Q1·a`'s *"Site Head"* | This document's treatment of `PO-T2`'s *"Site Head / Project Head"* |
> |---|---|---|---|
> | **How the owner's word is read** | *"a default role LABEL, not a permission"* — `NF-26` preamble | *"the owner naming the default holder, not as a licence to branch on a name"* — §3.4 preamble | **THE SAME. The owner is naming the DEFAULT HOLDER of an authority** |
> | **What is built** | A **permission** in the fixed vocabulary; the default role shipped as *Site Head* holds it by default | A **second, distinct permission**; same default-holder treatment | **A THIRD, DISTINCT permission** — `C-XX` |
> | **What a tenant may do** | Grant it to any tenant-defined role; renaming must not silently drop or grant it | Same | **Same** |
>
> **So `PO-T2` is honoured in substance — the authority is DEFINED, and it is defined as the owner's named
> default holders — while `R2` is honoured in form.** ⟦ARCHITECT ANALYSIS⟧ **This is not a compromise
> between the two instructions and it is not a softening of the owner's. It is the SAME construction the
> owner's own two prior role-namings already received in this series, applied a third time, and the owner
> has seen and not objected to it twice.**

### 2.3 The net, stated as arithmetic

> ⟦ARCHITECT ANALYSIS⟧ **`PF-86` — stated as arithmetic rather than as a claim, because this document's
> headline is large and a reader who is not told how much of it was already written will over-estimate what
> has moved.**

| | Count | Which |
|---|---|---|
| **Owner steps that are EXACT RATIFICATIONS of `P-G` as written, adding nothing** | **7** | `PO-T1·1`, `·2`, `·3`, `·4`, `·6`, `·9`, and `·5` (as `PO-S1` compressed) |
| **Owner steps that RESTATE the owner's own prior clauses** | **2** | `PO-T1·5` (`PO-S1`), `PO-T1·10` (`PO-S2`/`PO-Q2·c`) — counted again here because they are ratifications of nothing architect-derived |
| **Owner steps that CONTENT-RATIFY an existing NAMED-AND-NOT-MADE amendment** | **1** | `PO-T1·8` → `A-38`(b), and it CORRECTS its position |
| **Owner clauses that reach PAST anything in the sources** | **1** | **`PO-T2` — the step-7 authority.** `PO-T3` restates it plus three separations already established |
| **What the STATUS CHANGE is worth on its own** | **The whole of `P-G`** | ⟦ARCHITECT ANALYSIS⟧ **Seven exact ratifications are worth more than one novelty here, and saying so is the honest report. `P-G`'s value was never that its steps were surprising; it was that nobody had signed them. They are signed** |
| **New constraints** | **1** | `C-XX` |
| **New questions** | **1** | `AC-33` |
| **Open questions ANSWERED** | **0** | ⟦ARCHITECT ANALYSIS⟧ **Checked and reported as zero. `AC-24`, `AC-25`, `AC-15`/`AC-20`, `AC-6` and the rest are all exactly where AD-01S left them** — [§3.8](#38-which-open-items-this-decision-actually-reaches) |
| **Amendments required** | **3** | `A-51`, `A-52`, `A-53` |

---

## 3. The determinations

> ⟦ARCHITECT ANALYSIS⟧ **The determinations run in the order the reconciliation requires: first the crux
> (three decision points), then the clause it forces (`C-XX`), then the thirteen checkpoints, then the
> owner's boundary statements, then the owner's locked list, then `AC-6`, then the contradiction search,
> then the open-item accounting. *"Unaffected"* means CHECKED AND UNAFFECTED, never skipped.**

### 3.1 The three decision points, kept distinct

**THIS IS THE CRUX OF THE DOCUMENT. `PO-T3`'s four bullets exist to keep three decisions apart, and the
whole risk of an authority clarification is that naming one authority collapses it into another.**

> ⟦ARCHITECT ANALYSIS⟧ **`PF-87` — the three are set out side by side, in the form AD-01Q §2.1 established
> for two of them, because a comparison is the only presentation in which a collapse is visible.**

| | **(a) The ORDINARY attribution/clash decision** | **(b) The Site Head attribution OVERRIDE** | **(c) COMMISSION APPROVAL** |
|---|---|---|---|
| **Owner's step** | **`PO-T1·7`** (with `·4` as the process and `·6` as the admissible outcome) | **`PO-T1·8`** | **`PO-T1·10`**, downstream of **`PO-T1·9`**'s eligibility |
| **`PO-T3` bullet** | **1** — *"Normal CP-vs-CP attribution/clash decision → authorised Site Head / Project Head as already established"* | **2** — *"→ the separately defined override authority/mechanism already established in AD-01Q"* | **3** — *"remains governed by the existing commission authority model; do not move this into the attribution decision"* |
| **The question it answers** | ***"Who produced THIS Inquiry?"*** | ***"Notwithstanding that decision, what is the attribution on THIS Inquiry?"*** | ***"Is this CP entitled on THIS booking, and is it approved for payment?"*** |
| **Its subject** | **One Inquiry's attribution claim set** — `K-52`: *"clash is scoped to one Inquiry's claim set"*; `K-12` | **ONE named attribution/clash decision** — `PO-Q1·a`; `C-XVIII` forbids any standing or generalised form | **One (resolved claim × booking) pair** — `NF-24`'s Subject row: *"One determination — one (CP claim × booking) eligibility outcome"* |
| **Who decides it** | ⟦PRODUCT-OWNER DECISION⟧ **`PO-T2` — the authority whose DEFAULT holders are the roles shipped as *Site Head* / *Project Head*, held as a PERMISSION** → **`C-XX`.** ⟦ARCHITECT ANALYSIS⟧ **NEW at this document; previously ⟦SOURCE⟧ §11's generic *"builder-side authorized leadership"*** | ⟦PRODUCT-OWNER DECISION⟧ **`PO-Q1·a` — Site Head, as a SECOND, DISTINCT permission** — AD-01Q §3.4, requirement 2 | **A COMPOUND of separated authorities, NOT one** — see the note below this table |
| **When it happens** | **At §11 adjudication.** `C-IV` requires it to complete **before** the §32 milestone. **A booking need not exist and may never exist** | **AFTER (a), BEFORE the Booking** — `PO-Q2·c`'s ordering, corroborated by `PO-T1·8`'s position between steps 7 and 9. **Until when is `AC-29`, OPEN** | **At and after the booking.** A booking is a precondition of eligibility existing at all |
| **Can it exist with no Booking** | **YES, routinely.** Most adjudicated Inquiries never convert | **YES** — it acts on (a), which needs no booking | **NO. Structurally impossible** |
| **Its answer-bearer** | **The resolved attribution claim on that Inquiry** — `K-18` | **A separate appended permanent decision record** — `PO-Q1·c`; it mutates nothing — `PO-Q1·d` | **A JOIN, never a single anchor** — `K-48` as extended by `NF-28` to three conjuncts |
| **`PO-P10` level** | **5 — Attribution: *"the resolved claim"*** | **5, SECOND LAYER** — AD-01Q §3.0's two-override-layer finding. **`PO-P10`'s enumeration does not name it: `X-24`/`A-38`(a), OPEN** | **6 — Commission Eligibility**, then **7 — Commission Override** for the `NF-24` exception |
| **Mandatory reason + evidence** | ⟦SETTLED⟧ **Stated reasons ARE required** — AD-01M §11.2's Policy-E obligations (*"the human decision, its date, its author, and its stated reasons — for every case"*), plus `C-V`. **Whether structured EVIDENCE is required as it is for (b) is NOT stated by any source and is NOT invented here** | ⟦PRODUCT-OWNER DECISION⟧ **BOTH, mandatory** — `PO-Q1·b`. **The asymmetry between (a) and (b) is `AC-28`, OPEN** | ⟦PRODUCT-OWNER DECISION⟧ **Mandatory reason for the `NF-24` exception** — `PO-N9` |
| **Status of its mechanism** | ⟦PRODUCT-OWNER DECISION⟧ **RATIFIED as to order, actor and authority (this document). `AC-15`/`AC-20` — the RULE it applies — UNANSWERED** | ⟦ARCHITECT RECOMMENDATION⟧ **`Q-OV` UNRATIFIED.** Gate 3 | ⟦ARCHITECT RECOMMENDATION⟧ **`NF-28`, `NF-24`, `NF-25`, `NF-26` all UNRATIFIED.** AD-01N at `VALIDATE` |

> ⟦ARCHITECT ANALYSIS⟧ **One honest correction to the owner's third bullet, offered as a clarification of
> the architecture and NOT as a disagreement with the decision.**
>
> **`PO-T3`'s third bullet speaks of *"the existing commission authority model"* in the singular. The
> architecture does not have one authority there; it has at least FOUR, already separated, and the
> separation is `NF-26`(5)'s and consolidated §22's, not this document's:**
>
> | # | Authority | Source |
> |---|---|---|
> | **1** | **The eligibility authorization** — reads the join of (determination, any exception on it) and never a mutated input | `NF-25`; ⟦SOURCE⟧ **§40**, **§50** |
> | **2** | **The `NF-24` commission-eligibility-exception permission** — the `PO-N8`/`PO-N9` Site Head exception, `PO-P10` level 7 | `NF-24`, `NF-26`(1) |
> | **3** | **Accounts' §32 review** | ⟦SOURCE⟧ **§32**; `NF-26`(5) |
> | **4** | **Payout authorization** | ⟦SOURCE⟧ consolidated **§22**'s accrual/payout separation; `NF-26`(5) |
>
> **Nothing in this document touches, merges, renames or reassigns any of the four.** ⟦ARCHITECT ANALYSIS⟧
> **The bullet's INSTRUCTION — *"do not move this into the attribution decision"* — is honoured exactly, and
> is in fact stronger than it needs to be: `NF-26`(5) already requires the exception approval to be distinct
> from §32 review and from payout authorization, and AD-01Q §3.4(2) already requires the attribution
> override permission to be distinct from the eligibility-exception permission. `C-XX` adds the third leg —
> the step-7 permission is distinct from both.** **The exact separation matrix remains `M-9`'s and `M-3`'s
> and is NOT designed here.**

> ⟦ARCHITECT ANALYSIS⟧ **`PF-88` — the DECISION-versus-PERSON distinction, stated because it is the single
> most likely misreading of `PO-T3` and because AD-01Q made exactly this distinction for exactly this
> reason.**
>
> > **These are three DECISIONS, not three PEOPLE. The same individual may hold two of these permissions, or
> > all three, WITHOUT the decisions collapsing into one.**
>
> **What follows from that, and what does not:**
>
> | | Statement |
> |---|---|
> | **It is CONFIGURABLE, not fixed** | ⟦SOURCE⟧ **R2** — permissions are the fixed vocabulary, roles are the flexible composition tenants control. **Whether a tenant grants one role all three is a tenant configuration question, not an architecture question** |
> | **The DECISIONS stay distinct regardless** | ⟦ARCHITECT ANALYSIS⟧ **Each has a different subject, a different question, a different answer-bearer, a different timing and a different `PO-P10` level** — the table above. **Two decisions made by one person on one afternoon are still two decisions, two records and two audit links (`Q-AR` links 8 and 9)** |
> | **A DEFAULT SHIPPING CONFIGURATION IS NOT DESIGNED HERE** | ⟦ARCHITECT ANALYSIS⟧ **`PO-T2` names the default holder of (a) and `PO-Q1·a` names the default holder of (b), and those names overlap. Whether the SAME default role should ship holding both is a §88 authorization answer and is NOT decided here** → **`AC-33`**, [§5.1](#51-the-one-new-question-ac-33) |
> | **What is FORBIDDEN, and it is already forbidden** | ⟦SETTLED⟧ **A SINGLE PERMISSION spanning two of these decisions.** AD-01Q §3.4(2), verbatim reasoning: *"A single permission spanning both would authorize at `PO-P10` levels 5 AND 6 with one grant, which is exactly the conflation `PO-P10` exists to forbid."* **`C-XX` extends that reasoning to the (a)/(b) pair, which sit at the SAME `PO-P10` level and are therefore the pair most at risk of being merged** |
> | **Separation of duties is NOT designed here** | **The matrix is `M-9`'s and `M-3`'s.** `AC-27` (may one individual perform both OVERRIDES on one outcome) and `AC-13` (self-interest controls) are OPEN, and `AC-33` joins them |

### 3.2 `C-XX`, the authority clause, minted and stated in full

> ⟦ARCHITECT RECOMMENDATION — `C-XX`, a constraint⟧ *(RECOMMENDED, SUBJECT TO THE PROJECT OWNER'S EXPLICIT
> WRITTEN APPROVAL. **NOT APPROVED. NOT APPROVED FOR IMPLEMENTATION.** ⟦SOURCE⟧ **§88** MUST-ASK:
> **authorization rules**, CP commission logic, audit requirements. **Stated as a BUSINESS RULE. No schema,
> screen, workflow, role key, permission key, enum or role hierarchy is proposed, implied or derivable from
> it.** ⟦ARCHITECT ANALYSIS⟧ **Its CONTENT is `PO-T2`, which is the owner's. What `C-XX` adds is the FORM in
> which `PO-T2` can be implemented without breaching `R2` — and that form is the architect's recommendation,
> not the owner's decision.**)*

> **`C-XX` — The ordinary attribution/clash decision at `P-G` step 5 (the owner's step 7) is exercised under
> a PERMISSION in the fixed permission vocabulary, NEVER by a test of a role's name. ⟦PRODUCT-OWNER
> DECISION⟧ The Project Owner has named the DEFAULT holders of that permission as the roles shipped as
> *Site Head* and *Project Head*. ⟦SOURCE⟧ §11's phrase *"builder-side authorized leadership"* is thereby
> NARROWED, BY THE OWNER, to those default holders for this step — and that narrowing is a statement about
> WHO HOLDS AN AUTHORITY, never a licence to branch on a role name anywhere in logic.**

**Six corollaries, each stated because each is a separate implementation temptation:**

1. **It is a PERMISSION, and any tenant-defined role may hold it.** **The default roles shipped as *Site
   Head* and *Project Head* hold it by default; a tenant renaming, splitting or replacing either role must
   not silently drop the control and must not silently grant it.** ⟦SOURCE⟧ **R2**; `NF-26`(1); AD-01Q
   §3.4(1). ⟦ARCHITECT ANALYSIS⟧ **This is the THIRD instance of the same template and it is applied
   without amendment in principle, exactly as AD-01Q applied it as the second.**
2. **It is a THIRD, DISTINCT permission** — **NOT the `Q-OV` attribution-override permission, NOT the
   `NF-24` commission-eligibility-exception permission, and NOT any general attribution, adjudication,
   clash-visibility or lead-management permission.** ⟦ARCHITECT ANALYSIS⟧ **(a) and (b) sit at the SAME
   `PO-P10` level 5, which makes them the pair a single permission would most plausibly be thought to
   cover — and the pair whose merger would make `PO-T3`'s first two bullets indistinguishable in
   enforcement.** **Holding either must be an explicit, separately reviewable grant.** ⟦SOURCE⟧ **R2**,
   **§88**; `PO-P10`; `NF-26`(2); AD-01Q §3.4(2).
3. **It is SCOPE-BOUND to the Project — evaluated against the INQUIRY's Project.** ⟦ARCHITECT ANALYSIS⟧
   **AD-01Q §3.4(3)'s reasoning applies unchanged and is reused rather than re-derived: an attribution
   decision's subject is an Inquiry, `C-VII` gives every claim the Inquiry's project, and an Inquiry always
   has one whereas whether a registration must is `AA-2` — so this scope rule is `AA-2`-INDEPENDENT.** **A
   holder scoped to Project A must not decide a Project B attribution.** ⟦SOURCE⟧ **§08**, **R12**;
   `NF-26`(3).
4. **Server-side enforcement only.** ⟦SOURCE⟧ **§40**: *"Never rely on a greyed-out button as the actual
   security control."* ⟦SOURCE⟧ **§50**: *"server-side authorization must verify that the approver actually
   has authority."* ⟦SOURCE⟧ **§11** itself: *"The UI visibility rule must be enforced by
   authorization—not merely by hiding a badge."* ⟦ARCHITECT ANALYSIS⟧ **§11's own sentence is the closest
   the spec comes to saying anything about HOW this authority is enforced, and it says exactly this.**
5. **It supplies NO precedence rule, NO ranking, NO tiebreak, NO presumption and NO default outcome.**
   ⟦ARCHITECT ANALYSIS⟧ **Naming WHO decides says nothing whatever about HOW competing claims are ranked.**
   **`C-IX` is untouched, `C-XVI` is untouched, `C-XVIII` is untouched, and `AC-15`/`AC-20` are exactly as
   open as they were** — [§3.3](#33-the-thirteen-checkpoints-verified-one-at-a-time) checkpoint 5.
6. **It creates NO new record, NO new event, NO new field and NO new link.** ⟦ARCHITECT ANALYSIS⟧ **The
   attribution/clash decision record already exists as a business-record discipline (`Q-AR` link 8, AD-01
   §D.4's appended-never-overwritten), and AD-01M §11.2 already requires it to retain *"the human decision,
   its date, its author, and its stated reasons — for every case."* `C-XX` names the authority under which
   that author must have acted; it does not ask for anything to be recorded that was not already
   required** — [§3.3](#33-the-thirteen-checkpoints-verified-one-at-a-time) checkpoint 13.

> ⟦ARCHITECT ANALYSIS⟧ **Why a constraint rather than a finding, answered in one line because AD-01S
> declined `C-XX` for a symmetrical reason and the two decisions must be consistent.** **AD-01S declined
> because a RATIFICATION introduces no mechanism. This document ratifies AND narrows, and the narrowing is
> of an ⟦SOURCE⟧-level term. A narrowing of a spec term that is not carried as a numbered clause is a
> narrowing that survives only as long as someone remembers this conversation — which is precisely the
> condition `C-XI`, `C-XVI` and `C-XVIII` were each minted to escape.**

### 3.3 The thirteen checkpoints, verified ONE AT A TIME

> ⟦ARCHITECT ANALYSIS⟧ **`PF-89` — the confirmed sequence is walked against thirteen named checkpoints. Each
> is checked individually and the honest answer recorded for each, including where the answer is *nothing
> changes*. Two — checkpoints 5 and 13 — are worked at length because the intuitive answer to each is the
> wrong one.**

| # | Checkpoint | Result |
|---|---|---|
| **1** | **Inquiry creation — `J-2`** | **UNAFFECTED. NOT AMENDED, NOT NARROWED, NOT GIVEN A NEW INPUT.** ⟦ARCHITECT RECOMMENDATION⟧ **`J-2` as bounded by `K-50` decides alone whether a later expression of interest opens a new Inquiry or is an activity on a live one — the FIFTH consecutive document to report this.** ⟦ARCHITECT ANALYSIS⟧ **`PO-T1·1`'s *"becomes its own Inquiry record"* is `PO-R1·f`'s *"the later Inquiry remains a separate commercial Inquiry"* restated, which `K-18` and `C-VII` already carry. `AA-1` (are two live Inquiries on one client × Project ordinary) is NOT resolved** |
| **2** | **Live-registration determination — `P-G` steps 2/3, `O-U`** | **UNAFFECTED IN SUBSTANCE, CONFIRMED IN ACTOR AND OPERANDS.** ⟦PRODUCT-OWNER DECISION⟧ **`PO-T1·2` confirms the actor is *"the system"* and the operand set is CP × Client × Project, live — AD-01R's `PF-57` owner-confirmed set, with NO operand added and none removed.** **`O-U`'s at-most-one-live-registration-per-triple rule is what makes *"a currently-live CP Project Registration"* singular rather than a set, and it is UNCHANGED and STILL UNRATIFIED (AD-01O is advisory).** ⟦ARCHITECT ANALYSIS⟧ **WHICH DATE the check runs at is `AC-24` and the owner's sentence does not name it — the compression of `P-G` steps 2 and 3 into one owner step neither answers `AC-24` nor removes the need for it** |
| **3** | **Registration citation — `PF-23`** | **UNAFFECTED, AND CORROBORATED FOR A FOURTH TIME.** ⟦PRODUCT-OWNER DECISION⟧ **`PO-T1·3`'s *"explicitly citing that specific Registration record"* is `PO-P11`/`PO-P12` and `PO-Q2·c`'s *"registration used as attribution basis"* restated — the owner's FOURTH statement of the record-level requirement.** ⟦ARCHITECT RECOMMENDATION⟧ **`PF-23`'s CONCLUSION — that the citation is CLAIM-BORNE, the claim carrying the reference and the Inquiry's reference being DERIVED from its claim set — is NOT ratified by this. The owner states the REQUIREMENT, not its carrier.** **`PF-23` remains gate 2's business, UNRATIFIED, and the alternative AD-01P offered (a literal Inquiry-level reference) remains available and remains the owner's to choose knowingly** |
| **4** | **Attribution claim creation — the minting gate, `PF-5`'s four configurations** | **UNAFFECTED. `AC-25`'s FIRST HALF NOT ANSWERED** — see checkpoint 4's note below. **`PF-5`'s finding — that the registration sequence is consulted TWICE, at `G` and at `B`, for two different purposes — is UNTOUCHED, and all four configurations remain reachable and remain ordinary.** ⟦ARCHITECT ANALYSIS⟧ **Configuration 2 (`G` in registration #1, `B` in registration #2) is still the genuine gap `X-22`/`A-29`/`AC-22` names, and the owner's sequence does not reach it: `PO-T1` describes ONE registration grounding ONE claim and never asks which registration conjunct (iii) later reads. `C-XV`'s two-distinct-evaluations rule is UNTOUCHED and STILL UNRATIFIED** |
| **5** | **CP-vs-CP clash handling — `AC-15`/`AC-20`** | **WHO DECIDES IS NOW NAMED. WHAT THEY APPLY IS NOT.** **`AC-15`/`AC-20` UNCHANGED, NOT ANSWERED, NOT NARROWED IN WHAT THEY ASK — PRIORITY RAISED A SEVENTH CONSECUTIVE TIME** — see `PF-90` below |
| **6** | **Site Head / Project Head authority — this document's own new content** | ⟦PRODUCT-OWNER DECISION⟧ **NEW. `PO-T2`, investigated at [§2.2](#22-the-authority-clarification-investigated) and found to be new information rather than a confirmation. Carried as `C-XX`** — [§3.2](#32-c-xx-the-authority-clause-minted-and-stated-in-full) |
| **7** | **Site Head override — `Q-OV`** | **UNCHANGED, STILL A SEPARATE ACT FROM STEP 7, AND STILL UNRATIFIED.** ⟦PRODUCT-OWNER DECISION⟧ **`PO-T1·8` places it at step 8 — AFTER the step-7 decision — and `PO-T3`'s second bullet routes it by name to AD-01Q's mechanism, which is the owner confirming for a THIRD time (after `PO-Q1·a`'s *"override the normal … decision"* and `PO-Q2·c`'s ordering) that the override ACTS ON the ordinary decision and is not the ordinary decision.** ⟦ARCHITECT ANALYSIS⟧ **`Q-OV`, `C-XVII`, `C-XVIII`, `C-XIX`, AD-01Q §3.3's minimum field set and §3.4's permission are ALL STILL UNRATIFIED and NONE is ratified here. `AC-27 … AC-31` are ALL UNTOUCHED.** **And `C-XVIII` is restated rather than relied on: an override is NOT an anti-abuse mechanism, is NOT a precedence rule, and repeated use to enforce a de-facto policy is `C-IX`'s §88 authorization change arriving by the back door** |
| **8** | **Resolved Inquiry attribution — `K-18`** | **CONFIRMED FOR A SEVENTH CONSECUTIVE DOCUMENT.** ⟦ARCHITECT RECOMMENDATION⟧ **The attribution claim attaches to the Inquiry and to nothing else. `PO-T1·4`, `·6`, `·7` and `·9` all speak of attribution ON an Inquiry and of that Inquiry converting; NO anchor moves, and nothing attaches attribution to a registration, a client, a Project or a booking** |
| **9** | **Booking / conversion — `C-IV`'s timing bound** | **UNAFFECTED AND CORROBORATED.** ⟦ARCHITECT RECOMMENDATION⟧ **`C-IV` requires claim resolution to complete BEFORE the §32 milestone, and `PO-T1·9`'s *"only after attribution is resolved, and only if the Inquiry converts"* is that bound in the owner's own words.** ⟦ARCHITECT ANALYSIS⟧ **One consequence worth naming and NOT resolving: `PO-T1·8` places the override between the decision and the Booking, so `C-IV`'s bound must accommodate the override too — which is exactly `AC-29` (until when may an override be performed), and `AC-29` is NOT answered here.** **`AC-5` (which booking fact is the Booking Date) is UNTOUCHED** |
| **10** | **Commission eligibility — `NF-28`** | **UNTOUCHED, DOWNSTREAM, AND IN THE OWNER'S OWN WORDS.** ⟦ARCHITECT RECOMMENDATION⟧ **`NF-28`'s three conjuncts — (i) WHO, (ii) WHETHER AND WHEN, (iii) STILL WITHIN PROTECTION — are re-run unamended for a FIFTH round. `PO-T1·9` reaches conjunct (i)'s input (the resolved attribution) and conjunct (ii)'s trigger (conversion) only by ORDERING them, and reaches conjunct (iii) not at all.** **`K-48`'s JOIN is CONFIRMED and UNAMENDED. `AC-22` (which registration governs conjunct (iii)) is UNTOUCHED and still OPEN** |
| **11** | **Commission approval — AD-01N's existing commission authority model** | **UNTOUCHED, AND EXPRESSLY KEPT SEPARATE BY THE OWNER.** ⟦PRODUCT-OWNER DECISION⟧ **`PO-T3`'s third bullet: *"remains governed by the existing commission authority model; do not move this into the attribution decision."*** ⟦ARCHITECT ANALYSIS⟧ **This document touches none of the four separated authorities [§3.1](#31-the-three-decision-points-kept-distinct) enumerates, adds none, merges none and renames none. `NF-25`'s *"it does not create an attribution claim, does not resolve a §11 clash, does not satisfy the §32 milestone, and does not substitute for either half of `K-48`"* stands verbatim. `PO-N8`/`PO-N9` are UNCHANGED, NOT REPEALED, NOT ABSORBED and NOT ENGAGED** |
| **12** | **Payout / reconciliation** | **ORTHOGONAL AND UNTOUCHED.** ⟦PRODUCT-OWNER DECISION⟧ **`PO-T1·10` places approval/payout downstream of eligibility and says nothing else about it.** ⟦SETTLED⟧ **`M-9` owns every formula, rate, slab, percentage, amount, milestone value, clawback quantum, passback rule and TDS treatment, and NOTHING in this document touches any of them. ⟦SOURCE⟧ §32, §33, §34's clawback and TDS treatments are unreached. `M-9` still cannot start and its consumer list is unchanged from AD-01S's** |
| **13** | **Audit reconstruction — `Q-AR`** | **NO NEW LINK IS REQUIRED AND NO EXISTING LINK ACQUIRES A NEW REQUIREMENT — `Q-AR` IS UNAMENDED FOR A THIRD CONSECUTIVE DOCUMENT. But ONE property of link 8 becomes load-bearing that was not, and it is NOT a new requirement** — see `PF-91` below |

> ⟦ARCHITECT ANALYSIS⟧ **Checkpoint 4's note, stated separately because `AC-25` is the item most likely to be
> read as having moved and it has not.**
>
> **`AC-25` has two halves. BOTH are checked.**
>
> | Half | Effect of `PO-T1`/`PO-T2` |
> |---|---|
> | **First half — is the mint at step 3 AUTOMATIC or a DISCRETIONARY authorized act?** | **UNTOUCHED, NOT ANSWERED, NO DEFAULT IMPLIED.** ⟦ARCHITECT ANALYSIS⟧ **`PO-T1·3` names NO actor and uses the permissive *"may be recorded"*, which is `PO-P1`'s *"MAY"* — the exact word AD-01P said *"is NOT read as either answer."* The owner has now written a ten-step sequence that names an actor at steps 1, 2, 7 and 8 and names NONE at step 3. ⟦ARCHITECT ANALYSIS⟧ **That silence is recorded as silence. It is not read as *"the system,"* by analogy to step 2, and it is not read as *"the authorised decision-maker,"* by analogy to step 7** |
> | **Second half — does an UNOPPOSED claim require the same adjudication act as a contested one?** | **NOT ANSWERED — and ONE FURTHER CANDIDATE ANSWER IS NOW EXCLUDED, which is a narrowing of the ANSWER SPACE and NOT of the question.** ⟦ARCHITECT ANALYSIS⟧ **`PO-T1·4` routes *"the attribution claim"* — every one, without distinguishing opposed from unopposed — through *"the normal attribution/clash-resolution process"*, and `PO-S1·9` already ruled that *"the absence of competition does not convert registration into automatic attribution; the normal attribution determination still applies."* With `PO-T2` naming the authority for that normal process, an answer that routes the unopposed case to a DIFFERENT authority is EXCLUDED. AD-01Q had already excluded an answer producing NO recorded decision at all. What survives — the same act, or a lighter act under the same authority producing a record — is NOT chosen here and NO default is implied** |

> ⟦ARCHITECT ANALYSIS⟧ **`PF-90` — checkpoint 5 worked in full, because *"the decision-maker is named"* and
> *"the decision is decided"* are the two propositions this document must hold apart, and a reader in a
> hurry will merge them.**
>
> > **`PO-T2` answers WHO. `AC-15` asks WHAT THEY APPLY. These are different questions and an answer to the
> > first is not an answer to the second, in whole or in part.**
>
> **The configurations, restated from AD-01S §3.3 unchanged and re-checked against `PO-T2`:**
>
> | Configuration | Does `PO-T2` bear on it? |
> |---|---|
> | A registration-backed claim against a producer-FILED claim | **ONLY AS TO WHO DECIDES.** ⟦PRODUCT-OWNER DECISION⟧ **`C-XVI` still forbids any thumb on the scale and `PO-T1·5` restates it. No ranking is supplied** |
> | A registration-backed claim against another registration-backed claim | **ONLY AS TO WHO DECIDES.** ⟦ARCHITECT ANALYSIS⟧ **As between two such claims the registration property is still not a discriminator, and there is still no stated rule for what IS one** |
> | **WHO WINS, in either configuration** | **NOT REACHED. NOT ANSWERED. NOT INVENTED. That is `AC-15`, and it is OPEN** |
> | **May the SECOND registration exist at all** | **NOT REACHED. That is `AC-20`, and it is OPEN** |
>
> ⟦ARCHITECT ANALYSIS⟧ **And the consequence, stated because it is the reason this item is now the head of
> the register rather than merely on it.** **AD-01S found that removing the default tilt made the absence
> of a rule *"load-bearing rather than merely unnoticed."*** **`PO-T2` sharpens that again and in a new way:
> the architecture now has a NAMED, PERMISSIONED, PROJECT-SCOPED, SERVER-ENFORCED, AUDITED authority whose
> stated job is to resolve CP-vs-CP clashes, and NO STATED RULE FOR THEM TO APPLY.** **That is not a defect
> in `PO-T2` — ⟦SOURCE⟧ §11 reserves the judgement to leadership and `C-IX` holds that a binding systemic
> precedence rule is a §88 authorization change, so a rule is exactly the thing the architect may not
> supply.** **It is the reason [§6.2](#62-the-one-next-question) asks what it asks.** **NO ANSWER TO
> `AC-15` IS PROPOSED, RANKED, PREFERRED, HINTED AT OR IMPLIED HERE, AND NO PRECEDENCE RULE IS INVENTED.**

> ⟦ARCHITECT ANALYSIS⟧ **`PF-91` — checkpoint 13 worked in full, because the question is a genuine one and
> the intuitive answer is wrong in an instructive way. The question: now that step 7 has a NAMED authority
> and step 8 has a DIFFERENT named authority, must `Q-AR`'s reconstruction be able to show WHICH of the two
> made which call — i.e. does the chain need a new requirement to distinguish them?**
>
> **The question is decomposed into three.**
>
> **(a) Are links 8 and 9 already SEPARATE links? YES, and they always were.** ⟦ARCHITECT RECOMMENDATION⟧
> **`Q-AR` link 8 is *"attribution/clash decision"* and link 9 is *"any Site Head override."* They were
> separated by `PO-Q2·c`'s own chain — the owner's — before this document existed. `PO-T1·7` and `PO-T1·8`
> are the same separation stated a second time. NO FOURTEENTH LINK IS ADMITTED and none is needed.**
>
> **(b) Is the AUTHOR of link 8 already a required retained fact? YES — and NOT because of this decision.**
> ⟦SETTLED⟧ **AD-01P §3.16 established it and it is quoted rather than re-derived:** *"Under BOTH override
> layers AD-01M §11.2's audit obligations for E are live: 'the human decision, its date, **its author**, and
> its stated reasons — for every case.' … ⟦SOURCE⟧ §11 plus `C-V` require it for the adjudication layer."*
> **And `NF-24`'s Approver row states the FORM for the eligibility layer — *"identity plus the role and
> scope under which they acted, denormalised as at that moment"*, on ⟦SOURCE⟧ **R6**'s *"an event records
> the actor label and the facts as they were at the time."*** ⟦ARCHITECT ANALYSIS⟧ **So the author of the
> step-7 decision was ALREADY required, by a chain that predates this decision entirely, and this document
> neither adds that requirement nor may take credit for it.**
>
> **(c) Does anything become NEWLY LOAD-BEARING? YES, ONE THING — and it is a change in what hangs on an
> existing requirement, not a new requirement.** ⟦ARCHITECT ANALYSIS⟧ **Before `PO-T2`, link 8's recorded
> author was a fact about accountability. After `PO-T2`, it is also the ONLY evidence that the decision was
> taken under the right authority — because the property *"the decision-maker held the step-7 permission,
> scoped to this Inquiry's Project"* is checkable only against a retained author-plus-role-and-scope record.
> `C-XX`(1)'s tenant-renaming hazard is exactly why `NF-24`'s denormalised-as-at-that-moment form matters
> here and not merely for the exception.**
>
> > **Therefore, stated as the determination:** **`Q-AR` NEEDS NOTHING ADDED. Its thirteen links are
> > UNCHANGED, no fourteenth is admitted, and no new field, event, report, metric or monitoring rule is
> > proposed, designed or implied.** **What changes is that link 8's EXISTING author requirement now
> > carries a second job, and that link 8's KNOWN GAP gets worse.**
>
> ⟦ARCHITECT ANALYSIS⟧ **The gap, named rather than smoothed over, because it is the honest cost of this
> ratification.** **`Q-AR` link 8 is, in AD-01Q §3.10's own words, *"ESTABLISHED AS A BUSINESS-RECORD
> DISCIPLINE. NOT BACKED BY AN `NF-27` AUDIT EVENT"* — and `PO-Q2·b` names both substrates conjunctively.
> That is `X-25` and `A-37`, and they are NOT discharged, NOT narrowed and NOT affected by anything here.**
> **But link 8 is now the link at which an OWNER-NAMED AUTHORITY acts, and it remains the one link in the
> chain backed on only one of the two substrates the owner required. The priority of `X-25`/`A-37` rises
> accordingly and is recorded as rising.** ⟦PRODUCT-OWNER DECISION⟧ **`PO-Q2·a`'s requirement remains
> OWNER-RATIFIED AND NOT YET SATISFIABLE exactly as AD-01Q §3.10 found it. `AC-31` (who the auditor is) is
> untouched.**

### 3.4 The owner's three boundary statements — each tied to what already enforces it

> ⟦ARCHITECT ANALYSIS⟧ **`PF-92` — the owner's three boundaries are preserved as a labelled principle, and
> each is tied to the clause that already enforces it. The check run here is whether any of the three is
> NEW. The expected answer was *none*, and the expectation was tested rather than assumed.**

> ⟦PRODUCT-OWNER DECISION — THE THREE BOUNDARIES, PRESERVED⟧
>
> **1. Registration is a PERMITTED GROUNDING BASIS. Registration is NOT attribution.**
> **2. A claim EXISTING is not a claim WINNING.**
> **3. A claim WINNING is not commission being payable.**

| Boundary | Where the owner states it here | Already established by | NEW? |
|---|---|---|---|
| **1 — grounding basis, not attribution** | **`PO-T1·3`** (*"may be recorded … citing that specific Registration record"*) joined to **`PO-T1·4`** (*"is then evaluated"*) and **`PO-T3`** bullet 4 (*"Registration itself never decides the winner"*) | ⟦PRODUCT-OWNER DECISION⟧ **`PO-P1`** (the grant, permissive) · **`PO-P2`** (*"registration is NOT automatic attribution"*) · **`PO-O13`** · **`PO-R1·f`** · **`PO-S1·4`**/**`PO-S1·6`** · ⟦ARCHITECT RECOMMENDATION⟧ **`C-XI`** (the registration is a TEMPORAL PROTECTION fact and must never become an ATTRIBUTION fact — at owner status since `A-20`) · **`P-G`** steps 3→4→5, which are three steps precisely so that grounding and attribution cannot be one event · ⟦ARCHITECT ANALYSIS⟧ **`PF-4`'s ground-versus-anchor discriminator: the only discriminator is falsifiability** | **NO. Stated by the owner for a FIFTH time** |
| **2 — claim exists ≠ claim wins** | **`PO-T1·4`**, **`PO-T1·5`**, **`PO-T1·6`** (*"The claim can lose"*) | ⟦PRODUCT-OWNER DECISION⟧ **`C-XVI`, RATIFIED at AD-01S** — and specifically **corollary 1** (*"An UNOPPOSED minted claim is not thereby a RESOLVED claim"*) and **corollary 2** (*"A minted claim may LOSE, and losing must be a reachable, recorded, permanent outcome"*) · **`PO-S1·3`**, **`·7`**, **`·9`** · **`PO-P4`** · **`PO-R1·e`** · ⟦SOURCE⟧ **§11**'s reservation of resolution to authorized leadership · **`K-48`** conjunct (i)'s requirement of *"the RESOLVED attribution claim"* | **NO. This is the whole of AD-01S, restated by the owner in two sentences** |
| **3 — claim wins ≠ commission payable** | **`PO-T1·9`**, **`PO-T1·10`**, and **`PO-T3`** bullet 3 | ⟦PRODUCT-OWNER DECISION⟧ **`PO-P3`** (*"registration is NOT automatic commission eligibility"*) · **`PO-O14`** · **`PO-R1·f`** · **`PO-N5 … PO-N7`** · ⟦ARCHITECT RECOMMENDATION⟧ **`NF-28`**'s three-conjunct join and **`K-48`** (*"a JOIN, never a single anchor"*) · ⟦SETTLED⟧ **`K-9`** (*"winning attribution and being paid are different outcomes"*) and **`K-10`** (*"an internal claimant can WIN an attribution contest and generate NO §32 entitlement at all"*) · ⟦ARCHITECT ANALYSIS⟧ **`PF-5` configuration 3 is the sharpest available demonstration: a claim validly minted at `G`, WON at §11, and conjunct (iii) FAILS at `B`** | **NO. Stated by the owner for a FIFTH time** |

> ⟦ARCHITECT ANALYSIS⟧ **The check for anything the owner's phrasing surfaces that is NOT already covered,
> run rather than skipped. RESULT: NOTHING.** **All three boundaries are covered by name, by clause, and by
> multiple independent sources, and every one of them has been stated by the owner at least four times
> before this document.** **This is reported as the null result it is. A document that manufactured a novelty
> out of a restated boundary would be the mirror of one that reported `PO-T2`'s narrowing as a
> confirmation, and both errors are the same error.**

### 3.5 The owner's locked list — each item reconfirmed against its exact source

> ⟦ARCHITECT ANALYSIS⟧ **`PF-93` — the owner named twelve already-locked decisions as unweakened by this
> message. Each is checked INDIVIDUALLY against its exact source, and the check is for two things: is it
> genuinely locked where the owner says it is, and does anything in this document touch it. ONE
> DISCREPANCY WAS FOUND and it is item 7. The other eleven are clean.**

| # | Item as the owner names it | Exact source | Status after this document |
|---|---|---|---|
| **1** | **`AC-14` = Option A** | ⟦PRODUCT-OWNER DECISION⟧ **`PO-R1·a … PO-R1·g`**, AD-01R §1.3: *"`AC-14` = OPTION A IS NOW A LOCKED PRODUCT OWNER BUSINESS RULE"* | **LOCKED, UNTOUCHED, UNWEAKENED.** Nothing here reads a closure, consumes a registration or reopens `(L-6·ground)` |
| **2** | **`C-XVI` = No Special Weight** | ⟦PRODUCT-OWNER DECISION⟧ **`PO-S1·1 … PO-S1·10`**, AD-01S §1.3 | **RATIFIED, UNTOUCHED, UNWEAKENED — and RESTATED by `PO-T1·5`/`PO-T1·6`**, checked term by term at [§2.1](#21-the-ten-steps-walked-one-at-a-time) |
| **3** | **Registration scoped Client × Project × CP** | ⟦ARCHITECT RECOMMENDATION⟧ **`O-U`**, AD-01O — the per-triple sequence; ⟦PRODUCT-OWNER DECISION⟧ **`PO-P1`**'s *"same client/project"*; AD-01R's **`PF-57`** owner-confirmed operand set | **UNTOUCHED and RESTATED by `PO-T1·2`.** ⟦ARCHITECT ANALYSIS⟧ **`O-U` itself remains an ⟦ARCHITECT RECOMMENDATION⟧ in an advisory document and is NOT ratified by the owner restating the triple. `AC-21` and `Q10`/`M-5`'s uniqueness half still gate the CLIENT term** |
| **4** | **Project-scoped** | **`C-VII`** (every claim carries the Inquiry's project); `NF-26`(3); `O-U` | **UNTOUCHED, and `C-XX`(3) applies the same scoping to the new permission.** ⟦ARCHITECT ANALYSIS⟧ **This does NOT close `AA-2`** — see the note below this table |
| **5** | **Immutable registration date** | ⟦PRODUCT-OWNER DECISION⟧ **`PO-N1`**, **`PO-N3`**, **`PO-N9`**; **`PO-R1·c`**; AD-01N §D.1 (*"Immutable thereafter — no act by any actor moves it"*) | **UNTOUCHED and UNWEAKENED.** Nothing here writes to a registration |
| **6** | **Snapshotted lock-in** | ⟦PRODUCT-OWNER DECISION⟧ **`PO-N2`**; AD-01N **`D5`** (snapshot at registration, forward-only); `NF-27` event 1 | **UNTOUCHED and UNWEAKENED.** `AC-7`'s second half remains OPEN |
| **7** | **"Day 30 is included; Day 31 is expired"** | AD-01N **§F.2** — and this is the DISCREPANCY | **⚠ THE ITEM THE OWNER LISTS AS LOCKED IS `AC-6`, AND `AC-6` IS OPEN** — [§3.6](#36-the-ac-6-investigation) |
| **8** | **A registration may ground a later Inquiry while valid** | ⟦PRODUCT-OWNER DECISION⟧ **`PO-P1`**; **`PO-R1·e`** | **UNTOUCHED and RESTATED by `PO-T1·2`/`PO-T1·3` — the owner's FOURTH statement** |
| **9** | **Non-automatic attribution / clash-win / eligibility** | ⟦PRODUCT-OWNER DECISION⟧ **`PO-P2`**, **`PO-P3`**, **`PO-P4`**; **`PO-O13`**/**`PO-O14`**; **`PO-R1·f`**; **`PO-S1`** | **UNTOUCHED and RESTATED by `PO-T1·5` and `PO-T3` bullet 4 — the owner's FIFTH statement** |
| **10** | **Site Head override with mandatory reason + evidence** | ⟦PRODUCT-OWNER DECISION⟧ **`PO-Q1·a`** (the grant), **`PO-Q1·b`** (both inputs mandatory), **`PO-Q1·c`** (separate appended permanent record), **`PO-Q1·d`** (mutates nothing) | **UNTOUCHED and UNWEAKENED, and POSITIONED at step 8 by `PO-T1·8`.** ⟦ARCHITECT ANALYSIS⟧ **The owner's *"where its conditions are satisfied"* refers to `PO-Q1·b`'s two mandatory inputs plus whatever `Q-OV` becomes. It adds no condition and removes none** |
| **11** | **Full audit reconstructability** | ⟦PRODUCT-OWNER DECISION⟧ **`PO-Q2·a`**, **`PO-Q2·b`** (both substrates, conjunctively), **`PO-Q2·c`** (the illustrative chain) | **UNTOUCHED and UNWEAKENED — and STILL OWNER-RATIFIED AND NOT YET SATISFIABLE.** ⟦ARCHITECT ANALYSIS⟧ **Five of thirteen links rest on recommendations at `VALIDATE`; link 8 is backed on one substrate only; link 9 did not exist before AD-01Q; link 1's identity term is `AC-21`. This document changes none of that and raises `X-25`/`A-37`'s priority** — `PF-91` |
| **12** | **Invalid-side closure non-consumption** | ⟦PRODUCT-OWNER DECISION⟧ **`PO-R1·b`**, **`PO-R1·d`** | **UNTOUCHED and UNWEAKENED.** Nothing here reads, classifies or consumes a closure. **`N-4` stays PERMANENTLY OFF `P-G`'s critical path and NO value is proposed** |

> ⟦ARCHITECT ANALYSIS⟧ **Item 4's note, because *"project-scoped"* sits next to an open question and the
> two must not be merged.** **`AA-2` asks whether a PROJECT-LESS PRELIMINARY REGISTRATION may exist — i.e.
> whether a registration record may be created before its Project is determined. The owner's item 4
> restates the SCOPING of a registration that has a Project, which `PO-P1` and `O-U` already fix. It does
> not speak to whether a record may exist without one, and it does not use `AA-2`'s vocabulary.** **`AA-2`
> is NOT resolved, NOT narrowed and NOT reached, and its urgency is unchanged.** ⟦ARCHITECT ANALYSIS⟧
> **This is the same SHAPE of near-miss as item 7 and is reported as a near-miss rather than a closure for
> the same reason — but it is WEAKER than item 7's, because item 7 reproduces a prior document's
> discriminating sentence word for word and this one does not.**

### 3.6 The `AC-6` investigation

> ⟦ARCHITECT ANALYSIS⟧ **`PF-94` — this was WORKED, at length, and neither answer was assumed. Prematurely
> closing `AC-6` would violate the discipline every document in this series has kept — *do not close an
> unrelated question merely because it is adjacent.* Failing to notice a genuine closure would be exactly as
> wrong. The test was run against `AC-6`'s EXACT PRIOR WORDING, not against its summary.**

**`AC-6` as AD-01N minted it, quoted verbatim:**

> ⟦BUSINESS DECISION REQUIRED⟧ ***"Boundary convention: is the last eligible day `registration date + N`
> (convention α), or `registration date + N − 1` (convention β)? And is `N = 0` a legal configuration?"***
> **AD-01N's own note on it: *"Your own example is consistent with both."*** **AD-01N §F.2 states that
> nothing in the architecture prefers either, and that the owner may select convention β *"with one word."***

**The candidate closure.** **The owner's item 7 reads: *"Day 30 is included; Day 31 is expired."*** **That
sentence is WORD-FOR-WORD the FIRST STATED PROPERTY of convention α in AD-01N §F.2:**

> ***"Properties of (α): Day 30 of a 30-day window is INSIDE. Day 31 is the first excluded day."***

**On its face, that looks like the owner selecting α. The test is whether the sentence actually
DISCRIMINATES.**

| | The test, run |
|---|---|
| **1. What α says, in full** | **Eligible iff `D ≤ N`, where `D` counts whole calendar days from the registration date and THE REGISTRATION DATE ITSELF IS `D = 0`.** **Under α with `N = 30`, the last eligible day is `registration date + 30`.** ⟦ARCHITECT ANALYSIS⟧ **AD-01N's phrase *"Day 30"* therefore means `registration date + 30`, because AD-01N is indexing from zero — it says so in the same paragraph** |
| **2. What β says** | **Eligible iff `D ≤ N − 1`. Under β with `N = 30`, the last eligible day is `registration date + 29`** |
| **3. The decisive question** | **In the owner's sentence, what does *"Day 30"* index FROM?** ⟦ARCHITECT ANALYSIS⟧ **The owner does not say, and the sentence is true under BOTH conventions depending on the answer:** |
| **— under ZERO-indexing** (registration date = Day 0) | ***"Day 30 included, Day 31 expired"* ⟹ last eligible = `reg + 30` ⟹ CONVENTION α** |
| **— under ONE-indexing** (registration date = Day 1, the ordinary business way of counting *"day one"*) | ***"Day 30 included, Day 31 expired"* ⟹ last eligible = `reg + 29` ⟹ CONVENTION β** |
| **4. Result** | **THE SENTENCE IS CONSISTENT WITH BOTH CONVENTIONS. It appears to select α only because AD-01N happened to write α's property using zero-indexing — which is AD-01N's indexing choice, not the owner's** |
| **5. `AC-6`'s SECOND LIMB** | **Wholly unaddressed. The owner says nothing about whether `N = 0` is a legal configuration, and `AC-6` asks that too** |
| **6. `N` itself** | **Not fixed anywhere. *"A 30-day window"* is AD-01N's ILLUSTRATIVE example and no lock-in number is proposed, selected or implied anywhere in this series. `M-9` and the Project configuration own it** |

> ⟦ARCHITECT ANALYSIS⟧ **THE CONCLUSION, STATED PLAINLY.**
>
> > **`AC-6` IS NOT RESOLVED BY THIS MESSAGE AND IS NOT CLOSED HERE.** **The owner's sentence reproduces
> > EXACTLY the ambiguity AD-01N flagged when it wrote *"Your own example is consistent with both"* — it
> > states a boundary in ordinal day-numbers without fixing the indexing origin, which is the one thing
> > `AC-6` needs.** **`AC-6` remains OPEN, remains TRIPLY money-bearing, and both its limbs remain
> > unanswered.**
>
> ⟦BUSINESS DECISION REQUIRED — A NOTICE, NOT A NEW QUESTION⟧ **But the owner has LISTED `AC-6` AMONG
> ALREADY-LOCKED DECISIONS, and it is not locked. That discrepancy is reported rather than quietly
> corrected, because the risk it carries is the one AD-01B §4 calls *"unrecoverable if decided by
> implementation":* an owner who believes the convention is settled will not be asked again, and an
> implementer who reads *"Day 30 included"* will pick whichever indexing their language defaults to.**
> **`AC-6` is TRIPLY money-bearing — one day of ELIGIBILITY (AD-01N `NF-18`), one day of RE-REGISTRATION
> ADMISSIBILITY (`O-U`, AD-01O), and one day of CLAIM ADMISSIBILITY (`P-G` step 3, AD-01P `PF-10`) — and
> AD-01P found that all three uses MUST agree or the sequence can be made to overlap by one day, defeating
> `OF-4`'s determinacy.**
>
> ⟦ARCHITECT ANALYSIS⟧ **This is recorded as a NOTICE and NOT as [§6.2](#62-the-one-next-question)'s
> question, and the reason is given there.** **NO CONVENTION IS SELECTED, PREFERRED, RANKED OR IMPLIED HERE.
> `AC-6` acquires NO fourth money-bearing consumer from this document — `PO-T1`/`PO-T2` evaluate no window,
> compare no dates and consult no `(R, N)` pair.**

### 3.7 No existing rule is contradicted

> ⟦ARCHITECT ANALYSIS⟧ **The search was RUN rather than asserted, and its result is that NO new contradiction
> is allocated and `X-26` remains NOT minted. The candidate that had to be ruled out was the obvious one and
> it is shown being ruled out rather than dropped.**

| Rule | Result |
|---|---|
| ⟦SOURCE⟧ **§11** — *"Builder-side authorized leadership resolves attribution"* | **NOT CONTRADICTED, and this was the CANDIDATE `X-26`.** ⟦ARCHITECT ANALYSIS⟧ **The test: does naming *Site Head / Project Head* contradict §11's generic phrase? NO — it NARROWS it from within. ⟦SOURCE⟧ §03 lists *"Project Head / Site Head"* among BUILDER-SIDE USERS, so the named holders are a subset of *"builder-side"*, and *"authorized"* is satisfied by the permission `C-XX` describes. A narrowing that selects a subset of a generic term's extension is not a contradiction of it.** **§11 requires NO amendment: no precedence, ranking or tiebreak is added to it, so `C-IX`'s §88 bar is NOT engaged and `Q0-d` stays OFF the critical path** |
| ⟦SOURCE⟧ **R2** — no hardcoded role names in logic | **NOT BREACHED.** ⟦ARCHITECT ANALYSIS⟧ **`C-XX` is a permission clause and names no key, no enum and no branch. The owner's role names are read as DEFAULT HOLDERS, the same reading `NF-26` gave `PO-N8`'s *"Site Head"* and AD-01Q §3.4 gave `PO-Q1·a`'s** — `PF-85` |
| **`C-IX`** — a binding systemic precedence rule is a §88 authorization change to §11 | **NOT BREACHED, and NOT ENGAGED.** **Naming an authority creates no rule, ranks no claim, prefers no CP, weights no ground and sets no default** |
| **`C-XI`** — the registration is a TEMPORAL PROTECTION fact and must never become an ATTRIBUTION fact | **NOT BREACHED, and CORROBORATED by `PO-T3` bullet 4** |
| **`C-XVI`** — no special weight | **NOT BREACHED, and RESTATED by `PO-T1·5`** |
| **`C-XVII`**, **`C-XVIII`**, **`C-XIX`** | **NOT BREACHED and NOT ENGAGED. All three remain UNRATIFIED and none is ratified here.** **`C-XVIII`'s prohibition on `Q-OV` becoming a rule is pointed the same way by `C-XX`(5)** |
| **`C-XV`** — the minting test and the eligibility test are distinct | **NOT BREACHED and NOT ENGAGED. `PO-T1` caches nothing and evaluates nothing twice. STILL UNRATIFIED** |
| **`C-IV`** — resolution before the §32 milestone | **NOT BREACHED, and CORROBORATED by `PO-T1·9`** |
| **`K-18`** — the claim-anchor is the Inquiry and nothing else | **CONFIRMED for a SEVENTH consecutive document** |
| **`K-48`/`NF-28`** — eligibility is a JOIN, never a single anchor | **CONFIRMED and UNAMENDED** |
| **`K-9`/`K-10`** — winning attribution and being paid are different outcomes | **CONFIRMED and restated by `PO-T3` bullet 3** |
| **`PO-P10`** — the seven levels | **NOT BREACHED as a prohibition, and NOT EXTENDED.** ⟦ARCHITECT ANALYSIS⟧ **`PO-T1` is a PIPELINE, not a levels list — the same discipline AD-01S applied to `PO-S2`'s chain and AD-01R to `PO-R1·g`. `X-24`/`A-38`(a) stand exactly as AD-01Q left them and only the owner may extend their own list** |
| **`B-1`, `B-2`, `L-6`, `K-22`, `H-4`, `J-2`, `J-8`, `NF-9`, `C-V`, `C-VII`, `C-X`, `C-XIII`, `C-XIV`, `O-U`, `OF-6`, `L-25`** | **NOT ENGAGED. Each was checked individually and each is untouched; none is reopened, narrowed, ratified or rejected** |

### 3.8 Which open items this decision actually reaches

> ⟦ARCHITECT ANALYSIS⟧ **Every item below is checked INDIVIDUALLY. *"Unchanged"* means CHECKED AND
> UNCHANGED, never skipped. NOTHING is closed on the strength of an answer that does not reach it, and no
> item is closed out of momentum because a neighbouring one closed.**

| Item | Effect of `PO-T1 … PO-T3` |
|---|---|
| **`P-G`** — the grounding rule, its order and its actors | ⟦PRODUCT-OWNER DECISION⟧ **RATIFIED IN FULL. The THIRD item in the series closed by a direct Product-Owner ruling, and the FIRST to be an entire multi-step rule.** Its step-5 actor is NARROWED by `PO-T2` → `A-51`, `A-52`, `A-53`; `C-XX` |
| **`A-38`(b)** — `P-G`'s proposed seventh step | **CONTENT-RATIFIED, REPOSITIONED, NOT RENUMBERED, STILL NOT MADE** — precedent `A-32` (AD-01R) and `A-39` (AD-01S) |
| **`AC-15`** and **`AC-20`** — who WINS between two competing claims; may the second registration exist | **UNCHANGED, NOT ANSWERED, NOT NARROWED IN WHAT THEY ASK — PRIORITY RAISED A SEVENTH CONSECUTIVE TIME, and now at its sharpest.** ⟦ARCHITECT ANALYSIS⟧ **`PO-T2` answers WHO. `AC-15` asks WHAT THEY APPLY** — `PF-90`. **NO PRECEDENCE IS INVENTED** |
| **`AC-25`** — automatic mint vs discretionary act; is an unopposed claim adjudicated like a contested one | **NOT ENGAGED IN ITS FIRST HALF; ONE FURTHER CANDIDATE EXCLUDED IN ITS SECOND. NEITHER HALF ANSWERED, NO DEFAULT IMPLIED** — [§3.3](#33-the-thirteen-checkpoints-verified-one-at-a-time) checkpoint 4's note |
| **`AC-6`** — boundary convention α/β; is `N = 0` legal | **NOT RESOLVED, NOT NARROWED, NO CONVENTION SELECTED — and a DISCREPANCY IS REPORTED: the owner lists it among locked decisions and it is open** — [§3.6](#36-the-ac-6-investigation). **It acquires NO fourth money-bearing consumer and stays TRIPLY money-bearing** |
| **`AC-32`** — should any SEPARATE anti-abuse control exist at all | **UNCHANGED, NOT ANSWERED, NOT CLOSED, NOT NARROWED IN WHAT IT ASKS — and CHECKED rather than assumed.** ⟦ARCHITECT ANALYSIS⟧ **`AC-32` asks whether abuse-prevention infrastructure should EXIST. Naming an authority is not a control; it is a statement about who exercises judgement.** **One thing DID change and it is stated precisely: AD-01R `PF-64` named *"the real control"* as `PO-P4` + `C-XVI` operating *"per case, on judgement,"* and the person exercising that judgement now has a name and a permission — which makes the control ACCOUNTABLE without making it SYSTEMIC. The LIMIT on that row is unchanged and restated: it still operates per case, still depends on leadership having grounds to doubt, and `C-XVI` at owner status still forbids a systemic bar.** **The cost `PO-R2` accepts is NOT retired, NOT reduced and NOT offset. `AC-32` remains OPEN and OFF the critical path** |
| **`AC-13`** — self-interest controls | **ENLARGED A SECOND TIME, NOT ANSWERED, NOT NARROWED.** ⟦ARCHITECT ANALYSIS⟧ **`NF-26`(6) raised it for the eligibility-exception approver and AD-01Q §3.4(6)/§3.13 enlarged it to the override holder. It now reaches a THIRD decision point: whether the step-7 decision-maker may sit in a benefiting reporting line, may be the CP's relationship manager, or may decide a clash involving a claim they themselves filed. NOT STATED BY ANY SOURCE and NOT invented here.** **`V-7`'s fail-closed projection remains the mechanism and only the content is open** |
| **`AC-27`** — may one individual perform both OVERRIDES on one commercial outcome | **UNCHANGED, NOT ANSWERED — and CHECKED against `AC-33` so the two are not merged.** ⟦ARCHITECT ANALYSIS⟧ **`AC-27` is about the ATTRIBUTION OVERRIDE and the ELIGIBILITY EXCEPTION — two overrides at `PO-P10` levels 5 and 7. `AC-33` is about the ORDINARY DECISION and the ATTRIBUTION OVERRIDE — a decision and its own override, both at level 5. Different pairs, different risk** — [§5.1](#51-the-one-new-question-ac-33) |
| **`AC-28`** (the evidence asymmetry) | **UNCHANGED, NOT ANSWERED — and SHARPENED, which is recorded and is not a narrowing.** ⟦ARCHITECT ANALYSIS⟧ **`PO-Q1·b` makes reason AND evidence mandatory for the override; AD-01M §11.2 requires stated reasons for the ordinary decision and no source requires structured evidence of it. With both authorities now named, the asymmetry is between two named, permissioned acts rather than between a named act and a generic one. `AC-28`'s QUESTION is unchanged and NO evidence standard is proposed for either** |
| **`AC-29`** (until when may an override be performed) · **`AC-30`** (what an override's decision may SAY) · **`AC-31`** (who the auditor is) | **ALL UNCHANGED, each CHECKED.** ⟦ARCHITECT ANALYSIS⟧ **`PO-T1·8` positions the override in the sequence; it sets no deadline (`AC-29`), constrains no content (`AC-30`) and names no auditor (`AC-31`). `PO-T1·9`'s ordering makes `AC-29` slightly more visible — the override sits between a decision and a conversion that may never come — and that is a visibility consequence, not an answer. NONE resolved** |
| **`AC-24`** — which date the gate evaluates at | **UNCHANGED, NOT ANSWERED, NOT NARROWED.** ⟦ARCHITECT ANALYSIS⟧ **The owner's compression of `P-G` steps 2 and 3 into one sentence names no date. `AC-9`/`T-9` (offline capture date vs sync date) likewise untouched** |
| **`AC-22`** — which registration governs conjunct (iii) | **UNCHANGED, NOT ANSWERED. `PF-5` configuration 2 is not reached by any owner step** |
| **`AC-26`** — is a registration's grounding power SINGLE-USE or REPEATABLE | **UNCHANGED, NOT ANSWERED, NOT NARROWED — CHECKED rather than assumed.** ⟦ARCHITECT ANALYSIS⟧ **`AC-26` is a question about the GROUNDING POWER at steps 2–3 of the owner's sequence. `PO-T2` operates at step 7 and `PO-T1` describes one pass through the sequence without saying whether a second pass may use the same registration. `Y-1`, `Y-3`, `AA-1`, `K-26` and `T-5` — all coupled to `AC-26` — are likewise NOT resolved, NOT narrowed and NOT branched** |
| **`AC-21`** with **`Q10`**/**`M-5`**'s uniqueness half (the client identity term) · **`AA-2`** (project-less preliminary registration) | **UNCHANGED, each CHECKED and each RULED OUT as unaffected.** **`AA-2` in particular is NOT closed by the owner's *"project-scoped"* item** — [§3.5](#35-the-owners-locked-list--each-item-reconfirmed-against-its-exact-source). **`Q-AR` links 1 and 3 remain gated by `AC-21` and `AC-9` exactly as AD-01Q left them** |
| **`AC-8`** (business timezone) · **`AC-5`** (which booking fact is the Booking Date) · **`AC-7`** (snapshot's second half) | **UNCHANGED, each CHECKED FOR A NEW CONSUMER: there is none.** **`PO-T1`/`PO-T2` derive no date, compare no dates and consult no timezone** |
| **`AC-18`** — re-registration cadence, minimum gap, cap | **UNCHANGED, NOT RESOLVED, NOT NARROWED. No cadence, gap or cap is invented** |
| **`AC-1`** (authorization posture of the closing-reason tap) · **`N-4`** (the closing-reason vocabulary) | **UNCHANGED. Both stay NARROWED-NOT-CLOSED exactly as AD-01R and AD-01S left them. `PO-T1` reaches no disposition reason and touches no tap. NO VALUE IS PROPOSED, NAMED, RANKED OR IMPLIED** |
| **`X-24`**/**`A-38`(a)** — `PO-P10`'s incomplete enumeration | **UNCHANGED and NOT FILLED.** ⟦ARCHITECT ANALYSIS⟧ **`PO-T1` is a pipeline, not a levels list. Only the owner may extend their own list** |
| **`X-25`**/**`A-37`** — `NF-27`'s two missing audit events, of which link 8's is one | **NOT DISCHARGED, NOT NARROWED — PRIORITY RAISED, and the reason is new.** ⟦ARCHITECT ANALYSIS⟧ **Link 8 is now the link at which an owner-named authority acts, and it is still the one link backed on only one of `PO-Q2·b`'s two conjunctive substrates** — `PF-91` |
| **`X-5`** | **UNCHANGED. It stays DISCHARGED BY PRODUCT-OWNER DECISION exactly as AD-01R left it** |
| **`L-13`**'s hazard finding · **`MF-9`** · **`X-13`** | **UNAFFECTED AS FINDINGS. The cost `PO-R2` accepts is NOT retired and NOT reduced** |
| **`AC-2`, `AC-3`, `AC-10`, `AC-11`, `AC-12`, `AC-16`, `AC-17`, `AC-19`, `AC-23`** with **`Q11`** (still BLOCKING) | **UNCHANGED — CHECKED, NOT SKIPPED. NONE resolved, NONE narrowed, NONE enlarged. `AC-4` and `AC-14` remain CLOSED and neither is reopened** |
| **`AA-1`, `AA-3`, `AA-4`, `AA-5`** · **`AB-1`, `AB-2`, `AB-3`** · **`T-1`, `T-3`, `T-5`, `T-6 … T-11`** · **`Q10`, `Q11`** · **`V-4`, `W-1`, `Y-1`, `Y-3`, `Y-4`, `Y-5`, `Z-2`, `Z-5`** · **`Q0-a`, `Q0-b`, `Q0-d`, `Q0-e`** | **UNCHANGED — each carried forward and each accounted for. NONE resolved.** ⟦SETTLED⟧ **`V-4` is the cheapest unblocking question in the register for the ELEVENTH consecutive document and has still not been taken. `Q0-d` stays OFF the critical path. `Q0-c` stays ANSWERED with `AC-14`** |
| **`M-9`** (the commission model) | **UNCHANGED, and it still cannot start.** ⟦SETTLED⟧ **It owns every formula, rate, slab, percentage, amount, milestone value, clawback quantum, passback rule and TDS treatment, and NOTHING in this document touches any of them.** Its consumer list is unchanged from AD-01S's |

---

## 4. The amendment register

> ⟦ARCHITECT ANALYSIS⟧ **NO FILE IS EDITED BY THIS DOCUMENT.** Where an amendment is logically required it is
> **NAMED PRECISELY AND NOT MADE**, and the prior document is left exactly as it stands.
>
> **Result of the search against APPROVED decisions: NO contradiction was found with `Q1`, `Q4`, `Q6`, `Q7`,
> `V-1`, `V-2`, `V-3`, `V-7`, `V-11`, `V-12`, `J-2`, `K-18`, `PO-N1 … PO-N9`, `PO-O1 … PO-O14`,
> `PO-P1 … PO-P12`, `PO-Q1`/`PO-Q2`, `PO-R1`/`PO-R2` or `PO-S1`/`PO-S2`.** **Two of the three amendments
> below are NARROWING amendments to an ⟦SOURCE⟧-derived generic phrase the owner has now specified; the
> third is a pure status amendment.**

### 4.1 The three amendments, named and NOT made

| # | Document and target | Amendment — **NAMED AND NOT MADE** |
|---|---|---|
| **`A-51`** | **AD-01P §3.1 — `P-G`'s own table** | **THREE limbs, one target.** **(i) STATUS:** `P-G`'s header reads ⟦ARCHITECT RECOMMENDATION⟧ *"SUBJECT TO THE PROJECT OWNER'S EXPLICIT WRITTEN APPROVAL. NOT APPROVED."* ⟦PRODUCT-OWNER DECISION⟧ **It is now approved in its order and its actors and must be relabelled accordingly** — the same status change `A-20` made for `C-XI` and `A-48 … A-50` for `C-XVI`. **(ii) STEP 5's *"Who acts"* CELL:** it reads *"Builder-side authorized leadership."* **It must record `PO-T2`'s narrowing — the authority whose default holders are the roles shipped as Site Head / Project Head, held as a permission per `C-XX`.** ⟦ARCHITECT ANALYSIS⟧ **This is the amendment that carries the document's one piece of new information, and without it §11's generic phrase stands unqualified in the rule's own definition.** **(iii) THE SEVENTH STEP:** `A-38`(b) proposed it appended after step 6; `PO-T1·8` places it BETWEEN the step-5 decision and the step-6 eligibility test, matching `PO-Q2·c`. **`A-38`(b) is CONTENT-RATIFIED, REPOSITIONED, NOT RENUMBERED and STILL NOT MADE** |
| **`A-52`** | **AD-01Q §2.1's comparison table, and §3.4's permission requirements** | **TWO limbs.** **(i)** §2.1's **"Who decides it"** cell for the attribution/clash decision reads, in full, ⟦SOURCE⟧ **§11**: *"Builder-side authorized leadership resolves attribution."* **It must record `PO-T2`'s narrowing, exactly as its neighbouring cell already names *"the eligibility authorization — `NF-25`, §40, §50"* for the other column.** ⟦ARCHITECT ANALYSIS⟧ **This is the table AD-01S's question was built on and the one a reader will check first; leaving it generic after the owner has specified it is the precise failure `PO-T2`'s closing instruction forbids.** **(ii)** §3.4's requirement 2 requires the override permission to be distinct from *"any general attribution, adjudication or lead-management permission"* — a NEGATIVE with no named counterpart. **It must now name its counterpart: `C-XX`'s step-7 permission.** **Neither limb changes `Q-OV`, `PO-Q1` or any of `AC-27 … AC-31`** |
| **`A-53`** | **AD-01P §6.1, AD-01Q §6.1, AD-01R §6.1 and AD-01S §3.1/§6.1/§6.2** | **A PURE STATUS AMENDMENT, in four documents.** **Each states, in its own words, that `P-G` is an unratified architect derivation** — AD-01S §3.1: *"`P-G` ITSELF IS STILL AN ARCHITECT DERIVATION AND STILL UNRATIFIED"*; AD-01S §6.1 gate 2: *"`P-G` IS NOW THIS GATE'S HEAD"*; AD-01S §6.2's question. ⟦PRODUCT-OWNER DECISION⟧ **All are superseded. `P-G` is ratified in full and gate 2's head is now `C-XV`.** ⟦ARCHITECT ANALYSIS⟧ **No other content in any of the four sections changes, and gate 2 is NOT discharged — it loses one limb of four** |

> ⟦ARCHITECT ANALYSIS⟧ **THREE — the same count as AD-01R and AD-01S, and the count is reported rather than
> fitted.** **Candidates that were CONSIDERED and RULED OUT, shown so the count is checkable:**
>
> | Candidate | Why it is NOT an amendment |
> |---|---|
> | **AD-01N `NF-26`** — the eligibility-exception permission | **Untouched. `C-XX` is a THIRD instance of `NF-26`'s template, and a template gaining an instance is not a change to the template.** `NF-26`(9)'s registration-creation permission is likewise untouched |
> | **AD-01K `K-52`/`K-12`** — clash scoping | **Untouched. Naming who resolves a clash does not change what a clash is scoped to** |
> | **AD-01Q `Q-AR`** | **Untouched. No link is added, removed or given a new requirement** — `PF-91` |
> | **⟦SOURCE⟧ §11 itself** | **NOT AMENDED and CANNOT BE by this document.** ⟦ARCHITECT ANALYSIS⟧ **The narrowing lives in `C-XX` and in the owner's `PO-T2`, not in the spec. Whether the spec text should later be updated is a §88 matter for the owner and is NOT proposed here** |
> | **AD-01S §3.1's `P-G` table** | **Covered by `A-53` and not given a number of its own, on AD-01S's own precedent of folding same-fact status amendments together** |

### 4.2 Documents requiring NO change — each checked

| Document | Result |
|---|---|
| **AD-01 · AD-01A · AD-01B · AD-01C · AD-01E · AD-01F · AD-01G · AD-01H · AD-01I** | **NO CHANGE.** ⟦ARCHITECT ANALYSIS⟧ **Each checked individually. Nothing here reaches the lead state machine, terminality, re-engagement visibility, the dump/redistribution rules, identity/history visibility or the governance report. AD-01A §8.5's no-workflow discipline is OBEYED, not amended; AD-01B §4's *"unrecoverable if decided by implementation"* standard is APPLIED (to `AC-6` and to `AC-15`), not changed** |
| **AD-01J · AD-01K** | **NO CHANGE.** **`J-2`, `K-18`, `K-50`, `K-52`, `K-12`, `K-17`, `K-9`, `K-10`, `K-48`, `C-II`, `C-IV` all confirmed and none amended.** ⟦ARCHITECT ANALYSIS⟧ **Notable and recorded: these are the documents that BUILT the §11 attribution layer and they name no role for it. That is a finding about them, not a defect in them — §11 named none either** |
| **AD-01L · AD-01M** | **NO CHANGE.** **`L-6`, `K-22`, `L-25`, `L-13`, `MF-9` untouched. AD-01M §11.2's Policy-E obligations are RELIED ON at `PF-91` and are NOT amended, NOT extended and NOT narrowed** |
| **AD-01N** | **NO CHANGE.** **`PO-N8`/`PO-N9`, `NF-24`, `NF-25`, `NF-26`, `NF-27`, `NF-28`, `D5`, §F.2 all untouched.** ⟦ARCHITECT ANALYSIS⟧ **§F.2 is QUOTED at [§3.6](#36-the-ac-6-investigation) and is not amended by being quoted; `AC-6` stays exactly as AD-01N minted it** |
| **AD-01O** | **NO CHANGE.** **`O-U`, `OF-4`, `OF-6`, `OF-17` untouched and none ratified** |
| **AD-01R** | **ONE STATUS LIMB ONLY** (`A-53`). **`PO-R1`, `PO-R2`, `PF-55`, `PF-57`, `PF-64` and §3.3's seven-step sequence are all untouched** |
| **AD-01S** | **STATUS LIMBS ONLY** (`A-53`). **`PO-S1`, `PO-S2`, `PF-69`, `PF-70`, `PF-77` and every determination are untouched, and `C-XVI`'s ratification is not disturbed** |

### 4.3 No prior document was silently edited

> ⟦ARCHITECT ANALYSIS⟧ **Stated as a checked fact rather than as an intention, on AD-01Q's and AD-01R's
> precedent.** **Files `00-*.md` through `03s-*.md` are byte-for-byte as they were before this document was
> written. Every amendment above is NAMED AND NOT MADE. Every quotation above is a quotation and not a
> rewrite.**

### 4.4 The register after this document

| | Count |
|---|---|
| **Amendments NAMED AND NOT MADE** | **53** — `A-1 … A-53`, across twelve documents |
| **Of which CONTENT-RATIFIED by the owner but still not made** | **THREE** — `A-32` (AD-01R), `A-39` (AD-01S), **`A-38`(b)** (this document) |
| **Constraints** | **`C-I … C-XX`** — `C-XX` minted here |
| **Contradictions** | **`X-1 … X-25`** — `X-26` NOT allocated |
| **Open questions** | **`AC-1 … AC-33`** — `AC-33` minted here; `AC-4` and `AC-14` CLOSED; `AC-1` and `N-4` NARROWED-NOT-CLOSED |
| **Findings in the shared series** | **`PF-1 … PF-94`** |

---

## 5. Remaining open items

### 5.1 The one new question, `AC-33`

> ⟦ARCHITECT ANALYSIS⟧ **The `AC` series moves to `AC-33`, which AD-01S expressly left unallocated. This is a
> CHECKED RESULT: the question is shown to be answerable-only-now, and shown NOT to be a duplicate of the two
> items it most resembles.**

**Why it did not exist before.** ⟦ARCHITECT ANALYSIS⟧ **While the step-7 authority was ⟦SOURCE⟧ §11's generic
*"builder-side authorized leadership"* and the step-8 authority was `PO-Q1·a`'s named *Site Head*, the
relationship between them was undefined — one generic term and one name, with no stated overlap. `PO-T2` now
names the step-7 authority as the roles shipped as *Site Head* / *Project Head*, and `PO-Q1·a` names the
step-8 authority as *Site Head*. THE TWO NAMES OVERLAP. In a default shipping configuration, one role would
hold both — and a holder could therefore override their own attribution decision.**

> ⟦BUSINESS DECISION REQUIRED — `AC-33`⟧
>
> **May the individual who made the ordinary attribution/clash decision at step 7 also exercise the
> attribution override at step 8 UPON THEIR OWN DECISION? And, separately, should the default shipping
> configuration grant one role both the step-7 decision permission and the `Q-OV` override permission?**
>
> **Basis:** ⟦PRODUCT-OWNER DECISION⟧ **`PO-T2` + `PO-Q1·a`**, whose named holders overlap ·
> ⟦ARCHITECT RECOMMENDATION⟧ **`C-XX`(2)** · AD-01Q **§3.4**(2) and (5) · ⟦SOURCE⟧ consolidated **§22**
> (separation by responsible function) · ⟦SOURCE⟧ **§88** MUST-ASK: **authorization rules**.
>
> ⟦ARCHITECT ANALYSIS⟧ **NO ANSWER IS PROPOSED, PREFERRED, RANKED OR IMPLIED. No default shipping
> configuration is designed, and `C-XX` does not assume one. NO SEPARATION MATRIX IS DESIGNED — that is
> `M-9`'s and `M-3`'s. An answer of *"yes, freely"* is available to the owner and leaves `C-XX` exactly as it
> stands, which is where an answer of *"no"* also leaves it; `C-XX` is about the DISTINCTNESS of two
> permissions, never about who may hold both.**

**Why it is NOT `AC-27`, and why it is NOT `AC-13`.**

| | Its pair | Its risk |
|---|---|---|
| **`AC-27`** (AD-01Q) | **Two OVERRIDES** — the attribution override (`PO-P10` level 5) and the eligibility exception (level 7) | **One individual steering a single commercial outcome through two separate override layers** |
| **`AC-13`** (AD-01N, twice enlarged) | **An authority and a BENEFICIARY** — relationship manager, reporting line, own filed claim | **Self-interest in the outcome** |
| **`AC-33`** (this document) | **A DECISION and ITS OWN OVERRIDE** — both at `PO-P10` level 5, on the same Inquiry | **Self-review: the mechanism `PO-Q1` created as a check on a decision being exercised by the person checked.** ⟦ARCHITECT ANALYSIS⟧ **Neither `AC-27` nor `AC-13` asks this. `AC-27`'s pair is two overrides; `AC-13`'s concern is interest, not authorship** |

> ⟦ARCHITECT ANALYSIS⟧ **One thing `AC-33` is NOT, stated because the temptation to over-read it is real.**
> **It is NOT a criticism of `PO-T2`, and it does not qualify, condition or reopen it.** **It is the
> ordinary consequence of specifying an authority: an unspecified authority cannot collide with a specified
> one, and a specified one can. The collision is visible only because the clarification was made, which is
> an argument for the clarification and not against it.**

### 5.2 The rest, carried forward

**An INDEX onto [§3.8](#38-which-open-items-this-decision-actually-reaches), which is the authority.**

| Effect | Items |
|---|---|
| **RATIFIED** | **`P-G`** — in full, in its order and its actors, by direct Product-Owner ruling; the THIRD item in the series closed that way and the FIRST entire rule |
| **CONTENT-RATIFIED, REPOSITIONED, NOT RENUMBERED, STILL NOT MADE** | **`A-38`(b)** — `P-G`'s seventh step |
| **NEW** | **`C-XX`** (the step-7 authority clause) · **`AC-33`** (decision-then-own-override). **The `X` series stands at `X-25`** |
| **ENLARGED, NOT ANSWERED** | **`AC-13`** — a third decision point |
| **SHARPENED, NOT NARROWED IN WHAT THEY ASK** | **`AC-15`**/**`AC-20`** (a named authority with no stated rule — a SEVENTH consecutive document) · **`AC-28`** (the asymmetry is now between two named acts) · **`X-25`**/**`A-37`** (link 8's substrate gap, priority raised) |
| **NARROWED IN ANSWER SPACE ONLY, NOT ANSWERED** | **`AC-25`**'s second half — an unopposed path under a DIFFERENT authority is excluded; both halves stay OPEN |
| **CHECKED FOR AN EFFECT AND FOUND TO HAVE NONE ON WHAT THEY ASK** | **`AC-32`** (accountable, not systemic) · **`AC-6`** (no fourth consumer — **and a DISCREPANCY reported**) · **`AC-24`**, **`AC-22`**, **`AC-26`**, **`AC-27`**, **`AC-29`**, **`AC-30`**, **`AC-31`**, **`AA-2`**, **`AC-8`**, **`AC-5`**, **`AC-7`** |
| **UNCHANGED — CHECKED, NOT SKIPPED** | **`AC-1`, `AC-2`, `AC-3`, `AC-9`/`T-9`, `AC-10`, `AC-11`, `AC-12`, `AC-16`, `AC-17`, `AC-18`, `AC-19`, `AC-21`, `AC-23`** · **`AA-1`, `AA-3`, `AA-4`, `AA-5`** · **`AB-1`, `AB-2`, `AB-3`** · **`T-1`, `T-3`, `T-5`, `T-6 … T-11`** · **`N-4`** · **`Q10`**, **`Q11`** (still BLOCKING) · **`V-4`** (eleventh document), **`W-1`**, **`Y-1`, `Y-3`, `Y-4`, `Y-5`**, **`Z-2`, `Z-5`**, **`M-5`**'s uniqueness half, **`M-9`** · **`L-13`**, **`MF-9`**, **`X-13`** as findings · **`X-24`**/**`A-38`(a)** |
| **STILL CLOSED / DISCHARGED, NOT REOPENED** | **`AC-4`** (AD-01P) · **`AC-14`** and **`Q0-c`** (AD-01R) · **`C-XVI`** (RATIFIED at AD-01S) · **`X-5`** (DISCHARGED at AD-01R) |
| **NOT ANSWERED** | **`Q0-a`**, **`Q0-b`**, **`Q0-e`** — **`Q0-e`'s list is now `A-1 … A-53`**. **`Q0-d` stays OFF the critical path** |

---

## 6. Recommendation

> ⟦ARCHITECT RECOMMENDATION — advisory only⟧ *(This is a recommendation about **SEQUENCE and READINESS**. It
> is **NOT** an approval, **NOT** a self-authorization, and **NOT** permission to build anything. ⟦SOURCE⟧
> **§88** places this decision in the **authorization rules**, **CP commission logic** and **audit
> requirements** MUST-ASK columns. **Delegation to an architect is not authorization.** Per CLAUDE.md:
> assigning work to `architect` does not satisfy the §88 layer, and nothing in
> [§3](#3-the-determinations), [§4](#4-the-amendment-register) or [§5](#5-remaining-open-items) becomes
> approved by having been written.)*

### **VALIDATE**

**Not BUILD NOW. Not BUILD LATER. Not REJECT.**

> ⟦ARCHITECT ANALYSIS⟧ **The instruction to check honestly whether ratifying the full sequence changes the
> recommendation materially was taken seriously and the check was run. IT DOES NOT — but something adjacent
> to it does, and the honest report is both halves.**
>
> **MORE IS NOW RESOLVED THAN AT ANY PRIOR POINT IN THIS SUB-SERIES, and that is stated plainly rather than
> minimised.** **Three items now stand closed by direct Product-Owner ruling where AD-01Q had none.** **Gate
> 2 has fallen from five limbs to three.** **The rule at the centre of the sub-series — its order, its
> actors and its governing authority at every step — is signed.** ⟦ARCHITECT ANALYSIS⟧ **A document that
> reported this as *"nothing much has changed"* would be as dishonest as one that reported it as
> buildable.**
>
> **AND IT IS STILL `VALIDATE`, for a reason that is arithmetic rather than temperament: a ratified SEQUENCE
> is not a ratified ARCHITECTURE, and the single most load-bearing step in the newly-ratified sequence still
> has no stated rule to apply.**

**Why not REJECT.** ⟦PRODUCT-OWNER DECISION⟧ **The decision is the Project Owner's, it is internally
coherent, it is compatible with every approved decision in the chain, and it CONTRADICTS NOTHING** —
[§3.7](#37-no-existing-rule-is-contradicted). **It ratifies in full the rule AD-01S named as *"the largest
single thing standing between this sub-series and a buildable architecture."* It supplies, in one sentence,
an authority that neither the Master Spec nor eleven prior documents had named. It keeps three decision
points apart that an implementation would merge by default. And it requires only three amendments.**

**Why not BUILD NOW.** ⟦ARCHITECT ANALYSIS⟧ **Because a sequence tells you the order of the steps and the
hand at each one; it does not tell you what the hand decides.**

- **The gating list remains EIGHT items. Gate 2 is NOT discharged — it loses ONE limb of four.** **Gate 2
  had four remaining limbs after AD-01S; `P-G` discharges here; THREE remain — `C-XV`, `PF-6` and `PF-23`,
  all architect derivations and all unratified. `C-XV` is now gate 2's head.**
- **`AC-15`/`AC-20` — the RULE the newly-named authority applies — are unanswered for a SEVENTH consecutive
  document, and this decision sharpens rather than relieves them.** ⟦ARCHITECT ANALYSIS⟧ **The architecture
  now has a named, permissioned, project-scoped, server-enforced, audited authority for CP-vs-CP clash
  resolution and no stated basis on which it resolves. That is a more conspicuous hole than the one AD-01S
  left, not a smaller one.**
- **Gate 3 is ENTIRELY UNTOUCHED.** **`Q-OV`, `C-XVII`, `C-XVIII`, `C-XIX`, `Q-AR`, AD-01Q §3.3's minimum
  field set and §3.4's permission are ALL STILL UNRATIFIED, and `Q-AR` remains OWNER-RATIFIED AND NOT YET
  SATISFIABLE.** **`C-XX` joins them as a new unratified recommendation.**
- **`AC-6` is listed by the owner as locked and IS NOT** — [§3.6](#36-the-ac-6-investigation). **It is
  TRIPLY money-bearing and all three uses must agree.**
- **`AC-25` (both halves), `AC-26`, `AC-22`, `AC-24` with `AC-9`/`T-9`, `AC-23` with `Q11` (BLOCKING),
  `AA-2`, `AC-21` with `M-5`'s uniqueness half, and `AC-27 … AC-32` are all untouched, and `AC-33` is
  new.**
- **Fifty-three amendments stand NAMED AND NOT MADE across twelve documents.**

**Why not BUILD LATER.** ⟦ARCHITECT ANALYSIS⟧ **Because *later* implies the blockers need further ANALYSIS,
and they do not. Every remaining gate is a short answer from the Project Owner — and `AC-15` is the one
answer no amount of architect work can substitute for, because `C-IX` holds that supplying it would be a §88
authorization change to §11. A further architect document would be analysis substituting for a decision —
AD-01L's own words, applied now for the eighth time.** **And shelving would waste what this answer has
bought: the sub-series' central rule moved from a derivation nobody had signed to a rule an implementation
may not depart from.**

### 6.1 The gating list after this decision

> ⟦ARCHITECT ANALYSIS⟧ **AD-01S's eight-item list, restated with gate 2's HEAD DISCHARGED and nothing else
> changed except where a new item genuinely lands. Still eight items — a gate with three remaining limbs is
> not a discharged gate. Every one is an owner answer. NONE is answered here and NO default is implied for
> any of them.**

| # | Gate | Status |
|---|---|---|
| **~~1~~** | ~~`AC-14` — does a registration whose inquiry was closed as never a real opportunity remain usable~~ | ⟦PRODUCT-OWNER DECISION⟧ **DISCHARGED at AD-01R. ANSWERED YES — Option A. LOCKED** |
| **2** | **`C-XV`**, **`PF-6`** and **`PF-23`** — AD-01P gate 2's THREE remaining limbs. ~~`C-XVI` (with `A-39`)~~ ~~`P-G` (with `A-38`(b))~~ | ⟦PRODUCT-OWNER DECISION⟧ **`C-XVI` LIMB DISCHARGED at AD-01S. `P-G` LIMB DISCHARGED HERE — RATIFIED IN FULL** → `A-51`, `A-52`, `A-53`; **`A-38`(b) CONTENT-RATIFIED.** **The THREE remaining limbs are ALL ARCHITECT DERIVATIONS and ALL UNRATIFIED. `C-XV` IS NOW THIS GATE'S HEAD** |
| **3** | **`Q-OV`, `C-XVII`, `C-XVIII`, `C-XIX`, `Q-AR`, AD-01Q §3.3's minimum field set and §3.4's permission — and now `C-XX`** | **ALL UNRATIFIED.** `C-XVIII` carries the governance consequence; **`C-XX` carries the authority `PO-T2` requires to be defined** |
| **4** | **`AC-27`** — may one individual perform both overrides on one outcome — **joined by `AC-33`** — may the step-7 decision-maker override their own decision | **NEITHER ANSWERED.** ⟦ARCHITECT ANALYSIS⟧ **`AC-33` lands here rather than at gate 8 because it is an authorization-model question of the same kind as `AC-27`, and the two should be taken together or neither will make sense** |
| **5** | **`AC-30`** — what an override's decision may SAY | **NOT ANSWERED** |
| **6** | **`AC-29`** — until when may an override be performed | **NOT ANSWERED** |
| **7** | **`AC-28`** and **`AC-31`** — the evidence asymmetry; who the auditor is | **NOT ANSWERED. `AC-13` (self-interest controls) sits beside them, ENLARGED a second time** |
| **8** | **`AC-15`** and **`AC-20`** (a SEVENTH consecutive document) · **`AC-25`** (both halves) · **`AC-12`** as enlarged · and the operand tier — **`AC-26`, `AC-22`, `AC-24` with `AC-9`/`T-9`, `AC-23` with `Q11`, `AC-6`, `AA-2`, `AC-21` with `M-5`'s uniqueness half**, with **`AC-32`** as a VALIDATE / BUILD-LATER consideration | **NOT ANSWERED. No precedence is invented. `Q11` still BLOCKING. `AC-6` carries the discrepancy notice** |

**What VALIDATE means concretely.**

1. **Take gate 2 first** — it remains the head of the list, and **`C-XV` is now its head in turn**, with
   `PF-6` and `PF-23` beside it.
2. **Then gate 3** — AD-01Q's seven derivations plus `C-XX`. **Gates 2 and 3 are one sitting**, and now more
   tightly than before: `C-XX` is the FORM of `PO-T2`, `PO-T2` narrows `P-G` step 5, and `A-51`/`A-52`
   cannot be MADE until the documents that host `P-G` and AD-01Q §2.1 are signed off.
3. **Then gates 4–8 in the commercial sitting that NINE consecutive documents have now recommended** —
   AD-01L §15, AD-01M §14, AD-01N §M, AD-01O §5.5, AD-01P §6.1, AD-01Q §6.1, AD-01R §6.1, AD-01S §6.1 and
   this section. **`AC-33` should be taken with `AC-27` in that sitting.**
4. **Correct the `AC-6` record.** ⟦ARCHITECT ANALYSIS⟧ **The owner should be told, in one line, that the
   boundary convention is NOT closed and that their sentence is true under both conventions depending on
   whether the registration date is day zero or day one. This is a NOTICE, not a gate, and it is item 4 and
   not item 1 because it blocks no ratification — but it is money-bearing three times over and an
   implementer will otherwise resolve it by accident.**
5. **`AC-32` remains explicitly NOT on the critical path and must not be allowed to delay any of the
   above.** ⟦PRODUCT-OWNER DECISION⟧ **`PO-R1`, `PO-S1` and `PO-T1` are each locked whichever way `AC-32`
   is answered.**
6. **Ratify or reject `A-51 … A-53` alongside `A-1 … A-50`** (`Q0-e`). **Fifty-three unmade amendments
   across twelve documents.**
7. **Nothing may be built meanwhile that presupposes an answer to any remaining gate.** ⟦SOURCE⟧
   consolidated **§30**. **Everything AD-01P §6.1(4), AD-01Q §6.1(5), AD-01R §6.1(6) and AD-01S §6.1(6)
   list as NOT-TO-BE-BUILT remains not-to-be-built, and this decision adds to that list: no role-name test
   anywhere in logic for the step-7 decision, for the step-8 override, or for any commission authority; no
   single permission spanning the step-7 decision and the step-8 override; no precedence, ranking, tiebreak
   or default outcome supplied to the step-7 decision-maker; no boundary convention chosen for `AC-6`; and
   no assumption that the step-7 and step-8 authorities may or may not be the same individual.**

### 6.2 The one next question

> ⟦BUSINESS DECISION REQUIRED — ONE QUESTION, AND ONLY ONE⟧
>
> **This is `AC-15`/`AC-20`, stated in the same plain register the owner has been answering in. It is the
> single highest-priority remaining item, and it is asked alone because a list of several is how a gating
> list stops being taken.**
>
> > **"You have now told us who decides when two channel partners both claim the same buyer: your Site Head
> > or Project Head. What we still have no answer for is WHAT THEY DECIDE ON.**
> >
> > **Today, when two partners each hold a live registration for the same buyer and the same project — or
> > when one holds a registration and the other actually brought the enquiry — there is nothing written down
> > that tells your Site Head how that should come out. Every time, they would be deciding it fresh.**
> >
> > **So: do you want a stated basis they must follow — and if so, what is it? Or do you want it left
> > entirely to their judgement, case by case, with only their written reasons on the record?**
> >
> > **And, either way: should a second partner even be allowed to register the same buyer for the same
> > project while the first partner's registration is still running — or should the system refuse the second
> > registration outright?"**
>
> ⟦ARCHITECT ANALYSIS⟧ **Why this one, and not another — verified against AD-01S's own restated gating list
> rather than assumed.**
>
> | Candidate | Why it is not the question |
> |---|---|
> | **Gate 2's three remaining limbs** (`C-XV`, `PF-6`, `PF-23`) | **They are RATIFICATIONS of material already written in full. The owner can sign or decline each; nothing must be authored. `PF-6` is a no-change finding; `PF-23` offers the owner a stated alternative; `C-XV` is a distinctness rule with no open content** |
> | **Gate 3** (`Q-OV` and the rest, plus `C-XX`) | **Same shape — seven derivations and one new clause awaiting signature, none requiring the owner to author a rule** |
> | **`AC-25`** | **Genuinely open and genuinely important, but it decides WHETHER `PO-P1` executes automatically, not HOW a contest comes out. AD-01P put it at the top of the register when `AC-14` was open; with `AC-14`, `C-XVI` and `P-G` all closed, the contest itself is the larger hole** |
> | **`AC-6`** | **The closest runner-up, and the reason it is NOT the question is given rather than assumed: it is ONE WORD, it blocks no ratification, and the owner can supply it at any point in any sitting. `AC-15` is a POLICY that may not exist yet, that only the owner may author (`C-IX`), and that sits at the exact step the owner has just ratified and staffed. `AC-6` is carried instead as a NOTICE at [§6.1](#61-the-gating-list-after-this-decision) item 4, which is the form that fits a one-word correction** |
> | **`AC-33`** | **New here, and a governance refinement of two authorities that both already exist. It sharpens gate 4; it blocks nothing that `AC-15` does not block first** |
>
> **And the positive reason, stated once.** ⟦ARCHITECT ANALYSIS⟧ **`P-G` step 5 — the owner's steps 4, 6 and
> 7 — is the step every other step exists to reach. AD-01P said in terms that *"if any step's actor or
> authority is not what the owner intends, `P-G` changes and most of §3 changes with it,"* and the owner has
> now fixed both. What is left at that step is its CONTENT, and the content is `AC-15`/`AC-20`.** **Seven
> consecutive documents have raised it; `C-IX` forbids the architect answering it; `C-XVIII` forbids
> `Q-OV` becoming the answer by accumulation; and `C-XVI` — now at owner status — forbids the one default
> an implementation would otherwise supply by itself. There is no route to it except the owner.**
> **`AC-6` is deliberately NOT folded into this question and remains open in its own right, with the
> discrepancy notice attached. `AC-25`, `AC-33` and `AC-27` are likewise NOT folded in and remain open in
> their own right.**

---

## Closing note

**STATUS: PROPOSED — NOT APPROVED. RECONCILIATION — ADVISORY. NOT AN APPROVAL TO IMPLEMENT.**

**This document is a reconciliation and a recommendation, not a decision and not an approval.**
[§6](#6-recommendation)'s **VALIDATE** is **advisory.** **Nothing here is self-authorizing, and being asked
to record a Product-Owner ratification is not permission to build what it implies.** ⟦SOURCE⟧ Spec **§88**:
CP commission logic, authorization rules, source-of-truth rules and audit requirements are **MUST ASK BEFORE
DECIDING**; *"inventing business rules"* is **MUST NEVER DO WITHOUT EXPLICIT AUTHORIZATION.** ⟦SOURCE⟧
Consolidated **§30**: *"No schema/code implementation should be inferred from an unresolved product
ambiguity."*

**THE RATIFICATION, STATED ONCE MORE BECAUSE IT IS THE WHOLE POINT OF THE DOCUMENT.** ⟦PRODUCT-OWNER
DECISION⟧ **THE FULL `P-G` SEQUENCE IS RATIFIED IN FULL BY THE PROJECT OWNER — its order, its actors and its
governing authority at each step.** **A genuine Inquiry originates in its own right; the system checks for a
currently-live registration on the same Client × Project; if one exists a claim may be recorded citing that
specific registration record; the claim is evaluated through the normal attribution/clash process; the
registration carries no special weight of any kind; the claim can lose; the authorised decision-maker
resolves it; Site Head may afterwards exercise the separately defined override; only then, and only on
conversion, is commission eligibility evaluated; and approval and payout remain downstream of eligibility.**
**This is the THIRD item in the series closed by a direct Product-Owner ruling rather than by an architect
derivation — after `AC-14` at AD-01R and `C-XVI` at AD-01S — and the FIRST to ratify an entire multi-step
RULE rather than a single point within one.**

**THE ONE PIECE OF NEW INFORMATION, STATED PLAINLY BECAUSE HIDING IT INSIDE A RATIFICATION WOULD BE THE
DOCUMENT'S WORST AVAILABLE ERROR.** ⟦PRODUCT-OWNER DECISION⟧ **`PO-T2` NARROWS ⟦SOURCE⟧ §11's generic
*"builder-side authorized leadership"* to a named authority — the roles shipped as Site Head / Project Head —
for the ordinary attribution/clash decision, FOR THE FIRST TIME.** **No Master Spec section and no document
from AD-01J through AD-01S named a role for that step; §11 names none and AD-01Q §2.1's own *"Who decides
it"* cell cites the generic phrase.** **The narrowing is carried as `C-XX` as a PERMISSION, never as a
role-name branch, on the template `NF-26`(1) and AD-01Q §3.4(1) already applied to the owner's two prior
role-namings.** **The decision is NOT re-argued, NOT weighed, NOT ranked, NOT hedged and NOT softened
anywhere above.** It is recorded **verbatim** at
[§1.2](#12-the-decision-recorded-verbatim-before-any-analysis) as `PO-T1·1 … PO-T1·10`, `PO-T2` and `PO-T3`,
**before any analysis touched it.**
