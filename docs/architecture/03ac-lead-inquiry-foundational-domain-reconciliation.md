STATUS: PROPOSED — NOT APPROVED

# AD-01AC — FULL ADVERSARIAL RECONCILIATION OF THE FOUNDATIONAL `Lead` / `Client` / `Inquiry` / `Assignment` / `Dump` DOMAIN AGAINST THE ACCUMULATED PRODUCT-OWNER LEDGER: THE `Lead`/`Inquiry` SPLIT ON WHICH EIGHTEEN CP-DOMAIN DOCUMENTS REST IS RECORDED AS **APPROVED** BY AD-01L AND AD-01P AND AS **NOT APPROVED** BY AD-01G, AD-01H AND AD-01I — `X-35` IS MINTED FOR THAT STATUS CONTRADICTION AND `AC-49` FOR THE RATIFICATION IT REQUIRES; `X-28`'s EXTENT IS FOUND TO BE MATERIALLY LARGER THAN TEN BECAUSE ITS **ORIGIN** IS AD-01 §7 `E-13` AND EVERY PRIOR HUNT'S SEARCH SPACE BEGAN AT AD-01H; `W-3` IS CONFIRMED GENUINELY OPEN AND IS **ENLARGED** BY THE BULK-TRANSFER PATH THAT CAN REACH ITS EIGHTH CASE WITHOUT ANYONE DECIDING TO; THE ORIGINAL AD-01 STATE MACHINE IS FOUND TO HAVE COUPLED **FIVE** AXES THAT LATER DOCUMENTS HAD TO UN-COUPLE, TWO OF THEM BY PRODUCT-OWNER DECISION; `Dump` IS CONFIRMED **NOT** TO HAVE BECOME A UNIVERSAL TERMINAL STATE AND THE CP CHAIN IS FOUND TO HAVE ADOPTED THE INQUIRY-LEVEL READING CORRECTLY; AND ONE OF THE THIRTEEN STARTING-POINT STATEMENTS — *"New is derived, not a lifecycle state"* — IS **CONTRADICTED** BY A RECORDED PRODUCT-OWNER DECISION THAT HAS NEVER BEEN AMENDED

**What this document is.** Every document from AD-01J (`03j`) through AD-01AB (`03ab`) has worked inside the
CP / Channel-Partner attribution-and-commission domain. **This document steps back and audits the
FOUNDATIONAL domain built first — `03` through `03i` — fresh, adversarially, and without granting it the
benefit of incumbency.** The nine foundational documents are treated as **a subject for audit, not as
settled ground**, exactly as AD-01F §5.1 declined to give AD-01E's episode the benefit of incumbency and as
AD-01G §1.4 set aside the Product Owner's own provisional preference before testing it.

**The owner's standing process instruction, recorded here because it governs this document.**
⟦PRODUCT-OWNER DECISION⟧ ***"Do not accelerate anything in entire Application, keep auditing keep asking
questions."*** ⟦ARCHITECT ANALYSIS⟧ **It is honoured concretely: no prior document is edited; no
contradiction is silently repaired; no Product-Owner decision is manufactured, narrowed or inferred; no
open item is closed by adjacency; the Lead/Inquiry architecture is NOT declared reconciled or complete
([§20](#20-remaining-open-questions) states that in terms); and the comprehensive Audit Completeness Gate
is NOT opened, not designed and not partially designed.**

**What this document's HEADLINE FINDINGS are, and there are eight.**

⚠ **FIRST — `X-35`, MINTED: THE FOUNDATIONAL SUBSTRATE IS RECORDED AT TWO INCOMPATIBLE STATUSES.**
**AD-01L §2.2 records `V-1`, `V-2`/`V-3` and `V-11`/`V-12` as ⟦PRODUCT-OWNER DECISION⟧ … *"Approved. NOT
reopened."*, and AD-01P §3 writes *"`V-1` (approved)"*.** ⚠ **AD-01G, AD-01H and AD-01I — the documents
that produced them — each carry `STATUS: PROPOSED — NOT APPROVED`, label `G-1`/`H-4`/`I-1` as
⟦ARCHITECT RECOMMENDATION⟧ *"NOT APPROVED. NOT APPROVED FOR IMPLEMENTATION"*, and AD-01I §1.5 says in terms
that `G-1`, `G-2`, `G-8`, `G-9` and `H-1` are *"treated as input"* — which is not the same as approved.**
**Eighteen CP-domain documents carry the standing clause *"`V-1`, `V-2`, `V-3`, `V-7`, `V-11`, `V-12` … are
NOT reopened"*, which is a scope rule and reads as a status claim.** — [§11.3](#113-x-35--the-status-contradiction-at-the-seam), [§18.1](#181-x-35-stated-in-full).

⚠ **SECOND — `AC-49`, MINTED, AND IT IS THIS DOCUMENT'S SINGLE CLOSING QUESTION.** ⚠ **The remedy for
`X-35` is not an architect act. `A-81` names the editorial correction and is NOT made. The substantive
question — whether the Lead/Inquiry split is `PO LOCKED` or `ARCHITECT DERIVED` — is the owner's, is
un-owned by any existing register item (`Q0-a … Q0-e` are CP-specific ratifications; §19.4 item 12's
approve/reject list runs AD-01N onward and does not reach AD-01G/H/I), and is money-bearing because
`K-18`, `P-G`, the two-stage model and every Stage-1 scoping rule presuppose the Inquiry.** —
[§19.4](#194-ac-49-minted), [§20.3](#203-the-single-question).

⚠ **THIRD — `X-28`'s EXTENT IS MATERIALLY LARGER THAN RECORDED, AND ITS ORIGIN IS OLDER THAN ANY HUNT HAS
LOOKED.** **`X-28` is recorded at *"ten places across eight documents"*, the earliest being AD-01H §8.2.**
⚠ **The formulation *"commission keys on the resolved claim"* was MINTED at AD-01 §7 `E-13` and is restated
at AD-01 `E-12`, AD-01C §2.2 and §3.2, AD-01E §9 and §11 `R6`, and AD-01F `A-11` and `F-11` — EIGHT further
instances in FOUR documents that no `X-28` hunt has ever surveyed, because every hunt's search space began
at AD-01H.** **`X-28` IS ENLARGED AND NOT RENUMBERED, on AD-01Y's and AD-01Z's own standard. `A-82` names
the foundational remedy and is NOT made.** — [§11.4](#114-x-28s-true-extent-and-its-origin).

⚠ **FOURTH — `W-3` IS CONFIRMED GENUINELY OPEN, AND IT IS ENLARGED RATHER THAN RESOLVED.** **Re-read at
AD-01G §15.1 where it was minted. AD-01H, AD-01I and AD-01K each record it *untouched*; no CP document
reaches it.** ⚠ **AND AD-01G §8.2's ground for calling the eighth case *"undescribed"* — that
redistribution names *"a fresher"*, who is by construction not the prior handler — **does not cover the
BULK path**: `R-10`'s multi-select transfer and §57's absorption of a departed employee's book are acts
over record SETS whose prior-handler membership nobody screens, so the eighth case can be reached **without
anyone deciding to perform it**. **`W-3` is therefore not merely undescribed; it is reachable by
accident.** — [§7.4](#74-w-3--re-confirmed-open-and-enlarged).

⚠ **FIFTH — THE ORIGINAL AD-01 STATE MACHINE COUPLED FIVE AXES THAT LATER DOCUMENTS HAD TO UN-COUPLE, AND
TWO OF THE UN-COUPLINGS ARE RECORDED PRODUCT-OWNER DECISIONS.** **AD-01 §5.1 coupled transport sync to
business verification (split by AD-01A §8.4, PO-decided); §5.1/§6.3 persisted an assignment-condition axis
(eliminated by AD-01A §8.5, PO-decided); §5.1 put the lifecycle on the record (relocated to the Inquiry by
AD-01H `H-4`, amendment `A-1` NAMED AND NOT MADE); §13's single disposition control conflated commercial
closure with queue exit (decomposed by AD-01I `I-1`); and §3.6 modelled attribution as ONE axis with a
tri-valued contest state (split by the CP chain into Stage-1 at the Inquiry and Stage-2 at the Booking).**
⚠ **The original design's own separation test was sound; what it got wrong was the LEVEL at which each axis
attaches — which is precisely AD-01F `A-14`'s verdict, reached again here independently.** —
[§17](#17-state-machine-reconciliation--the-independent-axes).

⚠ **SIXTH — `Dump` HAS **NOT** BECOME AN ACCIDENTAL UNIVERSAL TERMINAL STATE, AND THE CP CHAIN IS NOT THE
PLACE IT WAS AT RISK OF BECOMING ONE.** **AD-01I `I-1` decomposes it into three independent axes and two
projections; AD-01L `L-2` adopts the Inquiry-level reading verbatim (*"Inquiry with a terminal closure
carrying a Q7 classified reason"*); AD-01R's `AC-14` Option A operates on the REGISTRATION, not on the work
record.** ⚠ **BUT a real coupling exists and it runs the other way: `PO-R1` locks a MONEY rule over
Dimension A's invalid class while `N-4` — which owns every value of that class — is unresolved, and `Z-2`
and `Z-5`, which ask what that class means about SCOPE and about redistribution eligibility, are open in
the Lead domain.** — [§4](#4-dump-semantics), [§11.5](#115-the-dimension-a-coupling).

⚠ **SEVENTH — ONE OF THE THIRTEEN STARTING-POINT STATEMENTS IS `CONTRADICTED` BY A RECORDED PRODUCT-OWNER
DECISION.** ⚠ **Statement 9 — *"'New' is derived, not a lifecycle state"* — is an ARCHITECT
recommendation (`AD-01F V-5`/`V-6`, `AD-01H H-5`) whose required amendment is `A-1`, **named and not
made**. **AD-01A §8.1 — a recorded Product-Owner decision — still says the lifecycle is four values
INCLUDING `New`.** **Six further statements are accurate in content and MIS-STATED in status, level or
completeness.** — [§1.5](#15-the-thirteen-starting-point-statements-audited-individually).

⚠ **EIGHTH — THE `Assignment Log` IS ASKED TO CARRY THREE FACTS IT DOES NOT YET CARRY, AND ALL THREE ARE
UNRECOVERABLE IF NOT RECORDED AT THE MOMENT OF THE ACT.** **`AD-01A §8.5` (PO-decided) put assignment
information on Owner / Handler / Assignment Log. `AD-01G G-9` adds the with/without-history decision and
assignment provenance. `AD-01I I-8`/`A-2` adds management intent (the work mandate).** ⚠ **Three
additions, none approved, each named *"unrecoverable if not recorded at the moment of the act"* by the
document that added it. The statement *"Assignment is derived from Owner + Handler + Log"* is accurate and
materially incomplete.** — [§6](#6-assignment-and-custody).

---

## How to read this document

| Label | Meaning |
|---|---|
| ⟦SOURCE⟧ | A verbatim or closely-paraphrased statement from `docs/BMEXA_MASTER_SPEC.md`, `BMexa_Base_Version_Product_Owner_Requirements_Consolidated.md` or `docs/ENGINEERING_RULES.md`. |
| ⟦PRODUCT-OWNER DECISION⟧ | A decision the Product Owner has recorded — in AD-01A §8, AD-01C §1.2, AD-01E §1.1, AD-01I §1, or the `PO-` clause sets of the CP chain. **Authoritative business input. Never re-argued, ranked, hedged or softened here.** |
| ⟦SETTLED⟧ | A matter already determined by a prior document and carried, not re-derived. Cited to the document that determined it. |
| ⟦ARCHITECT ANALYSIS⟧ | A finding derived from those sources. Mine to defend. |
| ⟦ARCHITECT RECOMMENDATION⟧ | A proposed course of action. **RECOMMENDED, SUBJECT TO THE PROJECT OWNER'S EXPLICIT WRITTEN APPROVAL. NOT APPROVED.** |
| ⟦BUSINESS DECISION REQUIRED⟧ | The sources are silent or in conflict and **nothing here fills the gap.** |

**The owner's five-way taxonomy** — `PO LOCKED` · `ARCHITECT DERIVED` · `VALIDATE-OPEN` · `SUPERSEDED` ·
`CONTRADICTED` — is applied item by item at [§19.2](#192-the-five-way-taxonomy-applied-to-the-foundational-register).
**A restatement, a paraphrase or a derived consequence is NOT counted as a new Product-Owner decision.**

### Numbering discipline, verified at source rather than assumed

⟦ARCHITECT ANALYSIS⟧ **The foundational domain and the CP domain use DIFFERENT identifier series, and two
of them COLLIDE. Every high-water mark below was verified by reading, not by carrying a summary.**

| Series | Meaning | Owner | High-water verified | This document |
|---|---|---|---|---|
| `Q1 … Q16` | AD-01 §10's open product decisions | AD-01 | `Q16` | **Not extended** |
| `E-01 … E-24` | AD-01 §7's edge cases | AD-01 | `E-24` | **Not extended** |
| `N-1 … N-4` | AD-01A §6.2's open items | AD-01A | `N-4` | **Not extended** |
| `D-1 … D-12`, `X-1 … X-7` | AD-01B's dependency edges | AD-01B | `D-12`, `X-7` | **Not extended — see the collision note** |
| `T-1 … T-12` | AD-01C §6's open items | AD-01C | `T-12` | **Not extended** |
| `U-1 … U-20` | AD-01E §12's open items | AD-01E | `U-20` | **Not extended** |
| `C-1 … C-11`, `V-1 … V-26`, `F-1 … F-11` | AD-01F's contradictions, open items, findings | AD-01F | `C-11`, `V-26`, `F-11` | **Not extended — see the collision note** |
| `G-1 … G-12`, `W-1 … W-5` | AD-01G's findings and open items | AD-01G | `G-12`, `W-5` | **Not extended** |
| `H-1 … H-12`, `Y-1 … Y-5` | AD-01H's findings and open items | AD-01H | `H-12`, `Y-5` | **Not extended** |
| `I-1 … I-14`, `Z-1 … Z-6` | AD-01I's findings and open items | AD-01I | `I-14`, `Z-6` | **Not extended** |
| `AC-n` | The **shared** cross-domain business-decision register | AD-01M onward | ⚠ **`AC-48`**, verified by grep over `03ab` | ⚠ **`AC-49` minted** |
| `X-n` (CP sense) | The **shared** contradiction register | AD-01W onward | ⚠ **`X-34`**, verified | ⚠ **`X-35` minted; `X-28` ENLARGED, NOT RENUMBERED** |
| `A-n` | The **shared** *amendment named and not made* register | AD-01H `A-1` → AD-01AB `A-80` | ⚠ **`A-80`**, verified | ⚠ **`A-81`, `A-82`, `A-83` minted, NONE made** |
| `C-` roman | The shared architect-constraint register | AD-01J onward | `C-XXV` | **Not extended — the necessity test fails** |
| `PF-n` | The shared cross-document finding series | AD-01AA/AB | ⚠ **`PF-318`**, verified | ⚠ **`PF-319 … PF-358`** |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-319` — TWO NAMESPACE COLLISIONS ARE DOCUMENTED AND NEITHER IS FIXED, on
> AD-01I `I-14`'s and AD-01J `J-16`'s own precedent (*flag, do not renumber*).**
>
> 1. ⚠ **`X-n` NAMES TWO DIFFERENT THINGS.** AD-01B §2.2 uses `X-1 … X-7` for **dependency edges between
>    open questions**. The CP chain uses `X-24 … X-34` for **contradictions**. **`X-5` is therefore both a
>    dependency edge (`Q3 ← N-4`) and, per AD-01AA §0, a contradiction *"DISCHARGED at AD-01R"*.** The
>    ranges do not overlap numerically today — AD-01B stops at `X-7` and the CP contradiction series is
>    recorded from `X-24` — **but the gap `X-8 … X-23` is unaccounted for by either series and a future
>    document minting into it would collide silently.** **`X-35` is minted in the CONTRADICTION sense and
>    is labelled as such at every mention.**
> 2. ⚠ **`A-n` NAMES TWO DIFFERENT THINGS.** AD-01F §3 uses `A-1 … A-14` for **current architecture
>    assumptions under test**. AD-01H mints `A-1` for the **first amendment named and not made**, AD-01I
>    continues it at `A-2`, and the CP chain has carried it to `A-80`. **AD-01F `A-11` and the amendment
>    `A-11` are different objects.** **`A-81 … A-83` are minted in the AMENDMENT sense.**
>
> **Neither collision is repaired, because repairing it would edit prior documents, which no document in
> this series does. Both are recorded so a reader does not resolve them by guessing.**

### Predecessors

`00-phase-0-architecture-note.md` · `01-bmexa-architecture-reconciliation.md` ·
`02-repository-governance-agent-rules-report.md` · **AD-01** (`03`) · **AD-01A** (`03a`) · **AD-01B**
(`03b`) · **AD-01C** (`03c`) · **AD-01E** (`03e`) · **AD-01F** (`03f`) · **AD-01G** (`03g`) · **AD-01H**
(`03h`) · **AD-01I** (`03i`) · **AD-01J** (`03j`) · **AD-01K** (`03k`) · **AD-01L** (`03l`) · **AD-01M**
(`03m`) · **AD-01N** (`03n`) · **AD-01O** (`03o`) · **AD-01P** (`03p`) · **AD-01Q** (`03q`) · **AD-01R**
(`03r`) · **AD-01S** (`03s`) · **AD-01T** (`03t`) · **AD-01U** (`03u`) · **AD-01V** (`03v`) · **AD-01W**
(`03w`) · **AD-01X** (`03x`) · **AD-01Y** (`03y`) · **AD-01Z** (`03z`) · **AD-01AA** (`03aa`) · **AD-01AB**
(`03ab`).

> ⟦ARCHITECT ANALYSIS⟧ **There is no `03d`. The series skipped the letter. Recorded so its absence is not
> read as a missing document.**

| | |
|---|---|
| **Document ID** | AD-01AC |
| **Type** | Full adversarial reconciliation of the foundational `Lead` / `Client` / `Inquiry` / `Assignment` / `Dump` domain against the accumulated Product-Owner ledger, cross-checked against the CP attribution / commission chain. **Not an amendment to any prior document. NO FILE FROM `00` THROUGH `03ab` IS EDITED BY THIS DOCUMENT.** |
| **Sources read in full** | `03`, `03a`, `03b`, `03c`, `03e`, `03f`, `03g`, `03h`, `03i` — the nine foundational documents, which are this audit's actual subject. |
| **Sources read for the cross-domain check** | `03z`, `03aa`, `03ab` in their restatement, ledger-audit and four-lists sections, **treated as authoritative restatements of the currently-locked CP rules per this series' own re-verification discipline**, plus targeted reads at source in `03j`, `03k`, `03l`, `03m`, `03n`, `03p`, `03w`, `03x`, `03y` wherever a question required the ORIGINAL wording of a rule rather than its restatement. |
| **Contains SQL / schema / migration / code / UI / workflow design** | ⚠ **NO. Deliberately, and `U` of the owner's enumeration requires it.** No table, column, type, master value, index, endpoint, screen, control, state-machine implementation or migration appears anywhere below, and none may be derived from anything below. |
| **Constraints honoured** | ⟦SOURCE⟧ Spec **Rule 1** (do not invent requirements — silence is recorded, never filled) · **§88** (canonical entities, relationships, booking lifecycle, financial rules, CP commission logic, tenant architecture, RLS, authorization, authentication, offline business behavior, source-of-truth rules and audit requirements are **MUST ASK BEFORE DECIDING**) · **§97** (when in doubt, stop and ask) · `ENGINEERING_RULES.md` **R1**, **R2**, **R4**, **R5**, **R6**, **R12**. |
| **Explicitly NOT performed** | ⚠ **The comprehensive Audit Completeness Gate. It remains DEFERRED by the Product Owner to the end of the entire architecture phase. [§8](#8-timeline-reconciliation) classifies timeline events and does NOT open the Gate, not even partially.** |

### The owner's twenty-one lettered investigations, mapped to the twenty required output sections

| Letter | Subject | Discharged at |
|---|---|---|
| **A** | Person vs Lead/Client-Sales-Record vs Inquiry — cardinality, identity and commercial boundaries | [§2](#2-person--lead--inquiry-boundaries) |
| **B** | The exact Inquiry-creation boundary; new Inquiry vs follow-up | [§3](#3-inquiry-lifecycle) |
| **C** | `Dump` semantics from first principles | [§4](#4-dump-semantics) |
| **D** | Eight reactivation scenarios traced end to end | [§5](#5-reactivation) |
| **E** | The Assignment / custody model across ten scenarios | [§6](#6-assignment-and-custody) |
| **F** | History visibility across eleven actor/scenario types; `W-3` | [§7](#7-history-visibility) |
| **G** | *Transfer with history* vs *without history* — the mechanism | [§7.5](#75-transfer-with-history-versus-without-history) |
| **H** | First-Response / FUT boundary testing across eleven event types | [§9](#9-first-response-and-fut) |
| **I** | Full timeline-event reconciliation | [§8](#8-timeline-reconciliation) |
| **J** | Today / Future / Overdue reconfirmed as derived | [§10](#10-derived-action-buckets) |
| **K** | Lead / Inquiry / CP-attribution cross-domain reconciliation | [§11](#11-cp-attribution-interaction) |
| **L** | Duplicate / clash audit | [§12](#12-duplicate-and-clash) |
| **M** | Manager redistribution across ten scenarios | [§13](#13-manager-redistribution) |
| **N** | Reporting semantics across thirteen named metrics | [§14](#14-reporting-semantics) |
| **O** | Authorization / ownership across thirteen named operations | [§15](#15-security-and-authorization) |
| **P** | Offline / concurrency across ten scenarios | [§16](#16-offline-and-concurrency) |
| **Q** | The independent-axes verification | [§17](#17-state-machine-reconciliation--the-independent-axes) |
| **R** | Independent, non-trusting contradiction hunt across fifteen topic areas | [§18](#18-contradictions) |
| **S** | `AC-6` / `AC-26` / `AC-42` / `AC-47` / `AC-48` re-check | [§19.3](#193-ac-6-ac-26-ac-42-ac-47-and-ac-48-re-checked) |
| **T** | Five-way-taxonomy ledger re-audit | [§19](#19-product-owner-ledger-changes) |
| **U** | No implementation of any kind | **The metadata table above, and [§20.5](#205-what-this-document-does-not-authorise)** |

---

## 1. Executive findings

### 1.1 The method, stated before the results

> ⟦ARCHITECT ANALYSIS⟧ **`PF-320` — three disciplines govern everything below, and each one was chosen
> because the corpus itself has a precedent for the failure it prevents.**
>
> 1. **Every foundational item is read AT THE DOCUMENT WHERE IT WAS MINTED, never at the document that
>    most recently mentioned it.** This is AD-01Z's and AD-01AA's rule, applied for the first time to the
>    `03`–`03i` range. It is what produced `X-35`: `V-1`'s status at AD-01G is not `V-1`'s status at
>    AD-01L, and only a read at the mint shows it.
> 2. **A prior document's confirmation of a position is not evidence for the position.** AD-01F §5.1
>    declined to give AD-01E's episode the benefit of incumbency; the same refusal is applied here to
>    `G-1`, `H-4` and `I-1`, all three of which this document tests rather than assumes.
> 3. **Where a prior document is CONFIRMED, that is recorded as deliberately as where it is corrected.**
>    AD-01F `C-11`'s standard. A reconciliation that reports only conflicts is not a reconciliation, and
>    [§18.4](#184-confirmations-recorded-because-a-reconciliation-that-reports-only-conflicts-is-not-one)
>    is where the confirmations go.

### 1.2 What the audit found, in one table

| # | Finding | Kind |
|---|---|---|
| **1** | ⚠ `V-1`, `V-2`/`V-3`, `V-11`/`V-12` are recorded as **approved** by AD-01L and AD-01P and as **not approved** by AD-01G, AD-01H and AD-01I | ⚠ **`CONTRADICTED` — `X-35`** |
| **2** | `X-28`'s formulation originates at AD-01 `E-13`, not at AD-01H §8.2; eight further instances exist in four unsurveyed documents | ⚠ **`CONTRADICTED` — `X-28` ENLARGED** |
| **3** | `W-3` is genuinely open, untouched by twenty-one subsequent documents, and reachable by accident through the bulk-transfer path | **`VALIDATE-OPEN`, enlarged** |
| **4** | The original AD-01 machine coupled five axes; two un-couplings are PO decisions, three are unapproved architect findings | **Mixed — see [§17](#17-state-machine-reconciliation--the-independent-axes)** |
| **5** | `Dump` has **not** become a universal terminal state; the CP chain adopted the Inquiry-level reading correctly | **CONFIRMED** |
| **6** | A money rule (`PO-R1`/`AC-14`) is locked over Dimension A, whose values (`N-4`) do not exist and whose scope (`Z-5`) is undecided | **`VALIDATE-OPEN`, coupling named** |
| **7** | Starting-point statement 9 (*New is derived*) contradicts a recorded PO decision that has never been amended | ⚠ **`CONTRADICTED`** |
| **8** | The `Assignment Log` is asked to carry three unapproved, unrecoverable-if-unrecorded facts | **`ARCHITECT DERIVED`, incomplete** |
| **9** | `V-4` — *what counts as a qualifying follow-up* — gates `New`, FUT, every first-response variant, `W-1` and `W-4`, and has been named *the cheapest unblocking question* for nineteen consecutive documents | **`VALIDATE-OPEN`** |
| **10** | `Y-1` (concurrent liveness) is the one question that decides whether the record's single next-action commitment survives, and it is still unanswered | **`VALIDATE-OPEN`** |

### 1.3 What moved, and what did not

| Category | Result |
|---|---|
| **Product-Owner decisions closed by this document** | ⚠ **ZERO.** No item in any series is closed here. |
| **Product-Owner decisions minted** | ⚠ **ONE — `AC-49`**, and it is this document's single closing question. |
| **Contradictions minted** | ⚠ **ONE — `X-35`.** |
| **Contradictions ENLARGED without renumbering** | ⚠ **ONE — `X-28`.** |
| **Amendments named** | ⚠ **THREE — `A-81`, `A-82`, `A-83`. ZERO made. The register becomes `A-1 … A-83`.** |
| **Constraints minted** | ⚠ **ZERO. The `C-` series stays at `C-XXV`** — the necessity test is run at [§19.5](#195-the-constraint-candidate-tested-and-not-minted) and it fails. |
| **Prior documents edited** | ⚠ **ZERO.** |
| **Business rules invented** | ⚠ **ZERO.** Every gap is recorded as `VALIDATE-OPEN`. |
| **Reporting denominators proposed** | ⚠ **ZERO** — [§14](#14-reporting-semantics). |
| **SLA targets proposed** | ⚠ **ZERO** — [§9.5](#95-no-sla-is-proposed). |
| **Conflict-resolution policies proposed** | ⚠ **ZERO** — [§16](#16-offline-and-concurrency). |

### 1.4 The one thing that must be said before the detail

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-321` — THE FOUNDATIONAL DOMAIN IS IN A WORSE EPISTEMIC POSITION THAN THE CP
> DOMAIN, AND THE ASYMMETRY IS THE REPORT.**
>
> **The CP chain has `PO-N1 … PO-AB5` — roughly a hundred clauses the Product Owner has stated in his own
> words and the architect has recorded verbatim before analysis.** ⚠ **The foundational domain has
> `AD-01A §8` (five decisions, 2026-09-12), `AD-01C §1.2` (one clarification), `AD-01E §1.1` (one
> direction), and a set of PO *evidence* and *provisional preferences* which AD-01G §1.4, AD-01F §1.2 and
> AD-01I §1.4 each explicitly and correctly declined to treat as authority.**
>
> **Everything else in `03f` through `03i` — the Lead/Inquiry split, the Inquiry as the commercial unit,
> Model D, the custody boundary, the `Dump` decomposition, the work mandate — is `ARCHITECT DERIVED` and
> unapproved.** **That is not a criticism of those documents: each says so, in terms, in its own closing
> note.** ⚠ **It becomes a problem only at the seam, where eighteen money-bearing documents were built on
> top of it and two of them wrote down that it had been approved. That is `X-35`, and it is the reason
> `AC-49` is asked rather than recorded.**

### 1.5 The thirteen starting-point statements, audited individually

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-322` — these were supplied as STARTING POINTS FOR AUDIT, NOT as
> pre-confirmed facts, and they are treated that way. Each is checked against the document that minted it.
> Where a statement is accurate, that is said. Where it is not, the discrepancy is CLASSIFIED and
> NEITHER silently retained NOR silently corrected.**

| # | Statement as supplied | Verdict | Discrepancy, classified |
|---|---|---|---|
| **1** | **Person / Client** — Person is identity; a Lead is not a status on Person | ⚠ **ACCURATE AND `PO LOCKED`** | **None.** ⟦SOURCE⟧ Spec §07 states it directly (*"Person = identity. Sales Lead = sales relationship/process. Customer = contextual role"*) and §06 forbids the collapse. This is the ONE statement of the thirteen that rests on the spec rather than on an architect derivation. |
| **2** | **Lead / Client Sales Record** — the long-lived, Person-bounded work record | ⚠ **ACCURATE IN CONTENT; MIS-STATED IN STATUS** | ⚠ **`ARCHITECT DERIVED`, not `PO LOCKED`.** AD-01G `G-1` is an ⟦ARCHITECT RECOMMENDATION⟧ under `STATUS: PROPOSED — NOT APPROVED`; AD-01G §1.4 records the owner's preference for it and then *"deliberately set[s] it aside."* **See `X-35`.** |
| **3** | **Inquiry / Project Interest** — the commercial unit | ⚠ **MIS-STATED — IT CONFLATES TWO OBJECTS THE REGISTER KEEPS APART** | ⚠ **`Y-3` asks whether a *Project Interest* IS an Inquiry or an AGGREGATE over one-or-more Inquiries on one project, and it is OPEN.** AD-01F §5.3 raised it; AD-01H `Y-3` registered it; nothing has answered it. **Writing the two as one name presumes `Y-3`'s answer.** Status is also `ARCHITECT DERIVED`, per statement 2. |
| **4** | **Assignment** — derived from Owner + Handler + Assignment Log, not a persisted lifecycle state | ⚠ **ACCURATE AND `PO LOCKED`; MATERIALLY INCOMPLETE** | ⚠ **AD-01A §8.5 is a recorded PO decision and the statement is exactly right about it.** **What it omits: `G-9` adds two facts to the Log and `I-8`/`A-2` adds a third, none approved, each *"unrecoverable if not recorded at the moment of the act."*** — [§6.3](#63-the-three-unapproved-facts-the-log-is-asked-to-carry). |
| **5** | **Dump** — an Inquiry-level disposition, not necessarily permanent, not necessarily terminal for the long-lived client relationship | ⚠ **MIS-STATED — IT CONFLATES THE TWO LEVELS THE DECOMPOSITION EXISTS TO SEPARATE** | ⚠ **At the INQUIRY level `Dump` IS permanent: `H-4` makes the commercial disposition *monotone and terminal-once*, AD-01A §8.3 preserves the reason, `R6` forbids the edit.** **At the RECORD level it is a derived, custody-scoped, non-terminal work condition.** *"Not necessarily permanent"* is true of the second and **false of the first**. **And `H-6(c)`'s *"must not be terminal"* was itself found OVER-GENERALISED by AD-01I §3.4 (`I-10`), with `Z-2` open.** |
| **6** | **Reactivation** — a new Inquiry, not necessarily a new Person/Lead record | ⚠ **ACCURATE, AND WEAKER THAN THE CORPUS** | ⚠ **`J-2` makes it FORCED, not optional: a customer-generated expression of interest opens a new Inquiry if and only if no non-terminal Inquiry covers it, and it opens on the EXISTING work record — never a new one for the same Person, subject only to `M-5`.** *"Not necessarily"* understates a structural consequence as a preference. |
| **7** | **History visibility** — authorization-based, with `W-3` deny-capability still open | ⚠ **ACCURATE, AND `W-3` IS CONFIRMED GENUINELY OPEN** | **None in content.** ⚠ **One addition: `W-3` is not merely undescribed — it is reachable by accident through bulk transfer.** — [§7.4](#74-w-3--re-confirmed-open-and-enlarged). |
| **8** | **Today / Future / Overdue** — derived UI buckets, never persisted | ⚠ **ACCURATE AND `PO LOCKED`** | **None.** AD-01A §8.1 decides it; consolidated §11 and §27 confirm it; AD-01F `C-11` records the owner's own retirement of the word *Pending* in favour of *Overdue*. ⚠ **One adjacent caveat: `Blocked` is NOT timing-derived (AD-01C §1.2, PO clarification) and AD-01A §8.6's phrasing is inaccurate for it — `T-12`, open.** |
| **9** | **"New"** — derived, not a lifecycle state | ⚠ **`CONTRADICTED` BY A RECORDED PRODUCT-OWNER DECISION** | ⚠ **AD-01A §8.1 — PO-decided, 2026-09-12 — states the lifecycle is four values *New / Follow-up / Success / Dump*. The derivation is AD-01F `V-5`/`V-6` and AD-01H `H-5`, and the amendment they require is `A-1`, NAMED AND NOT MADE.** **Presenting the derived reading as the current model states an amendment as though it had been accepted.** |
| **10** | **First Response** — the first user-performed action; system events excluded | ⚠ **MIS-STATED BY OMISSION** | ⚠ **⟦SOURCE⟧ consolidated §9 says *"first QUALIFYING user follow-up"* and never defines *qualifying* — that is `V-4`, open, and the observed operation's separate `Attempted` bucket is direct evidence the boundary is real.** **The clock is also undecided: creation→, assignment→, custody→ (`V-20`) and inquiry→ (`W-4`) are four variants and none is selected.** |
| **11** | **FUT** — total user-performed follow-up count; system events excluded | ⚠ **MIS-STATED BY OMISSION, TWICE** | ⚠ **Same `V-4` gap as statement 10 — and AD-01G `W-1` adds a second: one call covering two projects may be one activity referencing two Inquiries, one referencing a primary, or two activities. FUT's COUNT is undefined until `W-1` is answered.** |
| **12** | **Client-centric multi-inquiry workflow** | ⚠ **ACCURATE FOR EXISTENCE; OVER-STATED IF READ AS LIVENESS** | ⚠ **`H-1` establishes concurrent EXISTENCE with at most one non-terminal. Concurrent LIVENESS — two Inquiries non-terminal at one instant — is `Y-1`, OPEN, and `Y-2` (whether the record still holds exactly ONE next-action commitment) is open with it.** |
| **13** | **Redistributed dumped record may transfer with or without historical visibility** | ⚠ **ACCURATE; RESTING ON FOUR OPEN ITEMS** | ⚠ **`V-8` (mandatory? default? revocable? bulk?), `Z-6` (uniform or per-record intent in a bulk act), `W-3` (the eighth case) and `I-8`/`A-2` (the work mandate itself, unapproved) all sit underneath it.** AD-01I §9.2 records that redistribution is *"the assignment kind where the default does the most damage either way."* |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-323` — THE PATTERN IN THE THIRTEEN IS THE FINDING, AND IT IS NOT THAT THEY
> ARE WRONG.** **Eleven of the thirteen are accurate as descriptions of where the architecture landed.
> What ten of them get wrong is **STATUS** — they read as the settled model, and all but statements 1, 4
> and 8 are unapproved architect derivations. **Statement 9 goes further and states, as current, a
> proposition a recorded Product-Owner decision denies.** ⚠ **A conceptual model summary that does not
> carry status is how an unapproved substrate becomes load-bearing without anyone approving it — which is
> `X-35` observed from a second direction.**

---

## 2. Person / Lead / Inquiry boundaries

> ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `A`. Cardinality, identity and the commercial boundary,
> tested from the sources rather than from the summary.**

### 2.1 What the spec itself fixes, and it is less than the summary assumes

| # | ⟦SOURCE⟧ | What it fixes | What it leaves open |
|---|---|---|---|
| **1** | Spec **§06** | *"**Sales Lead** — A relationship between a Person, Project, and sales process. A Lead is NOT merely a status value on Person. The Sales Lead relationship must exist as its own business record."* | ⚠ **Whether anything may sit BETWEEN Person and that relationship. §06 neither contemplates nor forbids it** — AD-01G `G-2`. |
| **2** | Spec **§07** | *"Person = identity. Sales Lead = sales relationship/process. Customer = contextual role associated with a transaction… A Person can have multiple leads, interact with multiple projects… **Do not destroy historical identity when status changes.**"* | ⚠ **It illustrates multiplicity across CONTEXTS. It does not say two records may exist for one Person in one tenant, which is `M-5`'s uniqueness half, open.** |
| **3** | Consolidated **§4**, **§7**, **§13** | *"client-centric working record"*; *"a client may show interest in multiple projects over time"*; *"more than one project interest on the same record"*; *"The client record can contain customer details and multiple project interests."* | ⚠ **Whether interests may be CONCURRENT (`Y-1`) and whether *Project Interest* is an Inquiry or an aggregate (`Y-3`).** |
| **4** | Consolidated **§28** | ⟦PRODUCT-OWNER DECISION⟧ *"Exact definition and reporting treatment of multiple project interests on one Lead"* and *"the exact boundary between an additional project interest on the same Lead and a genuinely new sales opportunity"* — listed **OPEN in the owner's own words.** | — |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-324` — THE `C-1` CONTRADICTION IS NOT RESOLVED BY ANY RECORDED
> PRODUCT-OWNER DECISION, AND THIS IS THE AUDIT'S FIRST INDEPENDENT RESULT.** **AD-01F `C-1` set §06
> against consolidated §4/§7/§13 and called it *"the root of the whole line."* **AD-01G recommended
> `G-1`/`G-2` — the two-entity answer, with §06 EXTENDED rather than superseded.** ⚠ **A fresh search of
> the whole corpus for a Product-Owner statement RESOLVING `C-1` returns nothing. `PO-AB5`'s twelve
> restatements do not reach it. AD-01L's *"Approved"* label is the closest thing to one and it is an
> ARCHITECT's label on an ARCHITECT's recommendation, which is `X-35`.**

### 2.2 The cardinality table, stated because every downstream section reads it

| Relationship | Cardinality as the corpus currently has it | Status | Open item that could move it |
|---|---|---|---|
| `Person` → work record | **1 → 0..1 within a tenant**, on the recommendation | `ARCHITECT DERIVED` | ⚠ **`M-5`'s uniqueness half — OPEN. Nothing states that two working records may not exist for one Person, and AD-01G §5.6 records the silence rather than filling it.** |
| work record → `Inquiry` | **1 → 0..n** | `ARCHITECT DERIVED` | ⚠ **AD-01G §5.6: whether a record may exist with ZERO Inquiries is SILENT. §42's fast-capture case suggests yes; nothing states it. Scenario D of AD-01I §5.5 makes it operationally necessary.** |
| work record → non-terminal `Inquiry` | **1 → 0..1 (concurrent existence, at most one live)** | `ARCHITECT DERIVED` | ⚠ **`Y-1` — OPEN. If `Y-1` is *yes*, this becomes 0..n and `Y-2` decides the next-action commitment's cardinality.** |
| `Inquiry` → `Project` | **n → 1, definitional** | `ARCHITECT DERIVED` (AD-01G §5.1, *"Settled"*) | ⚠ **`Y-3` — whether two Inquiries on ONE project may be live at once.** |
| `Inquiry` → `Booking` | **1 → 0..1 reference on conversion** | `ARCHITECT DERIVED` (`H-8`) | ⚠ **`AB-1` — AD-01K `K-41` found `H-8` UNDERSPECIFIED for a multi-applicant Booking Group. OPEN.** |
| `Person` → `Customer` | **A CONTEXT, never a record** | ⚠ **`PO LOCKED`** — ⟦SOURCE⟧ §06/§07 | ⚠ **`Y-4` — whether a Success on one Inquiry changes the whole record's queue behaviour. OPEN.** |
| work record → `Custody Interval` | **1 → 1..n, DERIVED over the Assignment Log** | `ARCHITECT DERIVED` (`G-9`) | — |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-325` — ONE CARDINALITY IS ASSERTED NOWHERE AND ASSUMED EVERYWHERE:
> `Inquiry` → `Booking` AS 1 → 0..1.** **AD-01H `H-8` gives the Inquiry a reference to the Booking.
> AD-01K `K-41` found the MULTI-APPLICANT case underspecified and minted `AB-1`.** ⚠ **Neither addresses
> the INVERSE multiplicity: one Inquiry under which a client books TWO UNITS. §26's unit-transfer rule
> and §16's inventory holds both presuppose a unit-level object, and the CP chain's Stage-2 allocation,
> Broker/Sub-Broker category and DIRECT mark are all **per-Booking** (`PO-Y1`, `PO-Z1`, `PO-AA3`).** **If
> one Inquiry can carry two Bookings, one Inquiry carries two Stage-2 decisions and possibly two different
> Brokers — and nothing in either domain says whether that is legal.** ⚠ **THIS IS NOT MINTED AS A NEW
> ITEM. It is `AC-43`'s neighbourhood (*may a Booking carry more than one Sub-Broker*) inverted, and it
> lands inside `AB-1`'s subject — the Booking-Group entitlement question — which is OPEN. **Recorded as an
> ENLARGEMENT of `AB-1` and NOT as a new decision, on AD-01AB §18.2's own anti-promotion standard.**

### 2.3 The identity / commercial boundary, stated as the separation test resolves it

> ⟦ARCHITECT ANALYSIS⟧ **AD-01 §3's test — *two concepts belong on one axis only if a record can never
> legitimately be in both at once* — applied to the three candidate identity objects.**

| Pair | Can they legitimately differ at one instant? | Verdict |
|---|---|---|
| `Person` vs work record | **Yes.** ⟦SOURCE⟧ §07's *Customer* context: one Person is simultaneously a converted customer on one transaction and a live prospect on another. A Person also exists before any sales relationship (§42 helpdesk capture names a human before it names an opportunity). | **Separate. `PO LOCKED` by §06/§07.** |
| work record vs `Inquiry` | **Yes, and AD-01H Example 3 proves it structurally:** Project A closed as *Not Interested* with a preserved reason, Project B live. **One record-level disposition slot, two closures to record. AD-01A §8.3 forbids the overwrite outright.** | **Separate. `ARCHITECT DERIVED` (`H-1`, `H-4`).** |
| `Inquiry` vs `Booking` | **Yes.** ⟦PRODUCT-OWNER DECISION⟧ AD-01A §8.2: *"Booking has its own lifecycle/state machine. The Lead Lifecycle must not absorb Booking states."* A cancelled Booking leaves the Inquiry's conversion historically true. | ⚠ **Separate. `PO LOCKED`.** |
| `Person` vs `Inquiry` | **Yes** — and this is the pair the CP chain depends on. `K-18` attaches the claim to the Inquiry; AD-01J `J-24` and AD-01K eliminated the Person anchor **on capability grounds**, not on preference. | **Separate. `ARCHITECT DERIVED`.** |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-326` — THE SEPARATION SURVIVES THE AUDIT; THE QUESTION IS WHOSE IT IS.**
> **All four separations hold on their merits and none was found defective. TWO of the four are fixed by
> the spec or by a recorded PO decision. TWO are architect derivations that eighteen documents treat as
> fixed.** **That distribution is `AC-49`'s whole subject.**

### 2.4 What the sources still do not define about the work record

> ⟦ARCHITECT ANALYSIS⟧ **Recorded per Spec Rule 1 rather than filled. Each is a SILENCE, and a recorded
> absence is not a proved negative — AD-01I §3.4's own standard.**

| # | Undefined | Where the silence is recorded | `PF` |
|---|---|---|---|
| **1** | May a work record exist with **zero** Inquiries? | AD-01G §5.6 | `PF-327` |
| **2** | May **two** work records exist for one Person in one tenant, and what reconciles them? | AD-01G §5.6; `M-5` | `PF-328` |
| **3** | Does the work record have **any terminal condition at all**? | AD-01G §4.1 (*"Terminality — has none"*, a statement of SILENCE) — ⚠ **and AD-01H `H-6(c)` converted that silence into a PROHIBITION, which AD-01I §3.4 found over-generalised (`I-10`, `Z-2`)** | `PF-329` |
| **4** | Does a Person who becomes a **Customer** retain a working record for future interest? | AD-01G §5.6; `Y-4` | `PF-330` |
| **5** | Is the work record **tenant-unique on the Person**, or may a project-scoped operation hold its own? | `M-5`; `V-24` | `PF-331` |

---

## 3. Inquiry lifecycle

> ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `B`. The exact Inquiry-creation boundary, and what
> distinguishes a new Inquiry from a follow-up on an existing one.**

### 3.1 The rule as the corpus has it, restated at its mint

> ⟦SETTLED — AD-01J `J-2`, `ARCHITECT DERIVED`, NOT APPROVED⟧ **A customer-generated expression of
> interest opens a NEW Inquiry if and only if no non-terminal Inquiry on that work record already covers
> it. If a non-terminal Inquiry covers it, the expression is an ACTIVITY on that Inquiry, never a second
> Inquiry.**

> ⟦ARCHITECT ANALYSIS⟧ **`PF-332` — THE RULE IS DECIDABLE AT CAPTURE TIME AND ITS PREDICATE IS NOT.**
> **AD-01J §2.3 records the gap in terms: the rule turns on whether a live Inquiry *"covers"* the arriving
> interest, and ⟦SOURCE⟧ **the sources do not say whether unit type / configuration is inside or outside
> an Inquiry's boundary.** **Case (1a) — *"actually, a 3BHK"* — is one Inquiry whose requirement profile
> evolves. Case (1b) — a genuinely different product — is two.** ⚠ **AD-01J minted this as the
> requirement-profile question and it is OPEN. It is ALSO `V-3`'s second clause, which AD-01J `A-5` records
> AD-01H as having reported RESOLVED when only clause 1 was addressed.** **`A-5` is NAMED AND NOT MADE,
> and this audit confirms it independently: AD-01G §5.1 marks the requirement-profile row *"V-3 — not
> resolved here"*, and AD-01H §13/§14 record `V-3` as resolved without reaching it.**

### 3.2 The four creation paths, and what each settles

| Path | Does it open an Inquiry? | Basis | Status |
|---|---|---|---|
| **Customer expresses interest in a project with no live Inquiry** | **Yes, forced** | `J-2`; ⟦SOURCE⟧ consolidated §13 (*"record the new inquiry event rather than silently overwriting the previous interest"*) | `ARCHITECT DERIVED` |
| **Customer expresses interest in a DIFFERENT project** | **Yes, necessarily** | Project is definitional to the Inquiry (AD-01G §5.1); ⟦SOURCE⟧ consolidated §7/§13 | `ARCHITECT DERIVED` |
| **Customer re-engages after a terminal closure on the same project** | **Yes** — the closed Inquiry cannot be reopened (`H-4` terminal-once; AD-01A §8.3 preserved reason; `R6`) | `J-2`, AD-01J Case 3 | `ARCHITECT DERIVED` ⚠ **Whether it is a NEW COMMERCIAL OPPORTUNITY is `V-14`'s attribution half = `T-4`, OPEN** |
| **Management redistributes a record for another calling attempt** | ⚠ **NO — NEVER** | `I-6`; `I-7`. **A management act has no source, no channel, no project and no claimant; it is not a customer event** | `ARCHITECT DERIVED`, and **structurally enforced**: AD-01J `J-16`/`J-42` find a management act has *nowhere to put a commercial fact* |

### 3.3 The Inquiry's own lifecycle, audited against the original machine

| Property | As `H-4` has it | Traced back to | Audit result |
|---|---|---|---|
| **Monotone** | An Inquiry's commercial disposition never moves backward | AD-01C §2.5 and §3.7 removed `Dump → Follow-up` and `Success → Follow-up` from the legal set **at the LEAD level**; `H-4` relocated the axis to the Inquiry and the property came with it | ⚠ **CONFIRMED, and the relocation is what made the property ENFORCEABLE rather than policed.** AD-01H `H-8`: *"at the inquiry level Q6's terminality is true without exception and without enforcement pressure"* |
| **Terminal-once** | Exactly one terminal transition per Inquiry | `H-4` | **CONFIRMED.** It is what makes `T-4` and `T-5` well-posed (AD-01K) |
| **Reason mandatory on non-conversion** | Yes, on AD-01A §8.3's three dimensions | ⚠ **`PO LOCKED`** | **CONFIRMED.** ⚠ **Values remain `N-4`, OPEN — the twenty-second consecutive document to record it** |
| **Reason preserved, never rewritten** | Yes | ⚠ **`PO LOCKED`** (AD-01A §8.3) + `R6` | **CONFIRMED, and load-bearing far beyond its design purpose**: `K-22`, `PO-R1`/`AC-14` and `J-10` all read it |
| **Conversion** | References the Booking; the §20 Stage 3 milestone belongs to the Booking | ⚠ **`PO LOCKED`** (AD-01A §8.2) + `H-8` | **CONFIRMED.** ⚠ **Who or what SETS it is `V-22`/`T-6`, OPEN** |
| **May two Inquiries be live at once?** | At most one non-terminal | `H-1` | ⚠ **`Y-1`, OPEN. The corpus's own headline scenario depends on it (AD-01K §17 item 4)** |

### 3.4 The boundary question the owner's investigation `B` actually poses

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-333` — *"NEW OPPORTUNITY = NEW INQUIRY"* IS A TRUE STATEMENT THAT DOES
> NOT SAY WHAT IT APPEARS TO SAY, AND CONFLATING ITS TWO HALVES IS THE SINGLE EASIEST ERROR IN THIS
> DOMAIN.** **AD-01G `G-5` states it and AD-01J §3 decomposes it:**
>
> | Half | Question | Status |
> |---|---|---|
> | **Structural** | Does a returning interest produce a **new Inquiry record**? | ⚠ **FORCED AND ANSWERED: YES (`J-2`). It requires no business decision.** |
> | **Accounting** | Does that Inquiry enter the period's **Capture count and conversion denominator**? | ⚠ **FORCED AND ANSWERED: YES (`V-18`'s commercial half, `J-19`).** |
> | **Commercial** | Does the **prior producer retain any claim** over it? | ⚠ **`BUSINESS DECISION REQUIRED` — this is `T-4` exactly, and it is the ONLY part that is a policy about money. OPEN.** |
>
> **The sentence *"a new opportunity is a new Inquiry"* is therefore TRUE structurally, TRUE for counting,
> and SILENT on the only thing anyone is paid for.** ⚠ **A summary that carries the sentence without the
> decomposition will be read as having settled `T-4`, which is `AC-49`'s failure mode observed at a
> smaller scale.**

---

## 4. Dump semantics

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `C`, and the owner's explicit warning attached to it:
> `Dump` must not become an ACCIDENTAL UNIVERSAL TERMINAL STATE. The audit is run against that warning
> rather than against a general request to describe `Dump`.**

### 4.1 What `Dump` is, from first principles, re-derived rather than carried

> ⟦SOURCE⟧ Spec **§13**: *"**Dump** (closed without needing follow-up)."* ⚠ **AD-01 §2.5 read this
> correctly in the FIRST document of the series and the reading has never been improved on:** *"That is an
> operational statement about the rep's queue, not a commercial statement about the outcome… **Dump and
> Lost are not synonyms.**"*

> ⟦PRODUCT-OWNER DECISION⟧ Consolidated **§11**: *"**Dump** — Terminal/non-active sales disposition at
> lifecycle level, **but real-world usage shows it may also function as workload-management disposition;
> semantics must be kept precise.**"*

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-334` — THE DUALITY IS THE PRODUCT OWNER'S OWN, STATED IN HIS OWN WORDS,
> AND THE ARCHITECTURE SPENT FOUR DOCUMENTS REDISCOVERING IT.** **AD-01 §2.5 diagnosed it; AD-01A §8.3's
> three dimensions equipped it; AD-01F §10.2 decomposed it into D-i / D-ii / D-iii; AD-01H `H-6` placed
> it at two levels; AD-01I `I-1` tested all five candidate concepts and found **three independent axes and
> two derived projections**. ⚠ **No step in that chain contradicts an earlier one. This is the single
> cleanest derivation in the foundational corpus and it is recorded as such.**

### 4.2 `I-1`'s decomposition, re-tested independently against the owner's warning

| Concept | Level | Persisted? | Terminal? | Carries a reason? | Whose act? |
|---|---|---|---|---|---|
| **C1 — commercial disposition** | **Inquiry** | **Yes** | ⚠ **Yes, without exception** | ⚠ **Yes, mandatory** (AD-01A §8.3) | Handler |
| **C2 — queue exit / workload** | **Work record × custody interval** | **No — derived** | ⚠ **No** | ⚠ **No, and must never be asked to** | Handler |
| **C3 — redistribution eligibility** | **The management ACT, on the Assignment Log** | ⚠ **Intent is recorded; eligibility is not an owned value** | n/a | No | ⚠ **Management** — `I-2`, and the SOURCES separate the actors |
| **C4 — re-engagement eligibility** | **Derived** | No | n/a | Reads Dimension C | Policy, triggered by the customer |
| **C5 — "never workable" classification** | **Derived** | No | n/a | Reads Dimension A + C | Handler, at closure |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-335` — THE OWNER'S WARNING IS ANSWERED DIRECTLY: `Dump` HAS NOT BECOME A
> UNIVERSAL TERMINAL STATE, AND THE DECOMPOSITION IS WHY.** **A universal terminal `Dump` would require
> C1 and C2 to be one value. `I-1` separates them on evidence the owner supplied — `R-7`'s 7,000-record
> workload account and `R-8`'s fresher case — and `I-2` shows the SOURCES already separate them BY ACTOR:
> the rep exits the queue, the manager redistributes.** ⚠ **A single concept does not have two owners
> with two authorization surfaces.**

### 4.3 The one place the model is genuinely wrong, carried and not repaired

> ⚠ ⟦SETTLED — AD-01I §3.4, `I-10`⟧ **`H-6(c)`'s *"Terminal? — No, and it must not be"* is
> OVER-GENERALISED.** **AD-01G §4.1's *"Terminality — has none"* is a statement of SILENCE; `H-6` converted
> it into a statement of PROHIBITION on the strength of one class of evidence (non-responding records,
> i.e. Dimension A's *Commercial loss*).** ⚠ **A record whose only Inquiry closed as *Fake Lead*,
> *CRM Test Query* or *Invalid/Wrong Number* is, under `H-6` as written, **eternally redistributable** — a
> manager may hand it to a fresher to call a number that does not exist, for as long as the tenant exists.**

> ⚠ ⟦BUSINESS DECISION REQUIRED — `Z-2`, OPEN, NOT ANSWERED HERE⟧ **Does the opportunity validity class of
> a record's closed Inquiries CONSTRAIN redistribution eligibility, and is the constraint ENFORCED or
> merely SURFACED to the manager?** ⚠ **This audit adds nothing to `Z-2` except confirmation that it is
> still open and that AD-01I's reason for refusing to answer it survives: the reference operation carried
> a SINGLE record-level Dump-Reason column (`R-4`) and was structurally incapable of distinguishing *"this
> engagement failed"* from *"this relationship was never real"* — **its silence is an artefact of its data
> model, not evidence about the business.**

> ⚠ ⟦BUSINESS DECISION REQUIRED — `Z-5`, OPEN, NOT ANSWERED HERE⟧ **Does AD-01A §8.3's framework require a
> fourth dimension — CLOSURE SCOPE, *does this closure speak about this ENGAGEMENT or about this whole
> CLIENT RELATIONSHIP* — or is scope a policy rule read over Dimension A?** ⚠ **NO DIMENSION IS ADDED,
> PROPOSED OR PREFERRED HERE, AND AD-01A §8.3 IS NOT AMENDED BY THIS DOCUMENT.**

### 4.4 Commercial-Inquiry closure versus Lead/work-record queue status — the separation stated in terms

> ⟦ARCHITECT ANALYSIS⟧ **`PF-336` — The owner's investigation `C` asks for this separation EXPLICITLY, so
> it is stated as a table rather than left to be inferred from §4.2.**

| | **Commercial Inquiry closure** | **Work-record queue status** |
|---|---|---|
| **What it asserts** | *This engagement ended without conversion, for this classified reason* | *This record has no live work obligation for its current handler* |
| **Object** | The Inquiry | The work record, scoped to a custody interval |
| **Form** | Persisted, monotone, terminal-once, preserved | Derived at read time |
| **Reversible?** | ⚠ **Never** | ⚠ **Yes — it becomes false again the moment a new custody interval opens with a mandate, or a new Inquiry opens** |
| **Reason** | ⚠ **Mandatory** | ⚠ **None, ever** |
| **Who acts** | The handler | The handler (exit) / management (re-entry) |
| **Reporting role** | The loss denominator's numerator, subject to Dimension A | **Workload only. It must never enter a commercial denominator** |
| **If the two were one value** | ⚠ **Every workload act becomes a fabricated commercial loss, permanently, under `R6`, and AD-01 §9.1's contaminated denominator becomes UNCORRECTABLE** | — |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-337` — THE COST OF THE SEPARATION IS REAL AND IS RESTATED RATHER THAN
> BURIED.** ⟦SOURCE⟧ **§13 wants *"one-tap dispositions"*; §95 answers *"MINIMUM NECESSARY FRICTION, not
> minimum possible taps — a one-tap action that creates financial corruption is worse than a three-tap
> action that prevents it."* **AD-01I §2.4 resolves the tension in favour of precision and DELIBERATELY
> DOES NOT DESIGN THE CONTROL. This document does not design it either — that is UI, and `U` forbids it.**

### 4.5 Does `Dump` affect attribution, registration or future interest?

| Question | Answer | Basis | Status |
|---|---|---|---|
| Does closing an Inquiry extinguish its **recorded claims**? | ⚠ **No. Claims are append-only and never overwritten** | ⟦SOURCE⟧ §11; AD-01 §8.4; `K-18` | `ARCHITECT DERIVED` |
| Does closing an Inquiry let a claim **reach a later Inquiry**? | ⚠ **UNDECIDED — `T-4`. OPEN, and the central money question of the cluster** | AD-01C `T-4`; AD-01J §5; AD-01K; AD-01L | ⚠ **`VALIDATE-OPEN`** |
| Does closing an Inquiry **end a CP registration**? | ⚠ **No** | ⟦PRODUCT-OWNER DECISION⟧ `PO-R1` — `AC-14` Option A: **registration survives an invalid-side closure** | ⚠ **`PO LOCKED`** |
| Does closing an Inquiry **end the client relationship**? | ⚠ **No — and this is the statement the owner's warning protects** | `H-6`; AD-01G §4.1 | `ARCHITECT DERIVED`, ⚠ **with `I-10`/`Z-2`'s exception class open** |
| Does a **record-level** queue exit affect ANY of the above? | ⚠ **No, in every case. It is not a commercial statement** | `I-1` C2; `I-6`; `J-16` | `ARCHITECT DERIVED` |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-338` — THE CP CHAIN DID **NOT** BLUR THE SEPARATION, AND THE NULL RESULT
> IS REPORTED BECAUSE THE OWNER'S INVESTIGATION `C` ASKS FOR IT SPECIFICALLY.** **The check was run at
> source on the two most likely candidates:**
>
> | Candidate | Does it assume `Dump` behaves differently than AD-01I established? | Result |
> |---|---|---|
> | ⚠ **AD-01R / `AC-14` Option A** — *registration survives an invalid-side closure* | ⚠ **NO.** **It operates on the REGISTRATION record, an object the Lead domain does not own, and it says nothing about the work record's queue status or the Inquiry's terminality. Its subject is whether a registration's window keeps running when the Inquiry it was recorded against closes as invalid — a registration-lifecycle question** | ⚠ **NO BLUR** |
> | ⚠ **AD-01L `L-2`** — T-4's subject restated | ⚠ **NO, AND IT IS THE STRONGEST CONFIRMATION AVAILABLE.** `L-2` substitutes *"Dumped lead"* → *"Inquiry with a terminal closure carrying a Q7 classified reason"* **in terms**, citing `H-4`, `V-11` and AD-01A §8.3. **The CP chain adopted the Inquiry-level reading verbatim rather than inheriting the record-level one** | ⚠ **NO BLUR — and an active adoption** |
>
> ⚠ **The hazard the owner named is real in the FOUNDATIONAL direction (`I-10`/`Z-2`) and is NOT realised
> in the CP direction. Saying so is part of the finding.**

---

## 5. Reactivation

> ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `D`: eight named reactivation scenarios, each traced
> through Person, work record, Inquiry, assignment, attribution, registration, timeline and the reporting
> denominator. **No scenario's outcome is decided where the corpus leaves it open; the open item is named
> instead.**

### 5.1 The two routes, and the prohibition on deriving one from the other

> ⟦SETTLED — AD-01I `I-7`, `ARCHITECT DERIVED`, NOT APPROVED⟧ ***"Operationally active" and "commercially
> active" are two different conditions with two different producers, two different lifetimes and two
> different reporting effects. The architecture must carry both, and must never derive one from the
> other.***

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-339` — `I-7` IS THE FOUNDATIONAL DOMAIN'S EQUIVALENT OF THE CP CHAIN'S
> STAGE-1 ≠ STAGE-2 INVARIANT, AND THE STRUCTURAL PARALLEL IS EXACT.** **Both say: two decisions, two
> producers, two levels, and the first does not determine the second.** ⚠ **`PO-W1·1 … ·9` LOCKED the CP
> version; `I-7` is the Lead version and is `ARCHITECT DERIVED`. **The same proposition is locked on one
> side of the seam and unapproved on the other** — which is `X-35` seen from a third direction.**

### 5.2 The eight scenarios, traced

| # | Scenario | Person | Work record | Inquiry | Assignment | Attribution | Registration | Timeline | Reporting denominator |
|---|---|---|---|---|---|---|---|---|---|
| **R1** | **Customer re-contacts about the SAME project after a closure** | Unchanged | Unchanged, not reopened (it never closed) | ⚠ **NEW Inquiry (`J-2`). The closed one stays closed with its reason** | Unchanged unless management acts | ⚠ **New claim set on the new Inquiry. Whether the prior producer reaches it is `T-4`, OPEN** | ⚠ **Unaffected by the Inquiry event; `PO-O`'s revisit/expiry rules govern, and `AC-26` (single-use or repeatable) is OPEN** | `R-14`: the new inquiry event is RECORDED, the prior interest NOT overwritten | ⚠ **Capture: YES (`J-19`/`V-18`'s commercial half). The prior period's loss STANDS (AD-01F §12.4)** |
| **R2** | **Customer inquires about a DIFFERENT project** | Unchanged | Unchanged | ⚠ **NEW Inquiry, necessarily — project is definitional** | Unchanged | New claim set | Registration is per (CP, client, project) — a different project is a different triple | Recorded on the record's own timeline (⟦SOURCE⟧ consolidated §8) | ⚠ **Capture: YES. `Y-1` decides whether both may be LIVE** |
| **R3** | **Customer changes requirement WITHIN a live Inquiry** (*"actually a 3BHK"*) | Unchanged | Unchanged | ⚠ **UNDECIDED — case (1a) is ONE Inquiry with an evolving requirement profile; case (1b) is TWO. `V-3` clause 2, OPEN (`A-5` named, not made)** | Unchanged | ⚠ **Follows the Inquiry count — which is what makes this undecided case money-bearing** | Unaffected | An activity, or an inquiry event, depending on the same undecided answer | ⚠ **Capture: UNDECIDED, for the same reason** |
| **R4** | **Management redistributes a record whose only Inquiry is closed** | Unchanged | ⚠ **Operationally active; NOT commercially live** | ⚠ **NONE opens — never (`I-6`)** | ⚠ **New custody interval; a work MANDATE is recorded on the Assignment Log (`I-8`/`A-2`, unapproved)** | ⚠ **NONE. A management decision is not a producer's claim** | ⚠ **Untouched. `PO-N4`/`PO-O6`: transfers and activity do not move a window** | The redistribution act is a recorded business event | ⚠ **NOT a Capture, NEVER (`I-6`). Workload only** |
| **R5** | **A fresher works a redistributed record for three weeks and gets nowhere** | Unchanged | ⚠ **Still operationally active until the mandate discharges — and `Z-1` (does a mandate END?) is OPEN** | ⚠ **None. Closing the existing closed Inquiry is FORBIDDEN** | Custody interval closes when the next act occurs | None | Untouched | ⚠ **`Z-4`: is a reasoned outcome recorded, and on what? OPEN. Two homes named, NEITHER adopted** | ⚠ **Manager-effectiveness reporting is DISABLED until `Z-4` is answered (AD-01I §10.1 row 13)** |
| **R6** | **Customer re-contacts a record that is under a work mandate** | Unchanged | Both routes now true at once | ⚠ **NEW Inquiry opens; the mandate's status is `Z-1`'s** | Unchanged unless management acts | New claim set | Governed by `PO-O` | Both events recorded, distinguishably | ⚠ **Capture: YES for the Inquiry; the mandate contributes nothing** |
| **R7** | **Re-contact arrives through a DIFFERENT CP or source than the closed Inquiry** | Unchanged | Unchanged | NEW Inquiry | Unchanged | ⚠ **`V-15`'s structural half answered (`J-28`); its commercial half IS `T-4`. OPEN, with direct §32/§40 exposure** | ⚠ **A competing registration is PERMITTED (`PO-V2·a`, `PO-AB2`) and creates a CLASH the Site Head reads** | Recorded | ⚠ **Capture: YES. WHOSE capture is `T-4`'s and `AC-42` limb 8's** |
| **R8** | **Re-contact after the customer has already BOUGHT** (a prior Inquiry converted) | ⚠ **Now a `Customer` in one context and a prospect in another — ⟦SOURCE⟧ §07 permits exactly this** | Unchanged. ⚠ **Whether it stays in the sales queue is `Y-4`, OPEN** | NEW Inquiry (`Q6-S4`, restated at the Inquiry level by `G-12`/`H-8`) | Unchanged | ⚠ **`T-5` — repeat-purchase reach. `K-26` recommends NO REACH across a fully discharged claim; `ARCHITECT DERIVED`, not approved** | ⚠ **`AC-26`'s post-conversion limb — does a live registration ground a claim on an Inquiry opened AFTER that client's purchase converted? OPEN, twelve documents** | Recorded | ⚠ **Capture: YES. The prior conversion is NOT restated** |

### 5.3 What the eight scenarios establish, and what they do not

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-340` — SEVEN OF THE EIGHT ARE STRUCTURALLY DETERMINATE AND FIVE OF THE
> EIGHT ARE COMMERCIALLY INDETERMINATE. THAT GAP IS THE REACTIVATION MODEL'S ACTUAL STATE.**
>
> - **Structurally determinate:** R1, R2, R4, R5, R6, R7, R8 — the record/Inquiry/assignment outcome
>   follows from `J-2`, `I-6` and `I-7` without a business decision.
> - **Structurally INDETERMINATE:** ⚠ **R3 alone, and it is undecided because `V-3` clause 2 was
>   RECORDED AS RESOLVED WHEN IT WAS NOT (`A-5`).**
> - **Commercially indeterminate:** ⚠ **R1, R3, R5, R7, R8 — on `T-4`, `V-3`, `Z-4`, `T-4` and
>   `T-5`/`AC-26` respectively.**
>
> ⚠ **`Q5` — *"can a Dumped lead be re-engaged, or does re-contact create a new lead?"* — is recorded
> across the corpus as having no mechanical subject left (`H-4` leaves no backward edge to argue about).
> **That is true and it is NOT a resolution of `Q5`.** **`Q5`'s commercial half IS `V-14`, whose
> attribution third IS `T-4`, which is open.** ⚠ **The audit finds `Q5` OPEN, restated, and NOT closed by
> any document — including this one.**

---

## 6. Assignment and custody

> ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `E`: the assignment / custody model across ten scenarios,
> and whether the Assignment Log alone is SUFFICIENT or a missing mechanism must be identified. **No
> schema appears. `U` forbids it and it is honoured.**

### 6.1 The baseline, at its mint

> ⟦PRODUCT-OWNER DECISION — AD-01A §8.5, 2026-09-12⟧ ***Eliminate the persisted Assignment State axis. Do
> not create Unassigned / Assigned / Reassignment Pending as a separate state machine. Owner, Handler,
> Assignment History, activities, tasks and derived completeness/work-queue conditions should carry this
> information, unless a later explicit requirement proves otherwise. Do not invent a
> reassignment-approval workflow.***

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-341` — THE DECISION CONTAINS ITS OWN ESCAPE CLAUSE AND THREE DOCUMENTS HAVE
> SINCE USED IT WITHOUT SAYING SO.** **The clause is *"unless a later explicit requirement proves
> otherwise."* **AD-01G `G-9`, AD-01I `I-8` and AD-01G §9.1 each add a fact to the Assignment Log on
> exactly that basis.** ⚠ **Adding ATTRIBUTES to the Log is precisely what §8.5 DIRECTED (AD-01F `A-7`
> records this: *"The new evidence adds obligations to the Assignment Log, which is exactly what §8.5
> directed"*). **So the additions are legitimate in KIND. What none of the three documents does is state
> that the Log's required content has now grown by three facts that did not exist when §8.5 was decided,
> and that none of the three is approved.** **That is stated here.**

### 6.2 The ten scenarios

| # | Scenario | Expressible with Owner + Handler + Assignment Log **as §8.5 left it**? | What is missing |
|---|---|---|---|
| **A1** | Ordinary reassignment mid-pipeline | ⚠ **Yes** | — |
| **A2** | Handler departs; records become unassigned | ⚠ **Yes** — ⟦SOURCE⟧ §57 (*"preserve historical actor identity"*) | ⚠ **`N-3`: does deactivation CLEAR the live Handler reference or is *effectively unassigned* computed against the active flag? OPEN — either satisfies §57** |
| **A3** | Helpdesk capture with no handler (§42) | ⚠ **Yes** — absence of Handler | — |
| **A4** | Bulk transfer of many records in one act | ⚠ **Partly** | ⚠ **`R-10` gives the act; `Z-6` asks whether management INTENT is uniform across the batch or per record. OPEN, and *"unrecoverable if decided by implementation"*** |
| **A5** | Transfer **with** history | ⚠ **No** | ⚠ **`G-9` adds the conferral decision as a parameter of the transfer act. Unapproved. Unrecoverable if not recorded at the moment of the act** |
| **A6** | Transfer **without** history | ⚠ **No** | ⚠ **Same: the ABSENCE of a recorded conferral must itself be a recorded state of the act, or a later reader cannot tell *"withheld"* from *"never asked"*** |
| **A7** | Automatic assignment to a new handler | ⚠ **No** | ⚠ **`G-9`'s second fact — assignment PROVENANCE (automatic rule vs explicit management act). `U-7` made it necessary; AD-01G §10.1 requires it; unapproved** |
| **A8** | Redistribution of a record with **no live Inquiry** | ⚠ **No** | ⚠ **`I-8`/`A-2` — the work MANDATE. Without it the fresher holds a record the system says needs no follow-up (AD-01F `C-6`). Unapproved** |
| **A9** | A mandate that has run its course | ⚠ **No** | ⚠ **`Z-1`: does a mandate END, and on what basis? OPEN. Candidate discharge conditions named; NONE recommended, preferred or ranked** |
| **A10** | Return of a record to a handler who held it before | ⚠ **Yes for custody; NO for visibility** | ⚠ **`V-25`/`U-14`: does participation entitlement ENDURE through reassignment away, role change or departure? OPEN, and AD-01G §9.4 makes it the SOLE determinant of this case** |

### 6.3 The three unapproved facts the Log is asked to carry

| # | Fact | Added by | Recoverable later? | Status |
|---|---|---|---|---|
| **1** | The **with/without-history decision**, per record, even inside a bulk act | `G-9`; AD-01G §9.1 | ⚠ **NO** — AD-01G §9.3 constraint 2 | `ARCHITECT DERIVED`, unapproved |
| **2** | **Assignment provenance** — automatic rule vs explicit management act | `G-9`; AD-01E `R5`/`U-7` | ⚠ **NO** — AD-01E §7.1 | `ARCHITECT DERIVED`, unapproved |
| **3** | **Management intent / work mandate** | `I-8` / `A-2` | ⚠ **NO** — AD-01I §6.2 | `ARCHITECT DERIVED`, unapproved; ⚠ **`A-2` CONTRADICTS AD-01H §5.1's enumeration and AD-01I says in terms that the owner *cannot approve `H-4` as literally written AND `I-8`*** |

> ⚠ ⟦ARCHITECT RECOMMENDATION — the answer to investigation `E`'s question⟧ *(RECOMMENDED, SUBJECT TO THE
> PROJECT OWNER'S EXPLICIT WRITTEN APPROVAL. **NOT APPROVED.** No schema, no entity, no attribute name.)*
>
> ⚠ **`PF-342` — The Assignment Log is SUFFICIENT AS A CARRIER and INSUFFICIENT AS CURRENTLY APPROVED. No
> missing MECHANISM is identified — `G-9` is right that no new canonical entity is required and
> `AD-01G G-9`'s correction of AD-01F §14.1's pricing stands. What is missing is APPROVAL of three
> attributes, each of which is unrecoverable if not recorded at the moment of the act.** ⚠ **This is not a
> new finding — it is three existing findings counted together for the first time, and the COUNT is the
> report: the same "unrecoverable" argument has now been made three times by three documents about three
> different facts on one entity, and no document states the aggregate.**

### 6.4 What assignment must NOT do, confirmed

| Prohibition | Basis | Audit result |
|---|---|---|
| A transfer must not move the **lifecycle** | AD-01 `E-04`; AD-01F §8.2 | ⚠ **CONFIRMED, and STRENGTHENED by `H-4`: under Model D the record carries no persisted lifecycle value to move** |
| A transfer must not create, move or affect a **commercial claim** | `I-6`; `J-16`; ⚠ **`Q0-b`, which AD-01N records as *answered in substance* by `PO-N4` and still awaiting formal ratification** | ⚠ **CONFIRMED from both sides of the seam** |
| A transfer must not produce a fresh view by **write-time mutation** | `G-8` — no lifecycle reset, no deletion, no copy, no counter reset | ⚠ **CONFIRMED. It is ⟦SOURCE⟧ §07, `R6` and consolidated §31 expressed as a build rule** |
| A **reassignment-approval workflow** must not be invented | ⚠ **`PO LOCKED`** — AD-01A §8.5 | ⚠ **CONFIRMED HONOURED by AD-01G §9.1 (conferral is a PARAMETER of the transfer act, not a second act) and by AD-01I §5** |

---

## 7. History visibility

> ⟦ARCHITECT ANALYSIS⟧ **Owner's investigations `F` and `G`. Eleven actor/scenario types, `W-3`
> re-examined at its mint, and the with/without-history mechanism determined conceptually.**

### 7.1 The boundary, at its mint

> ⟦SETTLED — AD-01G `G-6`, `ARCHITECT DERIVED`, NOT APPROVED⟧ **Two boundaries, both cleared: the
> **custody interval** for handler-facing temporal history, and the **inquiry** for project-scoped and
> external visibility. Entitlement inputs are **I1 participation ∪ I2 management breadth ∪ I3
> conferral**, evaluated per *(viewer, work record, interval)*, **fail-closed**. **Custody is not an
> input.***

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-343` — THE REJECTION OF `original_handler_id = current_handler_id` IS THE
> BEST-ARGUED SINGLE FINDING IN THE FOUNDATIONAL CORPUS AND IT SURVIVES THE AUDIT UNCHANGED.** **AD-01E
> §6.1's reason is deeper than the owner's own counterexample: that predicate answers a question about
> **custody**, and the question being asked is about **entitlement**. **Custody is a present operational
> assignment that changes routinely; entitlement is a durable authorization property.** ⚠ **CONFIRMED,
> and the confirmation is recorded as deliberately as a correction would be.**

### 7.2 The eleven actor/scenario types

| # | Viewer / scenario | Entitled? | By which input | Open item |
|---|---|---|---|---|
| **V1** | Current handler, own custody interval | ⚠ **Always, in full** | Their own work | — |
| **V2** | Current handler, prior intervals, arrived by automatic assignment | ⚠ **No** | Fail-closed | ⚠ **`V-9`: existence or content? `V-11`/`U-11` offered a DEFAULT (disclose existence, withhold contents) and DECLINED to decide** |
| **V3** | Current handler, prior intervals, arrived by transfer **with** history | **Yes** | **I3** | `V-8` (mandatory? default? revocable?) |
| **V4** | Prior handler who worked an interval | ⚠ **Yes, permanently — *"the restriction is about a handler who was never there, not about un-showing what was already shown"*** | **I1** | ⚠ **`V-25`/`U-14`: does I1 SURVIVE reassignment away, role change, departure? OPEN, and LOAD-BEARING** |
| **V5** | Sales Head | **Yes, unconditionally** | **I2** | ⚠ **`W-5`: is that unconditionality AFFIRMED by the owner? A recommendation exists (*never* restrict I2); affirmation is SOUGHT, not assumed** |
| **V6** | "Authorized Reporting Manager" | **Yes** | **I2** | ⚠ **`U-10`: ⟦SOURCE⟧ §03's eleven roles do NOT include *Reporting Manager*; `M-3`/`M-7` open; `R2` forbids branching on a role name — it must resolve to BREADTH ON A GRANT** |
| **V7** | A manager whose grant CHANGED since the interval | ⚠ **Undecided** | — | ⚠ **`U-9`: is entitlement read against the grant NOW or the grant AT THE TIME? Both defensible; they differ materially. OPEN** |
| **V8** | A viewer scoped to Project A, on a record also holding a Project B Inquiry | ⚠ **Undecided** | Interest boundary | ⚠ **`V-24`: does ⟦SOURCE⟧ §08 project scope evaluate against the record or the Inquiry? OPEN — and AD-01G `G-10` shows §08 scoping is ALREADY an intra-record boundary under plural interests, independently of history** |
| **V9** | A **CP**, through the §39 portal | ⚠ **Their own engagement only — on the recommendation** | Interest boundary | ⚠ **`U-16`; and on the CP side `AC-13` — six open limbs plus limb (viii) — asks what a CP may be told, ENLARGED by `PO-AB1`'s Site-Head evidence surface. OPEN on both sides** |
| **V10** | A rep working Inquiry B, looking at clash information on Inquiry A of the same record | ⚠ **No** | Interest boundary | ⚠ **⟦SOURCE⟧ §11 already restricts clash information from reps; `PO-V2·c`'s non-disclosure LOCKS the CP-side half. **`H-6`/AD-01H §12 row 3 makes it engagement-scoped. Mechanism is `G-6`'s interest boundary; `V-24` unresolved** |
| **V11** | A handler reading a **derived aggregate** — activity count, record age, *last touched*, engagement depth, prior-handler count | ⚠ **MUST be inside the boundary, and this is the likeliest failure** | — | ⚠ **AD-01E §6.8: *"a boundary that hides records but exposes their shadow is not a boundary."* ⟦SOURCE⟧ §11: authorization, *"not merely hiding a badge."* **`V-9`/`V-10` own it and both are OPEN** |

### 7.3 The eleven content categories, carried

> ⟦SETTLED — AD-01E §6.6, `H1 … H12`⟧ **Calls · WhatsApp/email · site visits · notes · old clash
> information (⚠ **NOT a new question — §11 already restricts it per engagement**) · old assignment
> history · ⚠ **old Dump reason (`U-12`)** · ⚠ **old commercial information — budget, configuration,
> quotes, negotiation position (`U-12`)** · ⚠ **prior booking-attempt and inventory-hold information
> (`U-12`)** · ⚠ **the EXISTENCE of prior work (`U-11`/`V-9`)** · ⚠ **derived aggregates** ·
> person-level history on other projects (out of scope).

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-344` — `U-12`'s three sub-questions are the only place in this domain where
> an architect would be choosing a COMMERCIAL-DISCLOSURE POLICY, and all three remain open. Nothing here
> narrows them, and no default is offered beyond the one AD-01E already offered and declined to decide.**

### 7.4 `W-3` — re-confirmed open, and enlarged

> ⟦BUSINESS DECISION REQUIRED — `W-3`, read at AD-01G §15.1 where it was MINTED, verbatim⟧
> ***"Does the business ever transfer a record without history to a handler who PREVIOUSLY WORKED IT?"***
> **(The eighth case — the only one the union cannot express.)**

**The re-confirmation, run rather than assumed:**

| Check | Result |
|---|---|
| **Is it answered anywhere?** | ⚠ **NO.** AD-01H §14.2: *"`W-3`, `W-5` untouched."* AD-01I §13.3: *"`W-3`, `W-4`, `W-5` untouched."* AD-01K §19: `W-1 … W-5` not resolved. **A search of `03j` … `03ab` returns no substantive treatment of any `W-` item.** ⚠ **`W-3` has been open across TWENTY-ONE subsequent documents and touched by none.** |
| **Has the ground for calling it *undescribed* changed?** | ⚠ **YES, AND THIS IS THIS AUDIT'S CONTRIBUTION.** See below. |
| **Is it narrowed, branched or given a preferred answer here?** | ⚠ **NO.** |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-345` — `W-3`'s EIGHTH CASE IS REACHABLE BY ACCIDENT, WHICH IS A DIFFERENT
> AND WORSE STANDING THAN *UNDESCRIBED*.**
>
> **AD-01G §8.2's ground is precise and, for the case it considers, correct:** *"`§7`'s redistribution case
> names 'a fresher', and a fresher is by construction not the prior handler."* ⚠ **That ground covers the
> DELIBERATE, single-record path. It does not cover the BULK path, and two bulk paths are described by the
> owner's own sources:**
>
> | Bulk path | ⟦SOURCE⟧ | Why it reaches the eighth case |
> |---|---|---|
> | **Multi-select transfer** | Consolidated **§16** / `R-10`: *"Select one or multiple leads. Choose a Sales Representative from the reporting hierarchy. Transfer leads individually or in bulk."* | ⚠ **The manager selects a SET and names ONE recipient. Nothing screens the set for records that recipient previously held. If the transfer is made WITHOUT history, the eighth case occurs for every such record — silently** |
| **Absorption of a departed employee's book** | ⟦SOURCE⟧ **§57**; `Z-6` (*"absorbing a departed employee's book pushes live and closed records in one act"*) | ⚠ **The set is defined by the DEPARTING employee, not by the receiving one. A record that passed A → B → (B departs) → A is an ordinary outcome of staff movement** |
>
> ⚠ **CONSEQUENCE, STATED AND NOT RESOLVED: `W-3` is not only *"does the business ever deliberately do
> this"* — it is also *"what happens when a bulk act does it without anyone deciding to."* **Those are
> different questions with potentially different answers: a business may answer NO to the deliberate act
> and still need a defined behaviour for the accidental one.** ⚠ **`W-3` IS NOT RENUMBERED, NOT
> RE-MINTED AND NOT SPLIT. Its population is ENLARGED, on AD-01Y's own standard — *enlarging a
> contradiction's population is not minting a new one* — and the same standard is applied here to an open
> item.** **`W-3` is now COUPLED to `Z-6` (uniform or per-record intent in a bulk act) and to `V-8`
> (is the choice mandatory, and does a bulk act apply one decision to all). **NONE of the three is
> answered.**
>
> ⚠ **AND THE STRUCTURAL PARALLEL IS RECORDED BECAUSE THE OWNER'S FRAMING ASKS FOR IT: this is `AC-13`'s
> pattern — *enlarged, not resolved* — occurring in the Lead domain for the first time.** **`AC-13` has
> six open limbs plus limb (viii) and has been enlarged repeatedly without ever being answered; `W-3` now
> has the same shape. Naming the resemblance is not answering either.**

### 7.5 Transfer *with* history versus *without* history

> ⟦SETTLED — AD-01G `G-7`, `ARCHITECT DERIVED`, NOT APPROVED⟧ ***Transfer with history* = a transfer act
> that records an **I3 conferral** naming the receiving handler. *Transfer without history* = **the same
> act with no conferral recorded.** Nothing is denied; nothing is hidden by rewriting; the receiving
> handler simply has no entitlement input covering the earlier intervals, and the fail-closed default
> applies. The conferral is a **parameter of the transfer act** — one act, two outputs.**

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-346` — THE MECHANISM DETERMINATION SURVIVES THE AUDIT, WITH ONE PRECISION
> THE CORPUS DOES NOT STATE.** ⚠ **The model's correctness depends on *"no conferral recorded"* being
> DISTINGUISHABLE from *"the act predates the requirement"* and from *"the field was never populated."*
> **Under a union-of-grants read at query time those three are the same absence.** **AD-01G §9.3
> constraint 2 says the decision *"must be recorded at the moment of the act and is unrecoverable if
> not"* — which presupposes the distinction without naming it.** ⚠ **THIS IS NOT MINTED AS A NEW ITEM.
> It lands squarely inside `V-8` (*is the choice MANDATORY on every transfer, what is the DEFAULT*),
> because a mandatory choice makes the three absences distinguishable and an optional one does not.
> **Recorded as an ENLARGEMENT of `V-8` and not as a new decision.**

### 7.6 The privacy / security consequences of the mechanism

| # | Consequence | Basis | Status |
|---|---|---|---|
| **1** | ⚠ **Restricted history is a SECURITY boundary in the same class as tenant isolation, not a display preference** | ⟦SOURCE⟧ §11 (*"enforced by authorization — not merely by hiding a badge"*); §45 (*"search must NEVER become a side door"*); Spec **Rule 4**; §03 | ⚠ **CONFIRMED on both sides of the seam** |
| **2** | ⚠ **The boundary must bind API, search, export, reports, notifications, attachments, background jobs, audit access and offline caches** | AD-01E §8; `V-10` | ⚠ **`V-10` OPEN — no source ENUMERATES the surfaces** |
| **3** | ⚠ **Derived aggregates are the likeliest leak, and under an intra-record boundary they are the DEFAULT surface** | AD-01E §6.8 | ⚠ **`V-9`/`V-10` OPEN** |
| **4** | ⚠ **An escape hatch, IF `W-3` is *yes*, must never apply to I2 management breadth** | AD-01G §9.3 constraint 1 | ⚠ **`W-5` — affirmation SOUGHT, not assumed** |
| **5** | ⚠ **An escape hatch is deny-precedence IN SUBSTANCE, scoped — a cost knowingly accepted, never generalised into the permission system (`R2`)** | AD-01G §9.3 | ⚠ **Conditional on `W-3`** |
| **6** | ⚠ **Offline: a device-resident copy of restricted history sits outside every server-side control** | `U-20`; ⟦SOURCE⟧ §46 requires offline data minimisation and is **silent on this case** | ⚠ **`U-20` OPEN; `M-14` owns the offline queue** |

---

## 8. Timeline reconciliation

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `I`. Every timeline entry class is classified as
> business-event / derived-view / persisted-fact / system-event, with its metric relevance. **THE
> COMPREHENSIVE AUDIT COMPLETENESS GATE IS NOT OPENED HERE, NOT PARTIALLY OPENED, AND NOT DESIGNED. This
> section classifies; it does not certify completeness of anything.**

### 8.1 The requirement, and the apparent contradiction that dissolves

> ⟦SOURCE⟧ Consolidated **§8** presents ONE chronological timeline — creation, assignment, first user
> follow-up, response/sub-response, remarks, next follow-up, subsequent follow-ups, transfers,
> regeneration, success/booking, dump, *"other system-generated events"* — and then requires that *"the
> system should distinguish user-performed activities from system-generated timeline events"*, reinforced
> by **§31** (*"do not treat system-generated events as equivalent to user-performed sales work"*) and
> **§9** (FUT excludes them).

> ⟦SETTLED — AD-01F `C-8`⟧ **This is not a contradiction in the requirement. It is a contradiction only
> if the single timeline is read as a single STORAGE concept.**

### 8.2 The classification

| # | Entry class | Classification | Owner object | Metric relevance |
|---|---|---|---|---|
| **1** | Record creation | **Business event, persisted fact** | Work record | Denominator anchor for creation→first-response (`V-20`) |
| **2** | **Inquiry origination** | **Business event, persisted fact** | ⚠ **Inquiry** | ⚠ **Capture (`V-18`); source/campaign attribution; `W-4`'s clock** |
| **3** | Assignment / transfer | ⚠ **Business event, persisted — and it ALREADY HAS A CANONICAL HOME** | ⟦SOURCE⟧ §06 **Assignment Log** | Custody→first-response (`V-20`); custody-scoped FUT |
| **4** | **First user follow-up** | ⚠ **DERIVED VIEW — it must NOT be stored** | Computed over activities | ⚠ **AD-01F §7: storing it creates a second source of truth for a fact the activity history already carries** |
| **5** | User follow-up / activity (response, sub-response, remarks, next follow-up) | **Business event, persisted fact** | Work record, **referencing the Inquiry it concerns** | ⚠ **FUT — `V-4` (*qualifying*) and `W-1` (one activity, several Inquiries) both OPEN** |
| **6** | Site visit | **Business event, persisted fact** | ⟦SOURCE⟧ §09/§80 give it its own record | ⚠ **`V-19`: is *Site Visit Done* as a SUB-RESPONSE, or the site-visit RECORD, the source of truth? OPEN** |
| **7** | Regenerated / renewed inquiry | **Business event** = entry class 2 | Inquiry | `V-18` |
| **8** | Success / booking | ⚠ **Business event on the BOOKING; a CONVERSION REFERENCE on the Inquiry** | Booking (own lifecycle, `PO LOCKED`) | Conversion numerator; `V-17`'s key |
| **9** | Dump / disposition | ⚠ **TWO different entries** — a persisted commercial closure on the Inquiry, and a derived queue condition on the record | Inquiry / work record | ⚠ **Only the first may enter a commercial denominator** |
| **10** | Redistribution act | **Business event, persisted** | ⚠ **Assignment Log, carrying intent (`I-8`, unapproved)** | ⚠ **Workload only — NEVER a Capture (`I-6`)** |
| **11** | Correction / reconciliation | ⚠ **Business event, APPENDED, never an edit** | The corrected object | ⚠ **`T-7`: correction-reason vocabulary does not exist and none is invented. `U-18`: revival vs correction must be distinguishable in authorization AND in reporting — they have OPPOSITE reporting effects** |
| **12** | System-generated events | **System event** | — | ⚠ **EXCLUDED from FUT and from first-response by ⟦SOURCE⟧ §9/§31** |
| **13** | Derived buckets (Today / Future / Overdue / New) | ⚠ **DERIVED VIEW, never a timeline entry and never persisted** | — | ⚠ **`PO LOCKED` as derived (AD-01A §8.1)** |

### 8.3 The three requirements the classification imposes

> ⟦SETTLED — AD-01H `H-9`⟧ **Every business-history entry must be resolvable to (i) WHICH INQUIRY it
> concerns, (ii) WHICH CUSTODY INTERVAL it fell in, and (iii) WHETHER IT IS USER-PERFORMED SALES WORK.
> Without (i), the single chronological timeline required by consolidated §8 becomes FALSE for every
> record holding more than one Inquiry.**

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-347` — REQUIREMENT (i) IS IN DIRECT TENSION WITH `W-1`, AND NO DOCUMENT
> STATES THE TENSION.** **`H-9` requires every entry to resolve to WHICH Inquiry it concerns. `W-1` asks
> whether one activity may relate to MORE THAN ONE Inquiry — and AD-01I records the INVERSE case as
> already permitted: under Scenario B a fresher's activity references NO Inquiry at all, which *"`H-9`
> already permits."*** ⚠ **So `H-9`'s *"which inquiry it concerns"* is already known to admit ZERO and is
> undecided for MANY. **The requirement as written presupposes exactly one.** ⚠ **NOT MINTED — this is
> `W-1`'s subject and `W-1` is open. Recorded as an ENLARGEMENT of `W-1`'s consequence: it now reaches
> `H-9`'s formulation and not only FUT's count.**

### 8.4 Two things the timeline must never do

| Prohibition | Basis |
|---|---|
| ⚠ **Never rewrite, delete, reset or copy an entry to produce a presentation outcome** | `G-8`; ⟦SOURCE⟧ §07, `R6`, consolidated §31; ⟦PRODUCT-OWNER DECISION⟧ AD-01E §1.1 (*"never deleted, reset, copied into a new lead, or rewritten merely because visibility differs"*) |
| ⚠ **Never conflate EFFECTIVE time with RECORDED time** | AD-01 `E-23`; AD-01I §10.1 row 10. **Conflating them corrupts every duration after an offline sync** (⟦SOURCE⟧ §12, §47) |

### 8.5 What this section deliberately does not do

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-348` — The Audit Completeness Gate would ask *"is the set of recorded
> events SUFFICIENT to reconstruct every decision the business must be able to defend?"* **That question
> is NOT asked here and NOT answered here.** ⚠ **It is noted only that `X-25`/`A-37` — ⟦SOURCE⟧ `NF-27`'s
> missing audit events — has had its priority raised for NINE consecutive documents with SIX
> owner-stated permanences resting on it, and that `PO-Q2`'s full-reconstructability requirement is
> recorded as NOT YET SATISFIABLE. **Those are carried facts, not a Gate, and they are not to be mistaken
> for one** — AD-01AB §20 item 9's own instruction.**

---

## 9. First Response and FUT

> ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `H`: eleven event types tested against the First-Response
> and FUT boundaries. **NO SLA TARGET, THRESHOLD, WINDOW OR WORKING-HOUR RULE IS PROPOSED ANYWHERE.**

### 9.1 The definitions at their mint

> ⟦SOURCE⟧ Consolidated **§9**: first response is measured to the *"first **QUALIFYING** user follow-up"*;
> FUT is the *"total count of actual **user-performed** follow-ups"*. ⟦SOURCE⟧ **§31**: system-generated
> events are not equivalent to user-performed sales work.

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-349` — THE WORD *QUALIFYING* IS LOAD-BEARING, UNDEFINED, AND HAS BEEN
> NAMED *THE CHEAPEST UNBLOCKING QUESTION* FOR NINETEEN CONSECUTIVE DOCUMENTS.** **`V-4`.** **The
> observed operation carried a separate `Attempted` bucket, which is direct evidence that a non-connected
> attempt is *not nothing* and may also not be a follow-up.** ⚠ **`V-4` gates FIVE quantities that MUST
> share one answer or they will disagree permanently: `New` (all three predicates), FUT, every
> first-response variant, `W-1`'s counting rule and `W-4`'s clock.**

### 9.2 The eleven event types

| # | Event | Counts for **First Response**? | Counts for **FUT**? | Basis / open item |
|---|---|---|---|---|
| **1** | Connected call with a substantive outcome | ⚠ **Yes** | ⚠ **Yes** | The uncontroversial case |
| **2** | **Call attempt, not connected** (⟦SOURCE⟧ §49 *Not Connected* / *No Answer*) | ⚠ **UNDECIDED** | ⚠ **UNDECIDED** | ⚠ **`V-4`. OPEN** |
| **3** | Outbound WhatsApp / email sent by the handler | ⚠ **UNDECIDED** | ⚠ **UNDECIDED** | ⚠ **`V-4`; ⟦SOURCE⟧ §72 requires integrations never compromise core CRM integrity — the answer must not depend on a provider's delivery receipt** |
| **4** | **Inbound** customer contact | ⚠ **UNDECIDED, and it is the sharpest case** | ⚠ **No — it is not user-PERFORMED** | ⚠ **`V-4`. A first response measured against a customer's own inbound message would make responsiveness depend on the customer** |
| **5** | Site visit performed | ⚠ **Presumably yes** | ⚠ **Presumably yes** | ⚠ **`V-19`: is the site-visit RECORD or the *Site Visit Done* SUB-RESPONSE the source of truth? OPEN — and if both, it double-counts** |
| **6** | Remarks entered with no contact | ⚠ **No, on any reading** | ⚠ **UNDECIDED** | ⚠ **`V-4`. A note is user-performed and is not a follow-up on any natural reading, but nothing states it** |
| **7** | Next-action date moved with no contact | ⚠ **No** | ⚠ **UNDECIDED** | ⚠ **`V-4`. AD-01 §6.1 calls the self-transition *"the most frequent event in the system"* and requires it be recorded as an ACTIVITY, not a state change** |
| **8** | **Assignment / transfer** | ⚠ **No — it is not sales work** | ⚠ **No** | ⟦SOURCE⟧ §31; `I-6`. **CONFIRMED** |
| **9** | **Redistribution with a work mandate** | ⚠ **No** | ⚠ **No** | `I-6`/`I-7`. ⚠ **But it RESETS the custody clock, so `custody→first-response` restarts — AD-01I §10.1 row 9 moves this from *new* to *required*** |
| **10** | **System-generated event** | ⚠ **No** | ⚠ **No** | ⚠ **⟦SOURCE⟧ §9, §31. `PO LOCKED` in substance** |
| **11** | **One call covering TWO projects** | ⚠ **UNDECIDED — one first-response or two?** | ⚠ **UNDECIDED — one FUT or two?** | ⚠ **`W-1`. AD-01H records that concurrent interests make this ROUTINE rather than occasional** |

### 9.3 The four first-response clocks, none selected

| Clock | Registered as | Status |
|---|---|---|
| **creation → first response** | ⟦SOURCE⟧ consolidated §9 flags it OPEN | ⚠ **`V-20`** |
| **assignment → first response** | ⟦SOURCE⟧ consolidated §9 flags it OPEN | ⚠ **`V-20`** |
| **custody → first response** | AD-01F §13.1 added it; AD-01I §10.1 row 9 makes it **required** | ⚠ **`V-20`** |
| **inquiry → first response** | AD-01G `W-4` added it; AD-01H §10.2 moves it from *optional* to **necessary** | ⚠ **`W-4`** |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-350` — FOUR CLOCKS, FOUR DIFFERENT ANSWERS, ONE WORD.** **Two are needed
> for HANDLER responsiveness, one for SOURCE/CP responsiveness (`W-4`, because the source is the
> Inquiry's), and one for RECORD-level intake. **They are not variants of one metric; they answer
> different questions and a product that ships one of them under the name *first response* will be
> read as having shipped the others.** ⚠ **NONE IS SELECTED, PREFERRED OR RANKED HERE.**

### 9.4 FUT's two independent undefined boundaries

| # | Boundary | Owner |
|---|---|---|
| **1** | ⚠ **WHAT counts** — the *qualifying* predicate | ⚠ **`V-4`, OPEN** |
| **2** | ⚠ **HOW IT COUNTS when one act spans two Inquiries** | ⚠ **`W-1`, OPEN** |
| **3** | ⚠ **AT WHICH LEVEL it is reported** — record-lifetime, per custody interval, or per Inquiry | ⚠ **All three are legitimate and needed; AD-01G §4.1 makes FUT a WORK-RECORD quantity, `G-9` makes per-handler FUT a custody quantity, and `W-4` implies a per-Inquiry one. NO SELECTION IS MADE** |

### 9.5 No SLA is proposed

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-351` — ⟦SOURCE⟧ §58 endorses escalation for missed follow-ups and NAMES
> NO NUMBERS; §42's helpdesk queue implies urgency without quantifying it; AD-01E `U-4` established that
> **no dormancy threshold exists anywhere in the sources.** **`Q14` owns response-time SLAs and
> escalation thresholds and is OPEN. `Q2` owns the maximum next-action horizon and is OPEN. `N-2` owns
> which *waiting-on* values SUPPRESS escalation and is OPEN, and `X-6` records `Q14` as obstructed by it.**
> ⚠ **NO THRESHOLD, TARGET, WINDOW, HORIZON OR WORKING-HOUR RULE IS PROPOSED, IMPLIED OR RANKED
> ANYWHERE IN THIS DOCUMENT.**

---

## 10. Derived action buckets

> ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `J`: Today / Future / Overdue reconfirmed as derived, with
> the timezone / day-boundary implications placed where they are actually governed.**

### 10.1 The reconfirmation

> ⟦PRODUCT-OWNER DECISION — AD-01A §8.1, 2026-09-12⟧ ***"Today", "Future" and "Overdue" are
> operational/Action-Feed conditions, not Lead Lifecycle states. They must be DERIVED from next-action
> timing, never persisted as lifecycle state.***

> ⟦PRODUCT-OWNER DECISION — consolidated §11, recorded at AD-01F `C-11`⟧ ***"'Pending' and 'Overdue' mean
> the same historical thing. BMexa will use ONLY the term 'Overdue'."***

| Check | Result |
|---|---|
| Is the derivation still correct? | ⚠ **YES. AD-01 §2.2's argument is unimproved: a lead scheduled for tomorrow is *Future* now and *Today* in a few hours, with nobody touching the record and no business event occurring. Persisting it requires a clock-driven job that rewrites business state with no business event, which under `R6` either floods the audit log or writes unaudited state changes** |
| Has anything since contradicted it? | ⚠ **NO. Checked at AD-01F `C-11`, AD-01G, AD-01H `H-5` and across the CP chain. NOTHING reaches it** |
| Is `New` in the same class? | ⚠ **NO, AND THIS IS THE DISCREPANCY.** **`Today`/`Future`/`Overdue` are `PO LOCKED` as derived. `New` is `PO LOCKED` as PERSISTED (AD-01A §8.1's four values) and is ARCHITECT-recommended as derived (`V-5`/`V-6`/`H-5`), with the amendment `A-1` NAMED AND NOT MADE** |
| Is `Blocked` in the same class? | ⚠ **NO.** ⟦PRODUCT-OWNER CLARIFICATION — AD-01C §1.2⟧ ***"'Blocked' is NOT purely timing-derived if it represents who owes the next move."*** ⚠ **AD-01A §8.6's phrasing *"Today / Future / Overdue / Blocked, computed from next-action timing, not stored"* is INACCURATE AS WRITTEN for `Blocked` — `T-12`, open, and its corrective note is owed by whoever resolves `N-2`** |

### 10.2 The three `New` predicates, carried

> ⟦SETTLED — AD-01H `H-5`, `ARCHITECT DERIVED`, NOT APPROVED⟧ **`New` is a family of THREE derived
> predicates — **N-record** (no disposition has ever been recorded), **N-context** (no qualifying user
> follow-up in the current custody interval), **N-inquiry** (no qualifying user follow-up on this
> Inquiry) — all legitimate, all needed, none persisted.**

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-352` — `H-5` IS THE CLEANEST EXPLANATION IN THE CORPUS OF WHY ONE COLUMN
> COULD NOT WORK, AND IT IS ALSO THE PROPOSITION FURTHEST FROM APPROVAL.** **A record with a
> seventeen-entry timeline transferred WITHOUT history is `New` to its holder and heavily worked to the
> business. A single record-level value must lie to one of them.** ⚠ **WHICH predicate the operational
> queue uses is `V-5`; what *qualifying* means is `V-4`; whether `A-1` is made is the owner's. **THREE
> OPEN ITEMS SIT UNDER ONE WORD THE PRODUCT USES ON ITS PRIMARY SCREEN.**

### 10.3 Timezone and day-boundary — placed where it is actually governed

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-353` — THE DAY-BOUNDARY QUESTION IS **NOT** `AC-6`'s, AND SAYING SO IS THE
> POINT OF THIS SUB-SECTION.** **The check was run at `AC-6`'s mint rather than on its name.**
>
> | | |
> |---|---|
> | **`AC-6`, verbatim at AD-01N §F.2** | ⟦BUSINESS DECISION REQUIRED⟧ ***"Boundary convention: is the last eligible day `registration date + N` (convention α), or `registration date + N − 1` (convention β)? And is `N = 0` a legal configuration?"*** |
> | **What it governs** | ⚠ **The CP REGISTRATION WINDOW's last eligible day, with two money-bearing consumers and one presentational one** |
> | **Does it govern the Action Feed's day boundary?** | ⚠ **NO. `AC-6` fixes an ORDINAL DAY-COUNTING convention for a window of length `N`. The Action Feed asks a different question: against WHICH CLOCK — server, tenant or viewer — is *today* evaluated, and where does the day break** |
> | **Where the Action Feed's boundary IS governed** | ⚠ **`M-6` (timezone/region semantics) and `V-20` (first-response timezone and working-hour semantics). AD-01 §9.3, AD-01F §13.1 row 5 and AD-01I §10.1 row 10 each name `M-6` for exactly this. BOTH ARE OPEN** |
>
> ⚠ **THE TWO ARE RELATED AND NOT THE SAME, AND DEFERRING THE ACTION-FEED BOUNDARY TO `AC-6` WOULD ANSWER
> ONE QUESTION WITH A FACT ABOUT A DIFFERENT OBJECT — the failure mode the owner has warned against by
> name for eight consecutive documents.** ⚠ **`AC-6` IS NOT ENLARGED, NOT NARROWED AND NOT ANSWERED HERE,
> AND ITS EIGHTH-DOCUMENT DISCREPANCY NOTICE — that the owner LISTS it among already-locked decisions and
> it is NOT locked — IS CARRIED FORWARD UNCHANGED.**

### 10.4 One derived condition the model cannot currently produce

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-354` — THE OPERATION'S PRIMARY WORKING FILTER HAS NO DEFINITION.**
> ⟦SOURCE⟧ **`R-4`: the reference operation's most-used filter is `All-Without-Dump`.** **Under `H-4`
> there is no record-level `Dump` value to filter on.** **AD-01H §11.4 states in terms that its real
> meaning — *"records with no live work obligation for me"* — must be restated as a derived condition and
> that *"the Product Owner should approve it knowingly."*** ⚠ **Its exact definition depends on `V-11`'s
> control half and on the Scenario-B gap (`Z-3`: where does a record with a mandate and no live Inquiry
> APPEAR, and may a next action be committed on it?). **BOTH OPEN. NO DEFINITION IS PROPOSED HERE.**

---

## 11. CP attribution interaction

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `K`, and the highest-risk seam in the corpus: this is
> the FIRST document to check whether the ORIGINAL Lead/Inquiry architecture — built before any CP
> concept existed — is still compatible with everything the eighteen-document CP chain has locked in.
> **The check is run FOR CONTRADICTIONS, not for reassurance.**

### 11.1 The four propositions investigation `K` requires be verified

| # | Proposition | Verified? | Where |
|---|---|---|---|
| **K-a** | **CP attribution is Inquiry-scoped** | ⚠ **YES — and it was ANTICIPATED by the foundational documents rather than retrofitted** | [§11.2](#112-the-foundation-anticipated-cp-attribution-and-the-trail-is-traceable) |
| **K-b** | **Registration is CONTEXT / EVIDENCE, not automatic attribution** | ⚠ **YES, `PO LOCKED`** — `PO-AB1`, `PO-AB3`, `PO-V2·d` | [§11.6](#116-the-four-propositions-traced-against-the-foundational-text) |
| **K-c** | **Stage-1 does not determine Stage-2; a NON-CLAIMANT may receive Stage-2 commission by Site-Head decision** | ⚠ **YES, `PO LOCKED`** — `PO-W1·1 … ·9`, `PO-X1`, `PO-AA1` | [§11.6](#116-the-four-propositions-traced-against-the-foundational-text) |
| **K-d** | **The Lead / Client Sales Record is NOT the commercial attribution anchor** | ⚠ **YES — and it was eliminated on CAPABILITY grounds, not preference** | [§11.6](#116-the-four-propositions-traced-against-the-foundational-text) |

### 11.2 The foundation anticipated CP attribution, and the trail is traceable

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-355` — THE INQUIRY-LEVEL ANCHOR WAS NOT INVENTED BY AD-01J. IT WAS FORCED
> BY AD-01C's `D6` IN THE THIRD DOCUMENT OF THE SERIES, AND THE CHAIN IS UNBROKEN.**
>
> | Document | What it said | Effect |
> |---|---|---|
> | **AD-01 §3.6** | ⟦SOURCE⟧ §11 forbids collapsing attribution into lead state: claims must COEXIST; reps must NOT see competing claims; resolution is permission-gated. *"A single `source_id` on the lead represents one RESOLVED fact and is structurally incapable of representing a contested one."* | ⚠ **The original document already refused the single-value model** |
> | **AD-01C §2.2 `D6`** | One lead holding one claim set spanning two engagements cannot distinguish a genuine §11 clash from two CPs legitimately claiming two engagements — *"the decisive argument"* | ⚠ **FORCED a sub-lead commercial unit, with §32/§40 money attached** |
> | **AD-01E `U-15`** | `M-5`'s *"Lead or Person"* acquires a THIRD candidate — the Episode. *"The live financial defect"* | Candidate added |
> | **AD-01F `A-11`/`F-11`/`V-16`** | The bounded commercial unit the EVIDENCE names is the **inquiry**, not an abstract episode. `M-5` becomes a FOUR-way question | ⚠ **Candidate corrected to an entity the business already names, dates and sources** |
> | **AD-01G `G-1`/§5.1** | The Inquiry carries *"its attribution claim set"* — stated as one of its defining facts | ⚠ **The anchor is in the Inquiry's DEFINITION before AD-01J exists** |
> | **AD-01H §12 row 1** | The work record is shown **INCAPABLE** by Example 4 — two conversions, two Bookings, two entitlements, one anchor slot | ⚠ **Candidate ELIMINATED on capability** |
> | **AD-01J / AD-01K `K-18`** | *"`V-16`·claim-anchor and `M-5`'s attribution half: **THE INQUIRY, AND NOTHING ELSE.**"* | ⚠ **Confirmed INDEPENDENTLY against a test set including three cases AD-01J did not run** |
>
> ⚠ **CONCLUSION: the original architecture did NOT assume a simpler commercial model that the CP chain
> later complicated. It DIAGNOSED the complication, in AD-01 §3.6 and AD-01C `D6`, before a CP document
> existed — and the CP chain's anchor is the foundation's own conclusion carried forward.** **That is a
> CONFIRMATION and it is recorded as deliberately as a contradiction would be.**

### 11.3 `X-35` — the status contradiction at the seam

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-356` — AND HERE IS WHERE THE SEAM IS DEFECTIVE. THE MODEL IS SOUND; ITS
> RECORDED STATUS IS NOT.**

**The two sides, quoted at their own documents:**

| Side | What it says |
|---|---|
| ⚠ **AD-01L §2.2** | **`V-1`** — ⟦PRODUCT-OWNER DECISION⟧ *"AD-01G — **Lead = the work record; Inquiry = the commercial unit.**"* → ***"Approved. NOT reopened."*** **`V-2`/`V-3`** — ⟦PRODUCT-OWNER DECISION⟧ *"AD-01H — concurrent inquiry existence is valid; lifecycle is Inquiry-level (Model D, `H-4`)."* → ***"Approved. NOT reopened."*** **`V-11`/`V-12`** — ⟦PRODUCT-OWNER DECISION⟧ *"AD-01I — Dump decomposes…"* → ***"Approved. NOT reopened, NOT redefined."*** |
| ⚠ **AD-01P §3** | Table rows citing ***"`V-1` (approved)"*** as the basis for the Lead / Inquiry fact allocation |
| ⚠ **AD-01G** | `STATUS: PROPOSED — NOT APPROVED` · `G-1` is an ⟦ARCHITECT RECOMMENDATION⟧ *"**NOT APPROVED. NOT APPROVED FOR IMPLEMENTATION.** Nothing below is decided"* · §1.4 records the owner's preference for Model B and **deliberately sets it aside**, on the ground that *"a recorded preference is evidence about the operation, never a warrant for an architecture"* |
| ⚠ **AD-01H** | `STATUS: PROPOSED — NOT APPROVED` · `H-4` is an ⟦ARCHITECT RECOMMENDATION⟧ · `A-1` names an amendment to a PO decision that **only the Product Owner may make** |
| ⚠ **AD-01I** | `STATUS: PROPOSED — NOT APPROVED` · §1.5: *"**AD-01G `G-1`, `G-2`, `G-8`, `G-9` and AD-01H `H-1` are treated as INPUT.**"* · `I-1` is an ⟦ARCHITECT RECOMMENDATION⟧ *"NOT APPROVED"* |
| ⚠ **AD-01K §19** | Lists **`V-1 … V-15`** among items *"**not resolved**"* — ⚠ **which is a THIRD status for the same items, in a document written between AD-01L's two neighbours** |

> ⚠ ⟦BUSINESS DECISION REQUIRED — `X-35`, MINTED HERE⟧ **`V-1`, `V-2`/`V-3` and `V-11`/`V-12` — the
> Lead/Inquiry split, Inquiry-level lifecycle and the `Dump` decomposition — are recorded at THREE
> incompatible statuses across the corpus: `PO LOCKED` and *"Approved"* (AD-01L §2.2, AD-01P §3),
> `ARCHITECT DERIVED` and *"NOT APPROVED"* (AD-01G, AD-01H, AD-01I, at their own mints), and *"not
> resolved"* (AD-01K §19). **Eighteen CP documents carry the standing clause *"`V-1`, `V-2`, `V-3`,
> `V-7`, `V-11`, `V-12` … are NOT reopened"*, which is a SCOPE rule and reads as a STATUS claim.**

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **Why this is a CONTRADICTION and not a drafting slip, tested explicitly on
> AD-01AB's own `SUPERSEDED`-versus-`CONTRADICTED` standard:**
>
> | Test | Result |
> |---|---|
> | **Is it a SUPERSESSION?** | ⚠ **NO. A thing is superseded when the owner supersedes it, and the owner has not said so. No `PO-` clause anywhere resolves `C-1`, approves `G-1`, or ratifies `H-4` or `I-1`** |
> | **Is it a CONTRADICTION?** | ⚠ **YES. Two documents assert a status that three documents, at their own mints, deny — and both assertions are live and relied upon** |
> | **Is it harmless?** | ⚠ **NO. `K-18` (the attribution anchor), `P-G` (the ten-step claim sequence), the Stage-1 scoping rules, `NF-`'s eligibility tests, `C-XXI … C-XXV` and `M-9`'s entire input list PRESUPPOSE the Inquiry as a commercial object. If `V-1` were ever resolved toward Model A, AD-01G `G-4`, AD-01H `H-10`, AD-01I `I-9`, AD-01J `J-52` and AD-01K all state — in terms, five times — that the Sales Episode rejection **does not stand**. **The conditionality is recorded five times and the status that would trigger it is recorded as already settled*** |
> | **Is it owned by an existing item?** | ⚠ **NO. `Q0-a … Q0-e` are CP-specific ratifications; `Q0-e` ratifies AMENDMENTS. §19.4 item 12's approve/reject list runs AD-01N onward and does not reach AD-01G/H/I** |
>
> ⚠ **`A-81` — the editorial correction to AD-01L §2.2's and AD-01P §3's status labels — IS NAMED AND IS
> NOT MADE. NO PRIOR DOCUMENT IS EDITED BY THIS ONE.** ⚠ **The SUBSTANTIVE question is the owner's and is
> minted as `AC-49`** — [§19.4](#194-ac-49-minted).

### 11.4 `X-28`'s true extent, and its origin

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-357` — `X-28` IS RECORDED AT TEN PLACES ACROSS EIGHT DOCUMENTS. ITS
> ORIGIN IS OLDER THAN ITS EARLIEST RECORDED INSTANCE, AND THE REGION CONTAINING THE ORIGIN HAS NEVER
> BEEN SEARCHED.**
>
> **`X-28`'s form, as AD-01Z states it:** *"a resolved / winning / attributed claim standing as the
> determinant of entitlement, payability or eligibility."* **Its recorded instances begin at AD-01H §8.2
> and run through AD-01J, AD-01K, AD-01M, AD-01N, AD-01Q, AD-01R, AD-01S, AD-01T and AD-01AA's `NF-25`.**
> ⚠ **Every hunt that produced that list searched the CP chain and AD-01H. NOT ONE searched `03`, `03a`,
> `03c`, `03e` or `03f` — the documents in which the formulation was MINTED.**

| # | Instance, in the unsurveyed region | Document | Form |
|---|---|---|---|
| **1** | ⚠ ***"If that lead converts before reconciliation, commission eligibility (§32/§40) is GATED ON THE UNRESOLVED CLAIM — which is correct"*** | ⚠ **AD-01 §7 `E-12`** | ⚠ **Claim-resolution state as the determinant of payability** |
| **2** | ⚠ ***"Commission (§32) must key on the RESOLVED claim, not on the lead's state"*** | ⚠ **AD-01 §7 `E-13` — THE ORIGIN** | ⚠ **The canonical statement of the defect** |
| **3** | *"`E-13` already establishes that commission must key on the resolved claim"* | AD-01C §2.2 | Restatement |
| **4** | *"`E-13` already establishes that commission keys on the resolved claim, not on lead state"* | AD-01C §3.2 | Restatement |
| **5** | *"Must key on the **resolved claim of the converting episode**"* | AD-01E §9 | Restatement, relocated |
| **6** | *"Attribution and commission must key on the episode"* (`R6`) | AD-01E §11 | Restatement, relocated |
| **7** | *"Attribution claims and commission must key on the bounded commercial unit"* (`A-11`) | AD-01F §3 | Restatement, relocated |
| **8** | *"Attribution and commission key on the inquiry"* (`F-11`) | AD-01F §15 | Restatement, relocated |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **WHAT THE OWNER'S OWN RULINGS SAY AGAINST IT, THREE TIMES OVER:**
> ⟦PRODUCT-OWNER DECISION⟧ **`PO-W1·3`** — Stage-1 does not restrict Stage-2. **`PO-X1·5`** — a
> non-claimant may be searched, selected, linked and allocated. **`PO-Y1.F`** — the non-gate rule again.
> **`PO-AA1`** — the Site Head's Stage-2 allocation is SUFFICIENT for commission entitlement and NO
> Stage-1 claim is required. **`PO-AB3`** — registration status is not itself the commission decision.
> ⚠ **Under those five clauses an UNRESOLVED Stage-1 clash does not gate Stage-2 at all, and a RESOLVED
> Stage-1 claim does not determine it either.** ⚠ **AD-01 `E-12` and `E-13` are wrong in the same way
> the ten recorded instances are wrong — and they are where the sentence came from.**

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **THE DETERMINATION, STATED ON THE SERIES' OWN STANDARDS.**
>
> - ⚠ **`X-28` IS ENLARGED AND IS NOT RENUMBERED, NOT RE-MINTED AND NOT RESTATED.** AD-01Y's standard —
>   *"enlarging a contradiction's population is not minting a new one"* — applied to its own contradiction.
> - ⚠ **THE EXTENT IS NOT RE-COUNTED FROM SCRATCH ON THE CP SIDE.** AD-01Z's precedent is followed
>   exactly: the recorded ten are NEITHER confirmed as exact NOR contradicted. **What is established is
>   that AT LEAST EIGHT FURTHER INSTANCES exist in FOUR documents no hunt has surveyed, taking the total
>   to AT LEAST EIGHTEEN across AT LEAST TWELVE documents.**
> - ⚠ **THE MORE IMPORTANT FINDING IS NOT THE COUNT. IT IS THE ORIGIN.** **`X-28` has been treated as a
>   defect the CP chain introduced and must unwind. **It is a defect the CP chain INHERITED from AD-01
>   `E-13`, whose reading of ⟦SOURCE⟧ §32/§40 was made before the two-stage model existed and was
>   reasonable when written.** **A defect with an origin is discharged differently from a defect with a
>   population: the origin's restatements will regenerate it every time a document cites `E-13`.**
> - ⚠ **`A-82` — the amendment covering all eight foundational instances, in ONE entry to avoid
>   double-counting, on AD-01AA's own anti-double-counting precedent — IS NAMED AND IS NOT MADE.**
> - ⚠ **`A-62`, `A-63`, `A-66`, `A-68` and `A-78` — the existing `X-28` remedies — are UNCHANGED, and
>   none is applied.**

### 11.5 The Dimension-A coupling

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-358` — A MONEY RULE IS LOCKED OVER A CLASSIFICATION WHOSE VOCABULARY DOES
> NOT EXIST AND WHOSE SCOPE IS UNDECIDED. THIS IS THE SEAM'S SECOND REAL DEFECT AND IT RUNS FROM THE CP
> SIDE INTO THE FOUNDATION.**
>
> | Layer | What is locked or open |
> |---|---|
> | ⚠ **The money rule** | ⟦PRODUCT-OWNER DECISION⟧ **`PO-R1` — `AC-14` = Option A: a registration SURVIVES an invalid-side closure. `PO LOCKED`** |
> | **The adjacent architect rule** | `K-22`/`L-6`: a closure on **Dimension A's invalid side** admits NO citation, as a class, because the citation would have to assert both that the ground exists and that the business classified it as not existing. ⚠ **`ARCHITECT DERIVED`; `Q0-c` asks the owner to ratify or reject it and is OPEN** |
> | ⚠ **The classification it reads** | **AD-01A §8.3 Dimension A — *opportunity validity class*. `PO LOCKED` as a DIMENSION** |
> | ⚠ **Its values** | ⚠ **`N-4` — OPEN. ZERO values approved. The twenty-second consecutive document to record it** |
> | ⚠ **Its SCOPE** | ⚠ **`Z-5` — does a closure speak about this ENGAGEMENT or about this whole CLIENT RELATIONSHIP? OPEN, and AD-01I §3.6 found the framework's own two design standards DISAGREE about whether scope should be a fourth dimension or a rule read over Dimension A** |
> | ⚠ **Its operational consequence in the Lead domain** | ⚠ **`Z-2` — does validity class CONSTRAIN redistribution eligibility? OPEN** |
>
> ⚠ **THE COUPLING, STATED PLAINLY: the CP chain has made Dimension A LOAD-BEARING FOR MONEY — `PO-R1`
> locked over it, `K-22`/`Q0-c` reading it, `T-4`'s answerability gated on it (AD-01J `C-I`) — while the
> Lead domain, which OWNS it, has neither its values (`N-4`) nor its scope (`Z-5`) nor its redistribution
> consequence (`Z-2`).** ⚠ **AD-01J recorded `N-4` as *"the third document to find `N-4` blocking
> something downstream"*; this is at least the fifth. **NOTHING IS PROPOSED. NO VALUE, NO DIMENSION, NO
> SCOPE RULE. The coupling is NAMED, and `N-4`, `Z-2`, `Z-5`, `Q0-c` and `T-4` all stay exactly where
> they are.**

### 11.6 The four propositions traced against the foundational text

| Proposition | Does the ORIGINAL Lead architecture assume something incompatible? | Result |
|---|---|---|
| ⚠ **K-a — attribution is Inquiry-scoped** | ⚠ **NO — it FORCED it** (§11.2). ⚠ **One residue: AD-01 §5.1's attribution axis is *"a property of the claim CONTEST — Uncontested · Contested · Resolved"*, a TRI-VALUED single track that ends in a winner. **The CP chain has TWO decisions at TWO levels, and `C-XXV` makes the NULL OUTCOME (Direct / no CP) a POSITIVELY RECORDED Booking-level fact.** *"Resolved"* does not entail *"someone won"* | ⚠ **COMPATIBLE IN ANCHOR; AD-01A §8.6's ENUMERATION IS INCOMPLETE — `A-83`, named and not made** |
| ⚠ **K-b — registration is evidence, not automatic attribution** | ⚠ **NO — the foundation never modelled registration at all.** ⟦SOURCE⟧ §06's canonical list has `Lead Attribution Claim` and no registration object. **The CP chain added an entity with its own window, expiry, revisit and immutability rules** | ⚠ **COMPATIBLE — and it is a TENTH object the nine-component Orthogonal Lead Model does not enumerate. `A-83`** |
| ⚠ **K-c — Stage-1 ≠ Stage-2; a non-claimant may receive** | ⚠ **YES, IN TWO PLACES, AND THIS IS `X-28`.** AD-01 `E-12`/`E-13` gate commission on the claim's resolution state | ⚠ **`CONTRADICTED` — `X-28` ENLARGED; `A-82`** |
| ⚠ **K-d — the work record is not the commercial anchor** | ⚠ **NO.** AD-01 §3.6 refused the single-value model; AD-01H §12 row 1 eliminated the record on capability | ⚠ **COMPATIBLE, CONFIRMED** |

### 11.7 The question investigation `K` asks in its sharpest form

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **Did the original architecture assume that ONE INQUIRY HAS ONE CLEAN
> DISPOSITION, in a way that sits awkwardly with the CP chain's finding that one Inquiry can carry
> multiple competing claims, a Site-Head override and a Stage-2 allocation that may not track the Stage-1
> outcome at all? **The check, run at source:**
>
> | Layer | What the foundation says | Compatible? |
> |---|---|---|
> | **Multiple competing claims on one Inquiry** | ⟦SOURCE⟧ **§11 requires claims to COEXIST**, and AD-01 §3.6 says a single-valued column *"cannot represent"* a contested fact. AD-01 §8.4 makes `Lead Attribution Claim` a canonical multi-row entity | ⚠ **YES — anticipated in the FIRST document** |
| **A Site-Head override** | ⟦SOURCE⟧ §11 makes resolution *"a permission-gated action, not an edit"*, performed by **authorized leadership**. `PO-Q1`/`PO-Q2` lock the override with a mandatory reason and evidence | ⚠ **YES — the foundation named the actor class and the CP chain named the authority** |
| **A Stage-2 allocation that does not track Stage-1** | ⚠ **NO PROVISION AT ALL.** The foundation models ONE adjudication ending in a resolved claim. **The second decision does not exist in it** | ⚠ **NOT ANTICIPATED — and the gap shows as `X-28`, not as a structural impossibility. The Inquiry can carry both; what the foundation lacks is the STATEMENT that the second decision exists** |
| **One Inquiry, one *"clean disposition"*** | ⚠ **The foundation's `H-4` gives the Inquiry ONE commercial disposition — converted or closed-with-reason — and that is CORRECT and unaffected.** ⚠ **The CP chain's two-stage model attaches to the CONVERSION's Booking, not to the disposition. They are different axes** | ⚠ **YES — and this is the answer to the question: the awkwardness is in the ENUMERATION, not in the MODEL** |

> ⚠ **THE FINDING, STATED ONCE: the foundational architecture is STRUCTURALLY ADEQUATE for everything the
> CP chain has locked in. What it lacks is (i) a statement that a SECOND commercial decision exists at the
> Booking level (`A-83`), and (ii) the removal of the sentence that says the FIRST one determines money
> (`X-28`, `A-82`).** ⚠ **NEITHER IS MADE HERE.**

---

## 12. Duplicate and clash

> ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `L`: duplicate-detection must not be a
> commercial-attribution decision, and a CP clash must not be an automatic commission winner.**

### 12.1 The two questions, confirmed different at different levels

> ⟦SETTLED — AD-01H §12 row 2, AD-01G `T8`⟧ **⟦SOURCE⟧ §09 DUPLICATE detection asks *"is this the same
> human?"* — a Person / record question, owned by `M-5`. ⟦SOURCE⟧ §11 CLASH asks *"do two producers claim
> this engagement?"* — an Inquiry question. **Different subjects, different levels, different actors,
> different authorization.**

| Check | Result |
|---|---|
| Does duplicate detection decide attribution? | ⚠ **NO. It decides identity. A merge (`Q10`) decides which RECORD survives; the claims must SURVIVE the merge and remain individually resolvable (⟦SOURCE⟧ §11; AD-01 `E-03`)** |
| Does a clash decide a commission winner? | ⚠ **NO, AND THIS IS NOW LOCKED ON THE CP SIDE.** ⟦PRODUCT-OWNER DECISION⟧ `PO-AB2`: a competing CP registration creates a CLASH; **the Site Head reads both histories and decides among FOUR outcomes — CP-A, CP-B, split, or Direct/no-CP — and no additional outcome is to be invented.** **A clash is an INPUT to a decision, never the decision** |
| Does the Lead domain contradict that? | ⚠ **NO — except through `X-28`.** AD-01 §3.6's *"resolution is a permission-gated action"* is exactly right; `E-12`/`E-13`'s *"commission is gated on the claim"* is the defect |

### 12.2 The open items this area carries, none answered

| # | Item | Status |
|---|---|---|
| **1** | ⚠ **`M-5`'s uniqueness half** — what is a record's uniqueness boundary, and at what level does clash detection operate | ⚠ **OPEN. `X-1` records it as `Q10`'s HARDEST prerequisite** |
| **2** | **`Q10`** — merge state precedence; which record, handler, owner and next-action date survive; what happens to the losers' claims | ⚠ **OPEN, and ENLARGED three times: by `T-3` (successor vs merge linkage), by AD-01H (what happens to two records' INQUIRY SETS on merge), and by AD-01I (what happens to two records' MANDATES on merge). NONE answered** |
| **3** | **`N-1`** — when does the duplicate/clash gate run for an ONLINE-created record: synchronously at save, or asynchronously? | ⚠ **OPEN. ⟦SOURCE⟧ §09 requires the control and the spec never states the timing** |
| **4** | **`Q11`** — may a claim be filed against an already-converted record, and is there a CUT-OFF? | ⚠ **OPEN, AND RECORDED AS *STILL BLOCKING* IN THE CP CHAIN'S OWN GATING LIST FOR NINETEEN CONSECUTIVE DOCUMENTS** |
| **5** | **`T-1`** — must detection recognise a declared SUCCESSOR as a successor rather than an unresolved duplicate? | ⚠ **AD-01E §2.2 `D10` records it DISCHARGED under revival (no new record is created) and replaced by a different requirement: **detection must recognise an inbound re-contact matching a closed Inquiry and route it toward a NEW INQUIRY rather than a new RECORD.** **The replacement requirement is unowned by any register item** |
| **6** | ⚠ **`AC-20`** — competing registrations over one (client, project) | ⚠ **CLOSED by `PO-V2·a` on the CP side. Recorded so it is not re-opened by adjacency** |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`Q11` IS THE OLDEST BLOCKING ITEM IN THE CORPUS AND IT IS A LEAD-DOMAIN
> ITEM.** **It was minted at AD-01 §10 as an edge case (`E-13`) and the CP chain's gating list has carried
> it as *"STILL BLOCKING"* without interruption.** ⚠ **Recorded as a fact about the ledger, not as a
> recommendation to answer it now — `T-4`, `T-5` and `AC-26` sit around it and AD-01K has already
> recommended the sitting at which they are taken together.**

---

## 13. Manager redistribution

> ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `M`: ten scenarios, each asked whether it PRESERVES or
> CREATES a commercial Inquiry, changes the handler, changes visibility, or changes a reporting
> denominator. **The governing prohibition: an ownership change must NEVER create a commercial
> opportunity.**

### 13.1 The governing findings, at their mints

> ⟦SETTLED — AD-01I `I-6`, `I-7`, `I-8`⟧ **A redistribution NEVER creates an Inquiry, NEVER creates an
> attribution claim, NEVER carries a source, campaign, channel or project, and NEVER enters the
> conversion denominator. What it creates is a NEW CUSTODY INTERVAL and, optionally, a recorded WORK
> MANDATE on the Assignment Log.**

> ⟦SETTLED — AD-01I §4.1⟧ **Redistribution is NOT *"reopening"* anything, and the question dissolves: the
> work record never closed, so there is nothing to reopen; the Inquiry is closed and stays closed.**
> ⚠ **`Q0-b` — *"a manager reassigning a file can never create, move or affect a commercial claim"* — is
> the CP-side statement of the same proposition, recorded by AD-01N as *answered in substance by `PO-N4`*
> and STILL AWAITING FORMAL RATIFICATION.**

### 13.2 The ten scenarios

| # | Scenario | Creates an Inquiry? | Changes handler? | Changes visibility? | Changes a reporting denominator? |
|---|---|---|---|---|---|
| **M1** | One closed Inquiry; manager redistributes to a fresher; no new Inquiry | ⚠ **No** | **Yes** | ⚠ **Only if a conferral is recorded (`G-7`); `V-8` open** | ⚠ **Workload denominators only. NEVER Capture or conversion** |
| **M2** | The fresher starts calling; work resumes with no new Inquiry | ⚠ **No** | No | No | ⚠ **`custody→first-response` starts; FUT accrues. `V-4` gates both** |
| **M3** | Customer generates a new Inquiry; manager assigns a new handler | ⚠ **Yes — by the CUSTOMER act, not the management act** | **Yes** | Per the transfer's conferral | ⚠ **Capture: YES for the Inquiry. The assignment contributes nothing** |
| **M4** | Two Inquiries, both closed; manager redistributes before any new Inquiry | ⚠ **No** | **Yes** | Per conferral | ⚠ **Workload only. ⚠ `Z-3`: WHERE the record appears, and whether a next action may be committed, is OPEN** |
| **M5** | Inquiry A = Success, Inquiry B = Dump; manager redistributes the record | ⚠ **No** | **Yes** | Per conferral | ⚠ **BLOCKED ON `Y-4` — may a record holding a Success be redistributed into a sales churn queue? OPEN, and AD-01I refines it: `Y-4` is a management-AUTHORIZATION question, not only a queue-PRESENTATION one** |
| **M6** | **Bulk** redistribution of many records in one act | ⚠ **No** | **Yes, for all** | ⚠ **`V-8`: does one decision apply to all? OPEN. AD-01G §10.2 requires the history decision PER RECORD even in a bulk act** | Workload only |
| **M7** | **Absorption of a departed employee's book** (⟦SOURCE⟧ §57) | ⚠ **No** | **Yes** | ⚠ **`Z-6`: is management INTENT uniform across the batch or per record? OPEN — §57's case BREAKS a uniform intent, because live and closed records move in one act** | Workload only |
| **M8** | Redistribution of a record whose only closures are **invalid-side** | ⚠ **No** | **Yes** | Per conferral | ⚠ **`Z-2`: may it happen at all? OPEN. `I-10` names the eternal-redistributability hazard** |
| **M9** | A re-attempt ends with no new Inquiry and no result | ⚠ **No** | No | No | ⚠ **`Z-4`: is a reasoned outcome recorded, and on WHAT? OPEN. Two homes named, NEITHER adopted. Closing the existing Inquiry is FORBIDDEN** |
| **M10** | Redistribution back to a handler who **previously held the record** | ⚠ **No** | **Yes** | ⚠ **`W-3`'s eighth case if made WITHOUT history; `V-25` decides whether `I1` endures if made with** | Workload only |

### 13.3 What the ten establish

> ⟦ARCHITECT ANALYSIS⟧ **`PF-359` — TEN SCENARIOS, ZERO COMMERCIAL INQUIRIES CREATED, AND THE
> PROHIBITION HOLDS WITHOUT EXCEPTION.** ⚠ **In every one of the ten, an ownership change creates no
> commercial opportunity. `I-6` is not merely a policy — AD-01J `J-16`/`J-42` show it is STRUCTURAL: a
> management act has no source, no channel, no project and no claimant, so it has **nowhere to put a
> commercial fact.** **CONFIRMED from a second direction.**

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **AND WHAT THE TEN ALSO ESTABLISH: SIX OF THE TEN ARE BLOCKED OR
> INDETERMINATE.** ⚠ **M4 on `Z-3`; M5 on `Y-4`; M6 on `V-8`; M7 on `Z-6`; M8 on `Z-2`; M9 on `Z-4`; M10
> on `W-3`/`V-25`. **The single most routine management operation in the described business — the one the
> Product Owner described first and in the most concrete terms — is EXPRESSIBLE under the model and
> PERFORMABLE in only three of ten described cases.** **That is reported as the state of the domain, not
> as a request to decide seven items now.**

---

## 14. Reporting semantics

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `N`: thirteen named metrics, each checked for an
> UNDEFINED DENOMINATOR. **NO DENOMINATOR, GRAIN, ROLLUP, OWNERSHIP RULE OR ATTRIBUTION CONVENTION IS
> INVENTED, PROPOSED, PREFERRED OR RANKED ANYWHERE BELOW.** **Unresolved CP-side items are carried into
> `AC-42` rather than duplicated as new ones.**

### 14.1 The thirteen metrics

| # | Metric | Is its denominator defined? | Owner of the gap |
|---|---|---|---|
| **1** | **Leads captured in a period** | ⚠ **NO — *"leads"* is three different numbers** | ⚠ **`Y-5`** (Lead count / Inquiry count / project-interest count) **+ `V-18`** (does a re-inquiry count as a Capture) |
| **2** | **Conversion rate** | ⚠ **NO — the funnel's KEY is undecided** | ⚠ **`V-17`** (record or Inquiry). ⚠ **FORCED IN DIRECTION by `H-4`/`H-8` and still NOT RESOLVED — it is a SOURCE-OF-TRUTH change and ⟦SOURCE⟧ §88 MUST-ASK |
| **3** | **Loss analysis by reason** | ⚠ **NO — no reason values exist** | ⚠ **`N-4`. AD-01 §9.6 stands: reason-coded loss analysis must not be shipped as charts** |
| **4** | **Source quality** | ⚠ **NO, twice over** | ⚠ **`N-4` (the denominator must exclude *Invalid* and *Administrative*) **+ AD-01 §9.6's second defect: channel SOURCE and CP ATTRIBUTION are two different facts and one field cannot carry both** |
| **5** | **Handler workload** | ⚠ **PARTLY** | ⚠ **The record is the unit (AD-01G §4.1) — but `I-7`'s asymmetry means *live Inquiry count* is a LOWER BOUND on *records being worked*, never equal. **Any report treating them as interchangeable understates workload by exactly the redistribution population** |
| **6** | **FUT / follow-up volume** | ⚠ **NO** | ⚠ **`V-4`** (what counts) **+ `W-1`** (how a two-project call counts) |
| **7** | **First-response time** | ⚠ **NO** | ⚠ **`V-4`** + ⚠ **`V-20`/`W-4`** (four clocks, none selected) + ⚠ **`M-6`** (timezone / working hours) |
| **8** | **Overdue / escalation volume** | ⚠ **PARTLY** | ⚠ **Derived from next-action timing (`PO LOCKED`), but `N-2` owns which *waiting-on* values SUPPRESS escalation and `Q14` owns the thresholds. BOTH OPEN; `X-6` records the dependency** |
| **9** | **Velocity / time-in-state** | ⚠ **NO** | ⚠ **`V-17`'s key decides what a cycle is measured over; AD-01 `E-23` requires EFFECTIVE and RECORDED time held separately; `M-6` open** |
| **10** | **Project pipeline / project-scoped reporting** | ⚠ **NO** | ⚠ **`V-24`** — does ⟦SOURCE⟧ §08 project scope evaluate against the record or the Inquiry? **A viewer scoped to Project A seeing a record that also holds a Project B interest is undefined** |
| **11** | **Manager effectiveness of redistribution** | ⚠ **NO — AND THE INPUT DOES NOT EXIST** | ⚠ **`Z-4`: a re-attempt that produces nothing records no reasoned outcome anywhere. Dead number, hostile customer and a fresher who never called are INDISTINGUISHABLE. **Unrecoverable under `R6` if not recorded at the time** |
| **12** | **CP performance** | ⚠ **NO — NINE LIMBS** | ⚠ **`AC-42`, OPEN IN NINE LIMBS, ALL NINE CARRIED, NONE ANSWERED, NO TENTH ADDED HERE.** ⚠ **Limbs 6, 7, 8 and 9 require facts the corpus records as UNSTORABLE (`X-25`/`A-37`, `A-55`)** |
| **13** | **Registration activity / status** | ⚠ **NOT AN `AC-42` LIMB, AND IT IS NOT MADE ONE HERE** | ⚠ **AD-01AB determined it is a DECISION-SUPPORT surface for the Site Head, not a performance report — *"it counts nothing and ranks nobody."* **Its active/expired boundary is `AC-6`'s. **CARRIED, NOT FOLDED IN** |

### 14.2 The carry rule, honoured explicitly

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-360` — `AC-42` IS NOT ENLARGED BY THIS DOCUMENT AND NO TENTH LIMB IS
> ADDED.** **Metrics 12 and 13 are CP-side and land on `AC-42` exactly as AD-01AB left them. Metrics
> 1–11 are Lead-side and land on `Y-5`, `V-17`, `V-18`, `V-4`, `V-20`, `W-1`, `W-4`, `V-24`, `N-2`, `N-4`,
> `Q14`, `Z-4` and `M-6` — **every one of which already exists.** ⚠ **ZERO NEW REPORTING ITEMS ARE
> MINTED. Minting one for a question an existing item already owns is how a register becomes unusable —
> AD-01I §13.2's own standard.**

### 14.3 The four constraints on all of the above, carried unchanged

| # | Constraint | Basis |
|---|---|---|
| **1** | ⚠ **No report may filter on a state's CODE; every report branches on the SEMANTIC column** | ⟦SOURCE⟧ `R4`; AD-01 §9.7 |
| **2** | ⚠ **A workload act must NEVER enter a commercial denominator** | `I-1` C1/C2; AD-01 §9.1 |
| **3** | ⚠ **A re-engagement must NEVER restate a closed period** | AD-01F §12.4; `C-II`; confirmed by AD-01K |
| **4** | ⚠ **EFFECTIVE time and RECORDED time must be held separately** | AD-01 `E-23`; ⟦SOURCE⟧ §47 |

---

## 15. Security and authorization

> ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `O`: thirteen named operations, cross-checked against
> tenant / RLS rules, **without assuming RBAC alone suffices where SCOPE or ENTITLEMENT is required.**

### 15.1 The three-layer test applied to every operation

> ⟦ARCHITECT ANALYSIS⟧ **Every operation is checked on three layers, because the corpus shows repeatedly
> that satisfying one does not satisfy the others:**
>
> - **Layer 1 — TENANT.** ⟦SOURCE⟧ `R1`: fail-closed, an unset context matches ZERO rows, never
>   everything. **Applies to every table and every master without exception.**
> - **Layer 2 — PERMISSION.** ⟦SOURCE⟧ `R2`: union semantics, no deny rows, **and no logic may branch on
>   a ROLE NAME.** ⚠ **`M-3` (the default role set) is OPEN, so no grant can yet be EXPRESSED.**
> - **Layer 3 — SCOPE / ENTITLEMENT.** Record-visibility BREADTH on the grant (`M-7`, OPEN), ⟦SOURCE⟧ §08
>   PROJECT scope (`V-24`, OPEN), and the custody/interest ENTITLEMENT projection (`G-6`, unapproved).

| # | Operation | Layer 1 | Layer 2 | Layer 3 | Verdict |
|---|---|---|---|---|---|
| **1** | View a work record | `R1` | Grant | ⚠ **Breadth (`M-7`)** | ⚠ **RBAC alone INSUFFICIENT** |
| **2** | View a **prior custody interval's** history | `R1` | Grant | ⚠ **`I1 ∪ I2 ∪ I3`, fail-closed (`G-6`)** | ⚠ **RBAC alone INSUFFICIENT — and this is the paradigm case** |
| **3** | View an **Inquiry** on a project the viewer is not scoped to | `R1` | Grant | ⚠ **⟦SOURCE⟧ §08 (`V-24`, OPEN)** | ⚠ **UNDEFINED** |
| **4** | View **clash information** | `R1` | Grant | ⚠ **⟦SOURCE⟧ §11 restricts it from reps; `PO-V2·c` locks non-disclosure on the CP side; engagement-scoped per AD-01H §12 row 3** | ⚠ **RBAC alone INSUFFICIENT** |
| **5** | **Disposition** an Inquiry (close with a reason) | `R1` | ⚠ **`Q16`, OPEN** | — | ⚠ **UNDEFINED — and `Q16`'s calculus SPLIT: `Dump` got cheaper to get wrong, `Success` got more expensive** |
| **6** | **Set Success** | `R1` | ⚠ **`V-22`/`T-6`, OPEN — handler assertion or system derivation?** | Booking authorization | ⚠ **UNDEFINED** |
| **7** | **Transfer** a record | `R1` | ⚠ **`M-3`/`M-7`, OPEN — expressed as BREADTH, never a role name (`R2`)** | Reporting hierarchy | ⚠ **UNDEFINED IN EXPRESSION** |
| **8** | Transfer **with history** (confer `I3`) | `R1` | ⚠ **`M-3`/`M-7`** | ⚠ **`V-8`: mandatory? default? revocable? by whom?** | ⚠ **UNDEFINED** |
| **9** | **Redistribute** with a work mandate | `R1` | ⚠ **`M-3`/`M-7`; `I-2` fixes the ACTOR CLASS as management** | ⚠ **`Z-2`: may validity class BLOCK it?** | ⚠ **UNDEFINED** |
| **10** | **Merge** duplicates | `R1` | ⚠ **`Q10`, OPEN** | `M-5` | ⚠ **UNDEFINED** |
| **11** | **Correct** a wrongly-recorded disposition | `R1` | ⚠ **`T-7`, OPEN — no correction-reason vocabulary exists** | ⚠ **`U-18`: revival and correction act on the same record with OPPOSITE reporting effects and must be distinguishable IN AUTHORIZATION** | ⚠ **UNDEFINED, and it is the most dangerous of the thirteen** |
| **12** | **Resolve** an attribution clash | `R1` | ⚠ **⟦SOURCE⟧ §11: authorized leadership. `PO-Q1` locks the override with a MANDATORY reason and evidence** | ⚠ **`PO-Q2`'s full reconstructability is recorded NOT YET SATISFIABLE — `X-25`/`A-37`** | ⚠ **PARTLY LOCKED; the SUBSTRATE is missing** |
| **13** | **CP portal read** (⟦SOURCE⟧ §39) | ⚠ **`M-2` — are CPs users of the builder's tenant at all? OPEN** | ⟦SOURCE⟧ §05 restricts CP access | ⚠ **`U-16`; `AC-13`'s six limbs plus limb (viii)** | ⚠ **UNDEFINED, and it crosses a TENANT boundary** |

### 15.2 What this section establishes

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-361` — ELEVEN OF THIRTEEN OPERATIONS CANNOT CURRENTLY BE EXPRESSED, AND
> THE REASON IS NOT THE SAME IN EACH CASE.** ⚠ **Two causes must not be conflated:**
>
> | Cause | Operations | Remedy |
> |---|---|---|
> | ⚠ **The VOCABULARY does not exist** — `M-3` (roles), `M-7` (breadth) | 1, 2, 7, 8, 9 | ⚠ **`M-3`/`M-7`, OPEN. `R2` forbids the shortcut of branching on a role name, so this cannot be worked around** |
| ⚠ **The RULE does not exist** — nobody has decided who may do it | 5, 6, 10, 11 | ⚠ **`Q16`, `V-22`, `Q10`, `T-7`, OPEN** |
| ⚠ **The SCOPE BOUNDARY is undecided** | 3, 4, 13 | ⚠ **`V-24`, `U-16`, `AC-13`, `M-2`, OPEN** |
| ⚠ **The SUBSTRATE is missing** | 12 | ⚠ **`X-25`/`A-37` — ⟦SOURCE⟧ `NF-27`'s missing audit events, priority raised NINE consecutive times, SIX owner-stated permanences resting on it** |
>
> ⚠ **NO ROLE, GRANT, BREADTH VALUE, SCOPE RULE OR PERMISSION IS PROPOSED ANYWHERE. The authorization
> model is ⟦SOURCE⟧ §88 MUST-ASK in every one of its parts.**

### 15.3 Two security properties that ARE settled, recorded because they are

| Property | Basis | Status |
|---|---|---|
| ⚠ **Restricted history is enforced at the AUTHORIZATION boundary, never by hiding UI** | ⟦PRODUCT-OWNER DECISION⟧ consolidated §6, §17 (*"must not be implemented solely by hiding UI elements"*); ⟦SOURCE⟧ §11, §45, Spec Rule 4, §03 | ⚠ **`PO LOCKED` in principle** |
| ⚠ **Search must NEVER become a side door around authorization** | ⟦SOURCE⟧ §45 | ⚠ **`PO LOCKED`.** **Its APPLICATION to derived aggregates is `V-9`/`V-10`, OPEN** |

---

## 16. Offline and concurrency

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `P`: ten scenarios, **identifying MISSING DECISIONS
> ONLY. NO CONFLICT-RESOLUTION POLICY IS INVENTED, PROPOSED, PREFERRED OR RANKED ANYWHERE BELOW.**

### 16.1 The baseline

> ⟦PRODUCT-OWNER DECISION — AD-01A §8.4⟧ ***Sync and verification are SEPARATE concepts. Successful
> transport synchronization must NEVER imply successful business verification. An online record may be
> successfully synchronized while simultaneously being subject to duplicate/clash review.***

> ⟦SOURCE⟧ **§12**: *"a device cannot perform a reliable server-wide duplicate/clash check while
> disconnected… **Never pretend an offline lead has passed the server-side clash gate.**"* ⚠ **AD-01 §1.1
> calls this *"the single clearest state definition in the entire spec"* — a named entry condition, exit
> condition, failure behaviour and an explicit prohibition on faking it.**

### 16.2 The ten scenarios

| # | Scenario | Missing decision | Owner |
|---|---|---|---|
| **P1** | Offline capture that turns out to be a **duplicate** on sync | ⚠ **When does the gate run for an ONLINE record — synchronously at save or asynchronously? The two paths must not have different timing semantics** | ⚠ **`N-1`, OPEN** |
| **P2** | Offline capture that syncs into an **attribution clash** | ⚠ **How does the system say *"there is a conflict"* to the rep without saying *"a CP claimed this prospect three days ago"*? ⟦SOURCE⟧ §11 forbids the disclosure; the gate requires the notification** | ⚠ **⟦SOURCE⟧ §11 states the constraint; no source states the mechanism. Adjacent to `AC-13`** |
| **P3** | Offline capture by a user **deactivated before the device reconnects** | ⚠ **Accept and leave unassigned with the historical actor preserved, or reject?** | ⚠ **`Q13`, OPEN; `X-4` couples it to `N-3`** |
| **P4** | **Backdated** disposition after a multi-day offline stretch | ⚠ **None missing in principle — EFFECTIVE and RECORDED time must be held separately (AD-01 `E-23`). ⚠ **What IS missing: whether a backdated act may be recorded into a period already REPORTED** | ⚠ **Unowned by any register item. Recorded as a SILENCE, not minted** |
| **P5** | **Two users disposition the same record concurrently** | ⚠ **Is last-writer-wins acceptable, and what must the loser be told? ⟦SOURCE⟧ §68 does not list disposition among its strong-consistency cases; §61 requires the loser be told** | ⚠ **`Q15`, OPEN** |
| **P6** | Concurrent transition into a **terminal** state, discarding the loser's next-action date | ⚠ **Same as P5, and it is the dangerous variant. ⚠ **Under the Inquiry-level model the risk SHIFTS: a lost race now writes a PRESERVED, UNREWRITABLE commercial closure** | ⚠ **`Q15`, OPEN — and its consequence is WORSE under `H-4` than AD-01 priced it** |
| **P7** | **Offline re-engagement** — a device cannot identify the prior Inquiry while disconnected | ⚠ **May a record exist briefly as an unlinked probable duplicate, and who links it?** | ⚠ **`T-9`, OPEN; interacts with `N-1` and `M-14`** |
| **P8** | **Offline caching of a record whose prior intervals are restricted** from the holding handler | ⚠ **What may a device cache? ⟦SOURCE⟧ §46 requires offline data minimisation and is SILENT on this case. A device-resident copy sits outside every server-side control** | ⚠ **`U-20`, OPEN; `M-14` owns the queue** |
| **P9** | A **transfer** performed while the receiving handler's device is offline | ⚠ **Does the conferral decision reach the device before the record does, and what does the device show in the interval?** | ⚠ **Unowned. ⚠ **It is `U-20`'s inverse and lands inside `M-14`'s subject. NOT MINTED — recorded as an ENLARGEMENT of `U-20`** |
| **P10** | **Idempotency** — a retried sync or import must not create a second copy | ⚠ **⟦SOURCE⟧ §93 states the requirement. ⚠ **What is missing is `Q3`: does IMPORT run the gate at all, what is an imported record's initial state, who is the handler, and do SLAs fire for a thousand records at once** | ⚠ **`Q3`, OPEN; `X-5`/`X-7` couple it to `N-4` and `N-1`** |

### 16.3 What this section establishes

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-362` — EIGHT OF THE TEN LAND ON AN EXISTING OPEN ITEM. TWO DO NOT, AND
> NEITHER IS MINTED.** ⚠ **P4 (backdating into a reported period) is a genuine silence with no carrier;
> it is RECORDED as a silence rather than minted, because ⟦SOURCE⟧ Spec Rule 1 requires silence be
> recorded, not filled, and because minting an item for a case no source describes would be inventing a
> requirement. **P9 lands inside `U-20`/`M-14` and is recorded as an enlargement.**
>
> ⚠ **AND ONE ASYMMETRY IS NAMED: the Inquiry-level relocation of the lifecycle makes `Q15` WORSE, not
> better.** **AD-01C §4 recorded that under Q5-B a lost race into `Dump` was *recoverable by creating a
> successor*, softening last-writer-wins. **Under `H-4` a lost race writes a MONOTONE, TERMINAL-ONCE,
> PRESERVED, UNREWRITABLE commercial closure on an Inquiry — and AD-01A §8.3 forbids the overwrite.** **A
> concurrency defect now produces a permanent commercial falsehood rather than a recoverable one.** ⚠
> **`Q15` IS NOT ANSWERED, NOT NARROWED AND GIVEN NO PREFERRED BRANCH. Its CONSEQUENCE is restated.**

---

## 17. State-machine reconciliation — the independent axes

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `Q`, and its explicit instruction: **DO NOT ASSUME A
> SINGLE LEAD LIFECYCLE STATE MACHINE IS APPROPRIATE.** **Nine axes are tested for independence, and the
> ORIGINAL AD-01 design is reported honestly — including where a LATER document had to correct it.**

### 17.1 The nine axes, tested pairwise on AD-01 §3's own instrument

> ⟦SOURCE⟧ **AD-01 §3's test:** *"Two concepts belong on one axis only if a record can never legitimately
> be in both at once."* ⚠ **The test is the original document's own, and applying it to nine axes rather
> than eight is what this section adds.**

| # | Axis | Level | Persisted? | Independent of every other? |
|---|---|---|---|---|
| **1** | **Person identity** | Person | Yes | ⚠ **Yes — ⟦SOURCE⟧ §06/§07, `PO LOCKED`** |
| **2** | **Work context / queue condition** | Work record × custody interval | ⚠ **No — derived** | ⚠ **Yes.** A record is out of A's queue and in B's with no commercial fact changing |
| **3** | **Commercial lifecycle** | ⚠ **Inquiry** | Yes, monotone, terminal-once | ⚠ **Yes.** Example 3: A closed, B live, at one instant |
| **4** | **Assignment / custody** | Assignment Log | ⚠ **No axis — zero persisted values (`PO LOCKED`)** | ⚠ **Yes.** A reassigned record keeps whatever lifecycle it had (AD-01 `E-04`) |
| **5** | **Next action** | Work record | Yes (the commitment) | ⚠ **Yes for TIMING; ⚠ `Y-2` for CARDINALITY if `Y-1` is *yes*** |
| **6** | **Disposition reason** | Inquiry | ⚠ **Yes, mandatory, preserved** | ⚠ **DEPENDENT on axis 3, not orthogonal to it — a reason is only meaningful on a terminal value. AD-01 §3.7 states this correctly** |
| **7** | **CP attribution** | ⚠ **Inquiry (Stage-1) AND Booking (Stage-2)** | Yes, append-only | ⚠ **Yes — and `PO-W1` LOCKS that Stage-1 does not determine Stage-2** |
| **8** | **CP registration** | ⚠ **(CP, client, project) triple** | Yes, immutable, windowed | ⚠ **Yes — `PO-AB1`/`PO-AB3`: LIVE ≠ automatic commission, EXPIRED ≠ automatic disqualification, NO registration ≠ automatic disqualification** |
| **9** | **Booking** | Booking | ⚠ **Yes — its own state machine** | ⚠ **Yes — `PO LOCKED` (AD-01A §8.2): the Lead Lifecycle must NOT absorb Booking states** |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-363` — ALL NINE ARE INDEPENDENT, WITH EXACTLY ONE DEPENDENCY AND ONE
> UNDECIDED CARDINALITY.** **Axis 6 is DEPENDENT on axis 3 by design and AD-01 §3.7 says so. Axis 5's
> cardinality hangs on `Y-1`/`Y-2`. **Everything else clears the test.** ⚠ **A SINGLE COUPLED STATE
> MACHINE IS THEREFORE NOT APPROPRIATE, AND THE CORPUS'S ANSWER SURVIVES AN INDEPENDENT RE-TEST.**

### 17.2 The five couplings the ORIGINAL design made, reported honestly

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-364` — THE OWNER'S INVESTIGATION `Q` ASKS SPECIFICALLY WHETHER AN EARLY
> VERSION COUPLED AXES THAT LATER TURNED OUT TO NEED INDEPENDENCE. **IT DID, FIVE TIMES, AND EACH IS
> TRACED TO THE DOCUMENT THAT UN-COUPLED IT.**

| # | Coupling in AD-01 as first written | Un-coupled by | Status of the un-coupling |
|---|---|---|---|
| **1** | ⚠ **Transport SYNC and business VERIFICATION on ONE axis** (§5.1: *Verified · Pending Sync · Sync Conflict*) | **AD-01A §4** — *"these are two genuinely independent concepts, and merging them introduced a CORRECTNESS DEFECT into AD-01, not merely an inelegance"* | ⚠ **`PO LOCKED` — AD-01A §8.4. **The original document's strongest claim was that it had been WRONG, and the owner agreed** |
| **2** | ⚠ **A PERSISTED ASSIGNMENT-CONDITION AXIS** (§5.1, §6.3: *Unassigned · Assigned · Pending Enrichment · Reassignment Pending*) | **AD-01A §5** — the minimal persisted footprint is ZERO; *Unassigned*/*Assigned* is *"a shadow copy of the Handler reference"*; *Reassignment Pending* is *"a state for a workflow the spec does not define"* | ⚠ **`PO LOCKED` — AD-01A §8.5** |
| **3** | ⚠ **The LIFECYCLE coupled to the RECORD** (§5.1's four values at the Lead level) | **AD-01H `H-4`** — Model D: the Inquiry carries the commercial disposition; the work record carries NO persisted lifecycle value | ⚠ **`ARCHITECT DERIVED`. **The amendment `A-1` is NAMED AND NOT MADE, and only the Product Owner may make it** |
| **4** | ⚠ **COMMERCIAL CLOSURE coupled to QUEUE EXIT** in one three-value disposition control (⟦SOURCE⟧ §13; AD-01 §5.1's `Dump`) | **AD-01F §10.2 → AD-01H `H-6` → AD-01I `I-1`** — three independent axes and two derived projections | ⚠ **`ARCHITECT DERIVED`. **The Product Owner STATED the duality himself (consolidated §11) and asked that the semantics be kept precise** |
| **5** | ⚠ **ATTRIBUTION modelled as ONE axis with a tri-valued contest state** (§3.6, §5.1: *Uncontested · Contested · Resolved*) | ⚠ **The CP chain — `PO-W1`/`PO-X1`/`PO-AA1`: Stage-1 at the Inquiry, Stage-2 at the Booking, and `C-XXV` making the NULL OUTCOME a positively recorded Booking-level fact** | ⚠ **Stage separation is `PO LOCKED`; `C-XXV` is `ARCHITECT DERIVED` and unratifiable before `AC-47`. **AD-01A §8.6's enumeration still carries the single-axis form — `A-83`, named and not made** |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **WHAT THE FIVE HAVE IN COMMON, AND IT IS THE AUDIT'S MOST GENERAL RESULT.**
> ⚠ **In none of the five was the SEPARATION TEST wrong. AD-01 §3 applied it correctly to eight concepts
> and its conclusions about WHICH concepts are distinct have never been overturned.** **What AD-01 got
> wrong, five times, is the LEVEL at which each axis attaches — record versus Inquiry versus custody
> interval versus act versus Booking.** ⚠ **AD-01F `A-14` reached this verdict in one sentence
> (*"What the new evidence changes is the LEVEL at which each axis attaches. That is a material
> refinement, not a refutation"*) and this audit reaches it independently, from a different direction,
> across five couplings rather than one.** ⚠ **THAT IS A CONFIRMATION OF AD-01F `A-14`, and it is
> recorded as deliberately as a correction would be.**

### 17.3 The `Blocked` residue

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-365` — ONE AXIS IS NEITHER CONFIRMED NOR ELIMINATED AND HAS BEEN IN THAT
> STATE SINCE AD-01B.** ⟦PRODUCT-OWNER CLARIFICATION — AD-01C §1.2⟧ ***"Blocked is NOT purely
> timing-derived if it represents who owes the next move. Treat Blocked as an orthogonal operational
> qualifier whose exact persistence/derivation mechanism remains unresolved under `N-2`. Do NOT reopen
> `Q1`. Do NOT resolve `N-2` in this task."***
>
> | What it settles | What it does not |
> |---|---|
> | ⚠ **Branch (a) — *Blocked is derived from timing alone* — is ELIMINATED** | ⚠ **`N-2` in EVERY part: mechanism, values, and which values SUPPRESS ⟦SOURCE⟧ §58 escalation** |
> | ⚠ **`N-2` has a subject** | ⚠ **Branch (b) — *therefore stored* — is NOT established. AD-01C names a THIRD possibility: derived from a NON-TIMING input the system already holds. Neither chosen nor excluded** |
> | ⚠ **`Q1` is UNTOUCHED** | ⚠ **`X-6` — `Q14`'s escalation thresholds are still obstructed** |
> | — | ⚠ **`T-12` — AD-01A §8.6's phrasing is INACCURATE AS WRITTEN for `Blocked`, and its corrective note is owed by whoever resolves `N-2`** |
>
> ⚠ **AND ONE THING THIS AUDIT ADDS: `N-2` IS ON THE CRITICAL PATH OF THE CORE SALES WORKFLOW, NOT ONLY
> OF ESCALATION.** **AD-01F `C-7`/`V-23` established it: the record's operational condition BETWEEN
> booking initiation and Sales Support approval was answered by AD-01A §2.1 as *"remains Follow-up, with
> its waiting-on qualifier set to the internal builder verification function"* — **which depends entirely
> on `N-2`.** ⚠ **Either the rep is nagged to follow up a prospect who is in verification, or the record
> leaves the queue with no stated basis. `V-23` and `N-2` are BOTH OPEN.**

### 17.4 The one axis the Orthogonal Lead Model does not enumerate

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-366` — AD-01A §8.6's ENUMERATION HAS NINE COMPONENTS AND THE ARCHITECTURE
> NOW HAS AT LEAST ELEVEN.**
>
> | AD-01A §8.6's nine | Present state |
> |---|---|
> | Lead Lifecycle (persisted, four values) | ⚠ **Challenged as to LEVEL and PERSISTENCE — `A-1`, not made** |
> | Action / Next-Action conditions (derived) | ⚠ **Confirmed, except `Blocked` — `T-12`, not made** |
> | Activity history | Confirmed |
> | Assignment data/history (derived/log-based) | ⚠ **Confirmed, and asked to carry THREE unapproved facts** |
> | Attribution / Clash (persisted, on the claim entity) | ⚠ **INCOMPLETE — the CP chain has TWO decisions at TWO levels** |
> | Sync transport state (not a lead axis) | Confirmed |
> | Verification / Conflict state (persisted) | Confirmed |
> | Independent Booking lifecycle | ⚠ **Confirmed and LOAD-BEARING: Stage-2, the Broker/Sub-Broker category and the DIRECT mark are all per-Booking** |
> | Dump disposition classification | ⚠ **Confirmed as a FRAMEWORK; its values (`N-4`) and scope (`Z-5`) OPEN** |
> | ⚠ **MISSING — `CP Project Registration`** | ⚠ **A tenth object with its own window, expiry, revisit, immutability, competing-registration and history rules (`PO-N`, `PO-O`, `PO-AB1`). It did not exist when §8.6 was written** |
> | ⚠ **MISSING — the Stage-2 allocation decision** | ⚠ **An eleventh object: a Booking-level commercial determination with four outcomes, which `C-XXV` makes positively recorded even when NULL** |
>
> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`A-83` — AN AMENDMENT TO AD-01A §8.6's ENUMERATION IS NAMED AND IS NOT MADE.
> It concerns COMPLETENESS only, never any decision §8.6 records. **It shares an editorial pass with `A-1`
> (level and persistence) and `T-12` (the `Blocked` phrasing): THREE pending corrections to one recorded
> Product-Owner decision, none of them made, and only the Product Owner may make any of them.**

---

## 18. Contradictions

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `R`: an INDEPENDENT, NON-TRUSTING hunt across fifteen
> named topic areas. **Prior documents' contradiction lists are NOT carried; the hunt is run fresh, and
> where it returns nothing that NULL RESULT is reported rather than left implicit** — AD-01Y's own
> standard. **NO PRIOR DOCUMENT IS EDITED, AND NO CONTRADICTION IS SILENTLY REPAIRED.**

### 18.1 `X-35`, stated in full

> ⚠ ⟦BUSINESS DECISION REQUIRED — `X-35`, MINTED⟧ **THE FOUNDATIONAL SUBSTRATE'S STATUS.**
>
> **AD-01L §2.2 records `V-1`, `V-2`/`V-3` and `V-11`/`V-12` as ⟦PRODUCT-OWNER DECISION⟧ … *"Approved.
> NOT reopened"*; AD-01P §3 writes *"`V-1` (approved)"*. **AD-01G, AD-01H and AD-01I — at their own mints
> — carry `STATUS: PROPOSED — NOT APPROVED`, label the same findings ⟦ARCHITECT RECOMMENDATION⟧ *"NOT
> APPROVED"*, and AD-01I §1.5 says they are *"treated as INPUT."* **AD-01K §19 records `V-1 … V-15` as
> *"not resolved."***
>
> **Extent:** ⚠ **TWO documents assert approval; THREE deny it at their mints; ONE records non-resolution;
> EIGHTEEN carry a standing *"not reopened"* clause that reads as a status claim.**
> **Consequence:** ⚠ **`K-18`, `P-G`, every Stage-1 scoping rule, `NF-24 … NF-28`, `C-XXI … C-XXV` and
> `M-9`'s whole input list presuppose the Inquiry as a commercial object. FIVE documents record, in
> terms, that the Sales Episode rejection **does not stand** if `V-1` resolves toward Model A.**
> **Remedy:** ⚠ **`A-81`, NAMED AND NOT MADE** (the status labels are the architect's, not the owner's).
> **The substantive question is `AC-49`.**

### 18.2 `X-28`, enlarged and not renumbered

> ⚠ **See [§11.4](#114-x-28s-true-extent-and-its-origin). EXTENT ENLARGED BY AT LEAST EIGHT INSTANCES IN
> FOUR PREVIOUSLY UNSURVEYED DOCUMENTS; ORIGIN IDENTIFIED AS AD-01 §7 `E-13`. **NOT RENUMBERED, NOT
> RE-MINTED, NOT RESTATED. `A-82` named and not made.**

### 18.3 The fifteen topic areas, each with its result

| # | Topic area | Result |
|---|---|---|
| **1** | **Lead scope — §06 vs consolidated §4/§7/§13** | ⚠ **`C-1` STANDS, UNRESOLVED BY ANY PRODUCT-OWNER STATEMENT.** AD-01G `G-2`'s *"extended, not superseded"* reading is the recommended resolution and is unapproved. **This is `X-35`'s substance** |
| **2** | **Lifecycle level** | ⚠ **AD-01A §8.1 (`PO LOCKED`, four values at the Lead level) vs `H-4` (zero persisted at the Lead level). `A-1` NAMED AND NOT MADE. **A recorded PO decision and an architect recommendation are both live and incompatible as literally written** |
| **3** | **`New`** | ⚠ **Same as topic 2, and it is the instance the thirteen starting-point statements got wrong** — [§1.5](#15-the-thirteen-starting-point-statements-audited-individually) statement 9 |
| **4** | **`Blocked`** | ⚠ **AD-01A §8.6's phrasing INACCURATE AS WRITTEN. `T-12`, open. Diagnosed at AD-01B §7.1, clarified by the owner at AD-01C §1.2, still uncorrected** |
| **5** | **`Dump`** | ⚠ **NO CONTRADICTION FOUND. `I-1`'s decomposition is internally consistent and the CP chain adopted the Inquiry-level reading verbatim (`L-2`). **ONE over-generalisation carried: `H-6(c)` vs `I-10`, with `Z-2` open** |
| **6** | **Assignment** | ⚠ **NO CONTRADICTION. Three ADDITIONS to the Assignment Log, all legitimate in kind under §8.5's own escape clause, NONE approved, and the aggregate stated nowhere until now** |
| **7** | **History visibility** | ⚠ **ONE CORRECTION ALREADY ON THE RECORD AND CONFIRMED: AD-01G §8.2 corrects AD-01F §9.3/`F-8`'s finding that the union *"cannot express transfer without history."* **The union expresses every DESCRIBED case; what it cannot express is an UNDESCRIBED one (`W-3`)** |
| **8** | **Timeline** | ⚠ **ONE TENSION, NEWLY NAMED: `H-9` requires every entry to resolve to WHICH Inquiry it concerns; `W-1` leaves the cardinality open and AD-01I records ZERO as already permitted. **Enlargement of `W-1`, not a new item** |
| **9** | **First response / FUT** | ⚠ **NO CONTRADICTION — FOUR UNDEFINED BOUNDARIES. `V-4`, `V-20`, `W-1`, `W-4`. All open** |
| **10** | **Action feed / derived buckets** | ⚠ **NO CONTRADICTION. `PO LOCKED` and confirmed from four directions** |
| **11** | **CP attribution** | ⚠ **`X-28` ENLARGED; `X-35` MINTED. The ANCHOR is compatible and was anticipated; the ENUMERATION is incomplete (`A-83`)** |
| **12** | **Duplicate / clash** | ⚠ **NO CONTRADICTION. The two questions are confirmed different at different levels from both sides of the seam. **`T-1`'s REPLACEMENT requirement (detection must route a re-contact toward a new INQUIRY, not a new RECORD) is unowned by any register item — recorded, not minted** |
| **13** | **Redistribution** | ⚠ **NO CONTRADICTION. The prohibition holds across all ten scenarios and is STRUCTURAL (`J-16`/`J-42`). **Six of ten are blocked on open items** |
| **14** | **Reporting** | ⚠ **NO CONTRADICTION — THIRTEEN UNDEFINED DENOMINATORS, ALL LANDING ON EXISTING ITEMS. Zero new reporting items minted** |
| **15** | **Authorization / offline** | ⚠ **NO CONTRADICTION. ELEVEN of thirteen operations inexpressible; eight of ten offline scenarios land on existing items; two recorded as silences** |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-367` — THE HUNT'S SHAPE IS REPORTED AS A RESULT. Fifteen areas searched;
> TWO contradictions found (one new, one enlarged); FOUR pre-existing incompatibilities confirmed as
> standing (topics 2, 3, 4 and 7's already-recorded correction); NINE areas returned NO new contradiction.
> ⚠ **The nine null results are reported explicitly, because a hunt that only reports hits cannot be
> distinguished from a hunt that was not run.**

### 18.4 Confirmations, recorded because a reconciliation that reports only conflicts is not one

| Prior position | Audit result |
|---|---|
| ⚠ **AD-01 §3's eight-concept separation** | ⚠ **CONFIRMED, and extended to NINE axes. Its TEST has never been overturned; only the LEVELS moved** |
| ⚠ **AD-01 §2.2 — Today/Future are buckets on a screen** | ⚠ **CONFIRMED. `PO LOCKED` and unimproved in four documents** |
| ⚠ **AD-01 §2.5 — `Dump` ≠ `Lost`** | ⚠ **CONFIRMED. It is the root of `I-1`'s whole decomposition and the FIRST document got it right** |
| ⚠ **AD-01 §3.6 — a single-valued column cannot represent a contested fact** | ⚠ **CONFIRMED, and it is what made the CP chain's Inquiry anchor available rather than retrofitted** |
| ⚠ **AD-01A §8.4 — sync ≠ verification** | ⚠ **CONFIRMED and GENERALISED — consolidated §18/§19 show the identical principle at two further boundaries; *recorded ≠ verified* is a product-wide rule (§31)** |
| ⚠ **AD-01A §8.5 — no persisted assignment axis** | ⚠ **CONFIRMED. Every subsequent document has added to the LOG, which is exactly what §8.5 directed** |
| ⚠ **AD-01A §8.3 — the three-dimension framework** | ⚠ **CONFIRMED, empirically validated against fifteen real reason values (AD-01F §10.3), and LOAD-BEARING far beyond its design purpose (`K-22`, `PO-R1`, `J-10`). **Its values remain `N-4`** |
| ⚠ **AD-01A §8.2 / `Q4` — Success = §20 Stage 3** | ⚠ **CONFIRMED, NOT REOPENED, and STRENGTHENED by `H-8`: at the Inquiry level terminality is true without enforcement pressure** |
| ⚠ **AD-01C §3.7 / `Q6`** | ⚠ **CONFIRMED, NOT REOPENED. Its organising principle — *restate what was never true; never restate what was true at the time* — survives every case in this audit** |
| ⚠ **AD-01E §6.1 — custody is the wrong KIND of fact for entitlement** | ⚠ **CONFIRMED. Consolidated §17's scenario table never keys on *"is this the current handler"*** |
| ⚠ **AD-01F `A-14` — the separation survives; the LEVELS move** | ⚠ **CONFIRMED INDEPENDENTLY, from five couplings rather than one** |
| ⚠ **AD-01G `G-8` — read-time projection, never write-time mutation** | ⚠ **CONFIRMED and UNWEAKENED. `I-8` persists a fact about an ACT, never a projection of a condition** |
| ⚠ **AD-01G `G-9` — the custody interval is DERIVED, not an entity** | ⚠ **CONFIRMED. No new canonical entity is required anywhere in this audit** |
| ⚠ **AD-01H `H-10` / AD-01G `G-4` — the Sales Episode is unnecessary** | ⚠ **CONFIRMED for a SIXTH time, with its conditionality on `G-1` UNCHANGED and NOT made unconditional** |
| ⚠ **AD-01I `I-2` — the SOURCES separate queue exit from redistribution BY ACTOR** | ⚠ **CONFIRMED. It is stronger evidence than any structural argument and it is the Product Owner's own operation** |
| ⚠ **The CP chain's adoption of the Inquiry-level `Dump` reading** | ⚠ **CONFIRMED at source in `L-2`. The chain did NOT blur the separation** |

---

## 19. Product-Owner ledger changes

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **Owner's investigations `S` and `T`. **NO CLOSURE COUNT IS INFLATED. A
> restatement, a paraphrase, a confirmation and a derived consequence are each checked and NONE is
> counted as a closure.**

### 19.1 The headline results

| # | Result |
|---|---|
| **1** | ⚠ **ZERO items closed by this document, in any series.** |
| **2** | ⚠ **ONE `AC-` item minted — `AC-49` — and it is this document's single closing question.** |
| **3** | ⚠ **ONE contradiction minted — `X-35`. ONE enlarged without renumbering — `X-28`.** |
| **4** | ⚠ **THREE amendments named — `A-81`, `A-82`, `A-83`. ZERO made. The register becomes `A-1 … A-83` across twenty-one documents, and `Q0-e`'s list grows accordingly.** |
| **5** | ⚠ **ZERO constraints minted. The `C-` series stays at `C-XXV`** — [§19.5](#195-the-constraint-candidate-tested-and-not-minted). |
| **6** | ⚠ **ZERO Product-Owner clauses marked `SUPERSEDED` or `CONTRADICTED`.** **A thing is superseded when the owner supersedes it, and the owner has not. **What is marked `CONTRADICTED` is an ARCHITECT status label (`X-35`) and an ARCHITECT formulation (`X-28`).** |
| **7** | ⚠ **FOUR items ENLARGED without being answered — `W-3`, `W-1`, `V-8`, `AB-1` — plus `U-20` by one offline case. **ENLARGEMENT IS NOT AN ANSWER.** |
| **8** | ⚠ **`N-4`'s downstream load rises again: it now gates `N-4`-coded reporting, `T-4`'s answerability (`C-I`), `Z-2`, `Z-5`, `Q0-c`/`K-22` and `PO-R1`'s operand. AT LEAST FIVE DEPENDANTS.** |

### 19.2 The five-way taxonomy applied to the foundational register

| Item | Status | Moved by this document? |
|---|---|---|
| ⚠ **`Q1`'s decision** — four-value vocabulary, *Pending* rejected, Today/Future/Overdue derived | ⚠ **`PO LOCKED`** | ⚠ **NO. Its VOCABULARY is untouched; only its LEVEL/PERSISTENCE proposition is challenged, as `A-1`, which is not made** |
| ⚠ **`Q4`** — Success = §20 Stage 3 | ⚠ **`PO LOCKED`** | **NO. Not reopened.** |
| ⚠ **`Q6`** — Success terminal; corrections outside the ordinary machine | ⚠ **`PO LOCKED`** | **NO. Confirmed, not reopened.** |
| ⚠ **`Q7`** — the three-dimension framework | ⚠ **`PO LOCKED`** | ⚠ **NO. Confirmed a fifth time. NOT amended. `Z-5` refers one structural question and changes nothing** |
| ⚠ **Sync ≠ verification; no persisted assignment axis** | ⚠ **`PO LOCKED`** | **NO. Both confirmed.** |
| ⚠ **The `Blocked` clarification** (AD-01C §1.2) | ⚠ **`PO LOCKED`** | **NO.** ⚠ **`T-12`'s corrective note still owed** |
| ⚠ **The revival direction** (AD-01E §1.1) | ⚠ **`PO LOCKED` as a DIRECTION** | ⚠ **NO — and its SUBJECT has moved twice since (AD-01F `C-5`, AD-01G `G-5`) without the direction being restated. Recorded, not resolved** |
| ⚠ **`G-1`/`G-2`, `H-1`/`H-4`, `I-1`/`I-7`/`I-8`** | ⚠ **`ARCHITECT DERIVED` at their mints; recorded as `PO LOCKED` by AD-01L/AD-01P** | ⚠ **`CONTRADICTED` in STATUS — `X-35`. **Their CONTENT is neither superseded nor contradicted** |
| ⚠ **AD-01 §7 `E-12`, `E-13`** | ⚠ **`CONTRADICTED`** | ⚠ **YES — `X-28`'s eleventh and twelfth instances and its ORIGIN. `A-82` named, not made** |
| ⚠ **AD-01A §8.6's ENUMERATION** | ⚠ **`ARCHITECT DERIVED` content inside a `PO LOCKED` decision; INCOMPLETE as written** | ⚠ **YES — `A-83` named, not made** |
| ⚠ **`Q2`, `Q3`, `Q5`, `Q8 … Q16`** | ⚠ **`VALIDATE-OPEN`, all** | ⚠ **NONE answered. `Q5` is confirmed OPEN and NOT closed by the dissolution of its mechanical half. `Q11` confirmed STILL BLOCKING** |
| ⚠ **`N-1 … N-4`** | ⚠ **`VALIDATE-OPEN`, all** | ⚠ **NONE answered. `N-2` confirmed on the CORE WORKFLOW's critical path; `N-4`'s dependants recounted** |
| ⚠ **`T-1 … T-12`** | ⚠ **`VALIDATE-OPEN`** except `T-1`, recorded DISCHARGED-as-a-requirement by AD-01E `D10` | ⚠ **NONE answered. `T-1`'s REPLACEMENT requirement recorded as unowned. `T-4`/`T-5` untouched** |
| ⚠ **`U-1 … U-20`** | ⚠ **`VALIDATE-OPEN`** except `U-7` (recorded *effectively answered yes*) and `U-1`/`U-2`/`U-5` (relocated and partly answered by `J-3`/`J-19`/`J-28`) | ⚠ **NONE answered. `U-20` enlarged by one offline case** |
| ⚠ **`V-2 … V-26`** (other than `V-1`) | ⚠ **`VALIDATE-OPEN`, all** | ⚠ **NONE answered. `V-8` enlarged; `V-4` recorded as the cheapest unblocking question for a TWENTIETH consecutive document** |
| ⚠ **`W-1 … W-5`** | ⚠ **`VALIDATE-OPEN`, all five** | ⚠ **NONE answered. `W-3` ENLARGED by the bulk path; `W-1` ENLARGED to reach `H-9`'s formulation** |
| ⚠ **`Y-1 … Y-5`** | ⚠ **`VALIDATE-OPEN`, all five** | ⚠ **NONE answered. `Y-1` confirmed as gating the corpus's headline scenario; `Y-3` confirmed as gating the *Project Interest* vocabulary** |
| ⚠ **`Z-1 … Z-6`** | ⚠ **`VALIDATE-OPEN`, all six** | ⚠ **NONE answered. `Z-2`, `Z-4`, `Z-6` each confirmed as blocking a named redistribution scenario** |
| ⚠ **`M-2 … M-20`** | ⚠ **`VALIDATE-OPEN`** | ⚠ **NONE answered. Directly implicated and left to their owners: `M-2`, `M-3`, `M-5`, `M-6`, `M-7`, `M-8`, `M-9`, `M-14`** |

### 19.3 `AC-6`, `AC-26`, `AC-42`, `AC-47` and `AC-48` re-checked

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `S` requires an EXPLICIT re-check that each remains open
> unless INDEPENDENTLY answered by THIS audit. **Each is checked against what this document actually
> establishes, not against what it is adjacent to.**

| Item | Does anything in this audit answer it? | Determination |
|---|---|---|
| ⚠ **`AC-6`** — α/β boundary convention; is `N = 0` legal | ⚠ **NO, AND THE CHECK WAS RUN HARDER THAN THE NAME SUGGESTS BECAUSE §10.3 DISCUSSES DAY BOUNDARIES.** ⟦ARCHITECT ANALYSIS⟧ **The Action Feed's day boundary is a CLOCK question (`M-6`, `V-20`); `AC-6` is an ORDINAL DAY-COUNTING convention for a registration window. **ANSWERING ONE WITH THE OTHER WOULD BE ANSWERING A QUESTION WITH A FACT ABOUT A DIFFERENT OBJECT** | ⚠ **OPEN, BOTH LIMBS. NO CONVENTION SELECTED, PREFERRED OR IMPLIED. AD-01T's DISCREPANCY NOTICE CARRIED FORWARD UNCHANGED, NINTH CONSECUTIVE DOCUMENT** |
| ⚠ **`AC-26`** — is a registration's grounding power SINGLE-USE or REPEATABLE | ⚠ **NO. The one place this audit comes near it is reactivation scenario R8 (a new Inquiry after a conversion), which is `AC-26`'s POST-CONVERSION limb — and describing a scenario is not answering the cardinality question it raises. **`J-2` says a new Inquiry OPENS; it says nothing about how many claims one registration may ground** | ⚠ **OPEN, BOTH LIMBS. NOT narrowed, NOT branched. THIRTEENTH DOCUMENT** |
| ⚠ **`AC-42`** — the unit of CP performance; who owns a Booking for reporting | ⚠ **NO.** [§14](#14-reporting-semantics) checks all thirteen metrics against its nine limbs and adds NO tenth. **Metrics 12 and 13 land on limb 8 and on the *not-a-limb* determination respectively, both exactly as AD-01AB left them** | ⚠ **OPEN IN NINE LIMBS. ALL NINE CARRIED, NONE ANSWERED, NO TENTH ADDED. NO DENOMINATOR PROPOSED** |
| ⚠ **`AC-47`** — may a DIRECT Booking carry CP associations, categories or visibility records | ⚠ **NO. Not reached. [§17.4](#174-the-one-axis-the-orthogonal-lead-model-does-not-enumerate) notes that the Stage-2 decision is an object §8.6 does not enumerate — **which is a statement about an ENUMERATION, not about what a DIRECT Booking may carry** | ⚠ **OPEN, UNTOUCHED. It still blocks `C-XXV`'s ratification** |
| ⚠ **`AC-48`** — by what already-established rule may a Site Head revoke or change a recorded Stage-2 commission decision | ⚠ **NO. Not reached at all — this audit's subject is upstream of the commission decision** | ⚠ **OPEN, UNTOUCHED. It still blocks `M-9`** |

### 19.4 `AC-49`, minted

> ⚠ ⟦BUSINESS DECISION REQUIRED — `AC-49`, MINTED HERE⟧
>
> ⚠ ***Is the foundational `Lead` / `Inquiry` model — `V-1` (the work-record / Inquiry split, AD-01G
> `G-1`/`G-2`), `V-2`/`V-3` (concurrent Inquiry existence and Inquiry-level lifecycle, AD-01H
> `H-1`/`H-4`) and `V-11`/`V-12` (the `Dump` decomposition and redistribution model, AD-01I `I-1`/`I-7`)
> — `PO LOCKED`, as AD-01L §2.2 and AD-01P §3 record it, or `ARCHITECT DERIVED` and unapproved, as
> AD-01G, AD-01H and AD-01I record it at their own mints?***
>
> **Arises from:** ⚠ **`X-35`** — [§11.3](#113-x-35--the-status-contradiction-at-the-seam), [§18.1](#181-x-35-stated-in-full).
> **Source status:** ⚠ **No Product-Owner clause anywhere in the corpus resolves `C-1`, approves `G-1`,
> or ratifies `H-4` or `I-1`. AD-01G §1.4 records a PROVISIONAL PREFERENCE and expressly sets it aside,
> on the ground that *"a recorded preference is evidence about the operation, never a warrant for an
> architecture."***
> **Consequence if left open:** ⚠ **Eighteen documents of money-bearing architecture rest on a substrate
> whose status two of them assert and three of them deny. **If the substrate is locked, `A-81` is an
> editorial correction and nothing else moves. If it is not, then five documents' recorded conditionality
> is live — `G-4`, `H-10`, `I-9`, `J-52` and AD-01K each state that the Sales Episode rejection **does
> not stand** under Model A — and `K-18`'s attribution anchor, `P-G`, the `NF-` eligibility tests and
> `M-9`'s input list would each need re-basing.**
> **Not owned by any existing item:** ⚠ **Checked individually. `Q0-a … Q0-d` are CP-specific
> ratifications of CP claims; `Q0-e` ratifies AMENDMENTS (`A-1 … A-83`), not the model; AD-01AB §19.4
> item 12's approve/reject list runs AD-01N onward and does not reach AD-01G/H/I; `C-1` is a
> CONTRADICTION record, not a decision item, and AD-01F minted `V-1` for exactly this and never had it
> answered.**
> ⚠ **NO BRANCH IS PREFERRED, RANKED, RECOMMENDED OR IMPLIED. Either answer is coherent and they have
> materially different consequences.**

### 19.5 The constraint candidate, tested and NOT minted

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-368` — ONE CANDIDATE `C-XXVI` WAS CONSIDERED AND THE NECESSITY TEST
> FAILS, WHICH IS REPORTED AS A RESULT BECAUSE THE SERIES EXPECTS A CONSTRAINT FROM A DOCUMENT THIS SIZE.**
>
> **The candidate:** *"A summary or restatement of an architectural position must carry that position's
> approval status, and a position's status may only be changed by the Product Owner."*
>
> | Test | Result |
> |---|---|
> | **Is it necessary?** | ⚠ **NO. It is a PROCESS rule, not an architectural constraint. The `C-` series records constraints ON THE BUSINESS MODEL (`C-XXI` the allocation decision point, `C-XXIII` the per-Booking category, `C-XXV` the null outcome), not on how documents are written** |
> | **Is it already stated?** | ⚠ **YES, in substance and repeatedly: AD-01A §7 (*"delegation to an architect is not authorization"*), AD-01G §1.4, AD-01E §11.1 (*"a product-owner preference is not an approved architecture, and agreement is not ratification"*). **A constraint that restates an existing discipline adds a number and no content** |
> | **Would minting it help?** | ⚠ **NO. `X-35` is an instance of the discipline being breached, and the remedy for a breach is the correction (`A-81`) and the decision (`AC-49`), not a new rule** |
>
> ⚠ **THE `C-` SERIES STAYS AT `C-XXV`. IT IS NOT EXTENDED BECAUSE THE TEST FAILS, NOT BECAUSE NO
> CANDIDATE PRESENTED ITSELF.**

---

## 20. Remaining open questions

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-369` — the owner's standing instruction is *"keep auditing keep asking
> questions"*, and this section discharges it. **EVERY UNRESOLVED ITEM TOUCHING THIS DOMAIN IS LISTED
> EXPLICITLY AND INDIVIDUALLY. NOTHING IS OMITTED BECAUSE IT IS OLD, ADJACENT TO SOMETHING DISCHARGED, OR
> INCONVENIENT.**

### 20.1 The foundational register, in full

| # | Item | Status |
|---|---|---|
| **1** | ⚠ **`AC-49`** — is the foundational `Lead`/`Inquiry` model `PO LOCKED` or `ARCHITECT DERIVED`? | ⚠ **OPEN — MINTED HERE. THIS DOCUMENT'S SINGLE QUESTION** |
| **2** | ⚠ **`V-4`** — does a non-connected attempt count as a QUALIFYING follow-up? | ⚠ **OPEN. Gates `New` (three predicates), FUT, four first-response clocks, `W-1` and `W-4`. **THE CHEAPEST UNBLOCKING QUESTION FOR A TWENTIETH CONSECUTIVE DOCUMENT** |
| **3** | ⚠ **`Y-1`** — may two Inquiries be simultaneously non-terminal? · ⚠ **`Y-2`** — if so, does the record still hold ONE next-action commitment? | ⚠ **BOTH OPEN. `Y-1` gates the corpus's headline scenario** |
| **4** | ⚠ **`Y-3`** — is a *Project Interest* an Inquiry or an AGGREGATE? · ⚠ **`Y-4`** — does a Success change the whole record's queue behaviour? · ⚠ **`Y-5`** — which count is *"leads"*? | ⚠ **ALL THREE OPEN. `Y-4` BLOCKS redistribution scenario M5** |
| **5** | ⚠ **`W-1`** — may one activity relate to several Inquiries? · ⚠ **`W-2`** — which entity is called *Lead*? · ⚠ **`W-3`** — the eighth case · ⚠ **`W-4`** — inquiry→first-response · ⚠ **`W-5`** — may a restriction ever apply to management breadth? | ⚠ **ALL FIVE OPEN. `W-1` ENLARGED to reach `H-9`; `W-3` ENLARGED by the bulk path** |
| **6** | ⚠ **`Z-1`** — does a work mandate END? · ⚠ **`Z-2`** — does validity class constrain redistribution? · ⚠ **`Z-3`** — where does a mandated record with no live Inquiry APPEAR? · ⚠ **`Z-4`** — is a failed re-attempt's outcome recorded? · ⚠ **`Z-5`** — closure SCOPE · ⚠ **`Z-6`** — uniform or per-record intent in a bulk act | ⚠ **ALL SIX OPEN. FOUR of them each BLOCK a named redistribution scenario** |
| **7** | ⚠ **`V-5`/`V-6`** (*New*'s scope and the `A-1` amendment) · **`V-7`** (ANSWERED by AD-01G; NOT reopened) · **`V-8`** (the transfer choice — ENLARGED) · **`V-9`/`V-10`** (existence vs content; the bound surfaces) | ⚠ **`V-5`, `V-6`, `V-8`, `V-9`, `V-10` OPEN** |
| **8** | ⚠ **`V-11`'s CONTROL half** · **`V-12`** · **`V-13`** (answered in substance by `V-12`, recorded as a consequence) | ⚠ **`V-11`'s control half and `V-12` OPEN** |
| **9** | ⚠ **`V-14`** (new opportunity vs continuation) · **`V-15`** (different-source re-inquiry) · **`V-16`** (the anchor — claim and eligibility halves RECOMMENDED at `K-18`/`K-48`, reach half OPEN) · **`V-17`** (funnel re-key) · **`V-18`** (Capture) | ⚠ **`V-14`'s attribution third = `T-4`, OPEN; `V-15`'s commercial half = `T-4`; `V-16`'s reach half OPEN; `V-17` OPEN** |
| **10** | ⚠ **`V-19`** (response vocabulary) · **`V-20`** (first-response clocks and timezone) · **`V-21`** (temperature — owned by `Q9`/`M-8`) · **`V-22`** (who sets Success) · **`V-23`** (the pre-approval condition) | ⚠ **ALL OPEN. `V-23` puts `N-2` on the CORE WORKFLOW's critical path** |
| **11** | ⚠ **`V-24`** (§08 project scope at record or Inquiry) · **`V-25`** (does participation entitlement ENDURE) · **`V-26`** (IVR intake metadata) | ⚠ **ALL OPEN. `V-25` is the SOLE determinant of the return-to-an-earlier-handler case** |
| **12** | ⚠ **`U-1 … U-20`** | ⚠ **OPEN except `U-7` (recorded effectively answered *yes*) and `U-1`/`U-2`/`U-5` (relocated, partly answered). `U-9`, `U-10`, `U-11`, `U-12`, `U-13`, `U-14`, `U-16`, `U-17`, `U-18`, `U-19`, `U-20` ALL OPEN** |
| **13** | ⚠ **`T-1 … T-12`** | ⚠ **OPEN. `T-4`/`T-5` carry the money; `T-6` = `V-22`; `T-7` (correction vocabulary) blocks authorization operation 11; `T-10`, `T-11`, `T-12` untouched. **`T-1`'s REPLACEMENT requirement is unowned** |
| **14** | ⚠ **`N-1`** (gate timing) · **`N-2`** (the `Blocked` mechanism) · **`N-3`** (does deactivation clear Handler) · **`N-4`** (Dump reason values) | ⚠ **ALL FOUR OPEN. `N-4` now has AT LEAST FIVE dependants across both domains** |
| **15** | ⚠ **`Q2`, `Q3`, `Q5`, `Q8`, `Q9`, `Q10`, `Q11`, `Q12`, `Q13`, `Q14`, `Q15`, `Q16`** | ⚠ **ALL OPEN. `Q11` STILL BLOCKING for a TWENTIETH consecutive document. `Q15`'s CONSEQUENCE is WORSE under `H-4`** |
| **16** | ⚠ **`M-2`, `M-3`, `M-5`, `M-6`, `M-7`, `M-8`, `M-9`, `M-14`** | ⚠ **ALL OPEN. `M-3`/`M-7` block ELEVEN of thirteen authorization operations in EXPRESSION; `M-5`'s uniqueness half blocks `Q10`; `M-9` still cannot start** |
| **17** | ⚠ **`A-1`, `A-2`, `A-3`, `A-4`, `A-5` … `A-83`** | ⚠ **EIGHTY-THREE amendments NAMED AND NOT MADE across twenty-one documents. `Q0-e`'s list grows to `A-1 … A-83`** |
| **18** | ⚠ **The CP register** — `AC-2 … AC-48`, `X-25`/`A-37`, `X-28 … X-34`, `C-XXI … C-XXV`, `Q0-a … Q0-e` | ⚠ **ALL AS AD-01AB LEFT THEM. NONE answered here. `X-28` ENLARGED** |
| **19** | ⚠ **The `Broker`, `DIRECT` and *tagging* vocabularies · `W-2` (which entity is called *Lead*)** | ⚠ **FOUR vocabulary matters, ALL the owner's under ⟦SOURCE⟧ §88. **NO TERM IS PROPOSED FOR ANY OF THEM** |
| **20** | ⚠ **The Audit Completeness Gate** | ⚠ **DEFERRED by the owner to the end of the architecture phase. NOT opened, NOT designed, NOT partially designed** |

### 20.2 What remains explicitly OUT OF SCOPE for closure

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **Stated in terms rather than implied by omission, because a document that
> audits a whole domain is exactly the one a reader would take as a finish line.**
>
> ⚠ **THE `Lead` / `Client` / `Inquiry` / `Assignment` / `Dump` ARCHITECTURE IS NOT RECONCILED, NOT
> COMPLETE, NOT READY FOR IMPLEMENTATION AND NOT APPROVED. ONE CONTRADICTION WAS MINTED TODAY AND ONE WAS
> ENLARGED. THAT IS THE OPPOSITE OF A FINISH LINE.**

| # | Held out of closure | Why |
|---|---|---|
| **1** | ⚠ **The Lead/Inquiry split itself** | ⚠ **`AC-49`. Its status is the question** |
| **2** | ⚠ **The lifecycle's level and persistence** | ⚠ **`A-1` named and not made; a recorded PO decision says four values at the Lead level** |
| **3** | ⚠ **`Dump`'s control half, redistribution eligibility and the mandate** | ⚠ **`V-11`, `V-12`, `Z-1 … Z-6`, `I-8`/`A-2` — six open items and one unapproved mechanism** |
| **4** | ⚠ **History visibility** | ⚠ **`W-3`, `W-5`, `V-8`, `V-9`, `V-10`, `V-25`, `U-9 … U-20`** |
| **5** | ⚠ **Every metric's denominator** | ⚠ **Thirteen checked, thirteen undefined or partly undefined** |
| **6** | ⚠ **Every authorization operation's expression** | ⚠ **`M-3`/`M-7` unanswered; `R2` forbids the workaround** |
| **7** | ⚠ **Offline and concurrency behaviour** | ⚠ **`Q13`, `Q15`, `N-1`, `Q3`, `T-9`, `U-20`, `M-14` — and `Q15`'s consequence is WORSE than AD-01 priced it** |
| **8** | ⚠ **Every contradiction found or carried** | ⚠ **`X-35` (new), `X-28` (enlarged), `X-25`/`A-37`, `X-29 … X-34` — ALL UNDISCHARGED** |
| **9** | ⚠ **Cross-domain reconciliation as a whole** | ⚠ **[§11](#11-cp-attribution-interaction) checks whether the FOUNDATION is compatible with the CP chain's locked rules. **IT IS NOT A RECONCILIATION OF EVERY DOMAIN WITH EVERY OTHER, AND NO DOCUMENT IN THIS SERIES HAS PERFORMED ONE** |
| **10** | ⚠ **The Audit Completeness Gate** | ⚠ **LAST, BY THE OWNER'S EXPLICIT INSTRUCTION** |

### 20.3 The single question

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **`PF-370` — the owner's instruction is explicit: **do not manufacture a PO
> question; if a genuine PO decision is required, ask EXACTLY ONE; if none is necessary, say so in the
> exact required words.** **The test is run against what is actually open rather than against the habit of
> closing with a question, and against the habit of closing without one — AD-01AB ended with the
> no-question string, and a second consecutive empty result would be as unexamined a default as a
> manufactured question.** **THE CANDIDATES ARE WEIGHED INDIVIDUALLY BEFORE THE CONCLUSION IS STATED.**

| Candidate | Is it a NEW decision this reconciliation FORCES? |
|---|---|
| ⚠ **`AC-49`** — the foundational substrate's status | ⚠ **YES, AND IT IS THE ONLY ONE.** **It did not exist before this audit, because no document had read `V-1` at its mint and at AD-01L in the same pass. **It is not owned by `Q0-a … Q0-e`, by §19.4 item 12's approve/reject list, or by `C-1` (a contradiction record, not a decision item). **It is money-bearing through `K-18`, `P-G`, `NF-24 … NF-28` and `M-9`. **It is CHEAP — it asks for a status, not for an analysis. **And it is UNRECOVERABLE IN THE SAME SENSE the corpus uses that word: every further document built on the substrate deepens the exposure** |
| ⚠ **`W-3`** — the eighth case, enlarged by the bulk path | ⚠ **NO. It is an EXISTING item, minted at AD-01G §15.1 and open since. **ENLARGING AN ITEM IS NOT DISCOVERING A DECISION, and re-asking an existing open item as *the one question* would be PROMOTING rather than discovering** — AD-01AB §18.2's own standard |
| ⚠ **`V-4`** — the qualifying predicate | ⚠ **NO, AND IT WAS WEIGHED HARDEST OF ALL BECAUSE IT IS THE CHEAPEST AND MOST WIDELY-BLOCKING ITEM IN THE CORPUS.** ⚠ **It has been OPEN and NAMED for twenty consecutive documents. Asking it now would be promoting a known item, not surfacing a new one. **It is carried at [§20.1](#201-the-foundational-register-in-full) item 2 with its full dependant list** |
| ⚠ **`A-1`, `A-81`, `A-82`, `A-83`** | ⚠ **NO. FOUR amendments whose CONTENT is determined and whose application is a RATIFICATION step (`Q0-e`), not a decision to be made now** |
| ⚠ **`Y-1`, `Z-2`, `Z-4`, `Y-4`, `N-2`, `N-4`, `Q11`, `Q15`** | ⚠ **NO. ALL EIGHT WERE OPEN BEFORE THIS DOCUMENT AND ARE OPEN AFTER IT, UNCHANGED IN WHAT THEY ASK** |
| ⚠ **`AC-6`, `AC-26`, `AC-42`, `AC-47`, `AC-48`** | ⚠ **NO. ALL FIVE RE-CHECKED AT [§19.3](#193-ac-6-ac-26-ac-42-ac-47-and-ac-48-re-checked) AND NONE IS ANSWERED, NARROWED OR REACHED BY ANYTHING HERE** |
| ⚠ **Anything this document DISCOVERED that has no carrier** | ⚠ **TWO, AND NEITHER IS PROMOTED.** **(i) `T-1`'s replacement requirement — detection must route a re-contact toward a new INQUIRY rather than a new RECORD — is recorded at [§12.2](#122-the-open-items-this-area-carries-none-answered) as unowned; it is a REQUIREMENT ON `M-5`, not a business decision. **(ii) Backdating into an already-reported period (offline scenario P4) is recorded at [§16.3](#163-what-this-section-establishes) as a SILENCE; ⟦SOURCE⟧ Spec Rule 1 requires silence be RECORDED, not filled, and no source describes the case** |

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **THE CONCLUSION, STATED IN THE EXACT FORM THE OWNER REQUIRED.**
>
> > ⚠ **ONE NEW PRODUCT-OWNER DECISION IS REQUIRED BY THIS RECONCILIATION, AND IT IS A QUESTION ABOUT
> > STATUS RATHER THAN ABOUT BUSINESS POLICY — WHICH IS WHY IT SURVIVED THE MANUFACTURING TEST THAT
> > ELIMINATED EVERY OTHER CANDIDATE. THE MODEL IS SOUND; WHAT IS CONTRADICTED IS THE RECORD OF WHETHER
> > IT WAS EVER APPROVED.**

### 20.4 Recommendation

> ⚠ ⟦ARCHITECT RECOMMENDATION — advisory only⟧ *(A recommendation about **SEQUENCE and READINESS**. It is
> **NOT** an approval, **NOT** a self-authorization, and **NOT** permission to build anything. ⟦SOURCE⟧
> **§88** reserves every decision below to the Project Owner.)*
>
> ### **VALIDATE**
>
> ⚠ **The net position is WORSE than at AD-01AB, and it is worse by a category rather than by an item.**
> **`X-35` is not a new open question about the business — it is a discovery that a substrate eighteen
> documents rely on has never been approved. **`X-28` is not larger by one instance; it is larger by a
> region, and its ORIGIN turns out to predate every hunt that looked for it.** ⚠ **AGAINST THAT: nothing
> found here falsifies the MODEL. The Lead/Inquiry split, the Inquiry-level lifecycle, the `Dump`
> decomposition and the custody boundary all survived an adversarial re-test conducted without granting
> them incumbency, and five of AD-01's own couplings were confirmed as correctly un-coupled.**
> ⚠ **NOTHING MOVES TO BUILD. AD-01 THROUGH AD-01AB AND THIS DOCUMENT ALL REMAIN UNAPPROVED.**

**The gating list after this audit:**

| Step | Item | State |
|---|---|---|
| **1** | ⚠ **`AC-49`** — the substrate's status | ⚠ **NEW, AND FIRST. It is a status question, not an analysis, and every other item's meaning depends on the answer** |
| **2** | ⚠ **`V-4`** — the qualifying predicate | ⚠ **THE CHEAPEST ITEM IN THE CORPUS, blocking five quantities, for a TWENTIETH consecutive document** |
| **3** | ⚠ **`Y-1` with `Y-2`** | ⚠ **Decides whether the record's single next-action commitment survives** |
| **4** | ⚠ **`N-4`** — the Dump reason values | ⚠ **AT LEAST FIVE dependants across both domains, including a LOCKED money rule (`PO-R1`)** |
| **5** | ⚠ **`M-3` / `M-7`** — roles and breadth | ⚠ **ELEVEN of thirteen authorization operations cannot be EXPRESSED without them** |
| **6** | ⚠ **`W-3` with `V-8` and `Z-6`** | ⚠ **All three now couple, and the bulk path reaches `W-3` without anyone deciding to** |
| **7** | ⚠ **`Z-1 … Z-6`** | ⚠ **FOUR of six block a named redistribution scenario** |
| **8** | ⚠ **`T-4` with `T-5`, `V-15`, `V-16`'s reach half and `AA-3`** | ⚠ **AD-01K already recommended this sitting; unchanged** |
| **9** | ⚠ **Apply the amendments whose content is determined** — `A-1`, `A-81`, `A-82`, `A-83`, `T-12` | ⚠ **NONE APPLIED. Only the Product Owner may apply any of them** |
| **10** | ⚠ **`X-25`/`A-37`** — the audit substrate | ⚠ **Priority raised a TENTH consecutive time. `PO-Q2`'s reconstructability and operation 12 both rest on it** |
| **11** | ⚠ **`M-9`** — the commission model | ⚠ **STILL CANNOT START** |
| **12** | ⚠ **The Audit Completeness Gate** | ⚠ **LAST, BY THE OWNER'S OWN INSTRUCTION, AND NOT OPENED** |

### 20.5 What this document does not authorise

> ⚠ ⟦ARCHITECT ANALYSIS⟧ **Owner's investigation `U`, discharged explicitly.**
>
> ⚠ **NO SQL. NO SCHEMA. NO MIGRATION. NO CODE. NO UI. NO WORKFLOW. NO TABLE, COLUMN, TYPE, INDEX,
> ENDPOINT, SCREEN OR CONTROL. NO MASTER VALUE. NO BUSINESS RULE WHERE THE LEDGER IS SILENT. NO
> CONFLICT-RESOLUTION POLICY. NO REPORTING DENOMINATOR. NO SLA, THRESHOLD, HORIZON OR WORKING-HOUR RULE.
> NO ROLE, GRANT, BREADTH VALUE OR SCOPE RULE. NO DIMENSION ADDED TO AD-01A §8.3. NO TERM PROPOSED FOR
> ANY OF THE FOUR OPEN VOCABULARY MATTERS. NO PRIOR DOCUMENT EDITED. NO ITEM CLOSED. NO AMENDMENT MADE.**

---

## Closing note

**STATUS: PROPOSED — NOT APPROVED**

**NOT APPROVED FOR IMPLEMENTATION.**

Nothing in this document is decided, approved, implemented, implementable, seedable, migratable or
settled. **No schema, SQL, migration, master value, column, table or type is authorized by anything
above, and none may be derived from it.**

**None of `00` through `03ab` is modified by this document.** Where this analysis contradicts, corrects
or enlarges a prior position — AD-01L §2.2's and AD-01P §3's status labels (`X-35`, `A-81`); AD-01 §7
`E-12`/`E-13`'s commission formulation (`X-28`, `A-82`); AD-01A §8.6's enumeration (`A-83`); AD-01G
§8.2's ground for calling `W-3`'s eighth case undescribed; AD-01H `H-9`'s single-Inquiry presupposition —
**that is stated explicitly, with the document and section named and the reason given, and the prior file
is left exactly as it stands.**

In particular: **every recorded Product-Owner decision is treated as binding input throughout. `Q1`'s
vocabulary, `Q4`, `Q6`, `Q7`, the sync/verification split, the elimination of the assignment axis, the
`Blocked` clarification and every `PO-` clause from `PO-N1` to `PO-AB5` are NOT reopened, NOT re-argued,
NOT narrowed and NOT ranked.** **`Dump` semantics are NOT finalised. `N-2` is NOT resolved. `N-4` is NOT
resolved and no reason value is proposed. `V-4` is NOT resolved. `W-3` is NOT resolved. `T-4` and `T-5`
are NOT resolved. `AC-6`, `AC-26`, `AC-42`, `AC-47` and `AC-48` are NOT answered, narrowed or reached.
Blockers `M-2 … M-20` are NOT resolved. The Audit Completeness Gate is NOT opened.**

Every recommendation above sits in Master Spec **§88**'s **MUST ASK BEFORE DECIDING** column — canonical
entities, relationships, booking lifecycle, financial rules, CP commission logic, tenant architecture,
RLS, authorization, offline business behavior, source-of-truth rules and audit requirements are all
engaged. Per AD-01A §7, **delegation to an architect is not authorization**, and a Product-Owner
preference recorded as evidence is not an approved architecture. The analysis above is offered so that it
can be attacked on its merits rather than deferred to.

**Nothing here is implemented, and no prior document is modified, until the project owner approves in
writing.** Per **§97**: *when in doubt, STOP AND ASK.* This document is the asking.

### **THE ONE QUESTION**

> ⚠ ⟦BUSINESS DECISION REQUIRED — `AC-49`⟧
>
> ⚠ ***Is the foundational `Lead` / `Inquiry` model — `V-1` (AD-01G `G-1`/`G-2`: the Person-bounded work
> record and the project-bounded Inquiry as the commercial unit), `V-2`/`V-3` (AD-01H `H-1`/`H-4`:
> concurrent Inquiry existence, and the commercial lifecycle carried by the Inquiry with no persisted
> lifecycle value on the work record) and `V-11`/`V-12` (AD-01I `I-1`/`I-7`: `Dump` decomposed into an
> Inquiry-level commercial closure, a record-level derived queue condition and a management
> redistribution act that never creates an Inquiry) — `PO LOCKED`, as AD-01L §2.2 and AD-01P §3 record
> it, or `ARCHITECT DERIVED` and still unapproved, as AD-01G, AD-01H and AD-01I record it at their own
> mints?***

**STATUS: PROPOSED — NOT APPROVED**
